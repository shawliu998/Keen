# Changelog

- 2026-07-28: Promoted the BYOK provider slice into the current packaged local
  Alpha gate. A new frozen-sidecar smoke uses a loopback OpenAI-compatible API
  base ending in `/api/v1` and a fresh database to test model discovery,
  source import/indexing, a grounded generated answer, focused-study creation,
  an intentional missing-model failure, provider recovery, Session restore,
  token rotation and idempotent replay across three service generations. It
  runs against both the mounted DMG and the retained standalone app during
  `npm run package:macos`. The current arm64 DMG is 34,275,318 bytes with
  SHA-256
  `87072d5c06cda56a0c1a527523887fac03c985d03bbc8b080b8cfe197083e0e2`;
  checksum, matching app/sidecar architecture, embedded notices, deep ad-hoc
  signature and all three package smoke suites passed. Added a local-Alpha
  install and first-learning guide. No vendor key was used. `spctl` correctly
  rejects this ad-hoc, unnotarized build, so Developer ID, notarization and a
  clean-Mac public-install pass remain open.

- 2026-07-28: Added a provider-first native setup flow for a downloadable BYOK
  build without adding onboarding, navigation or another Agent surface.
  Settings now offers Ollama and OpenAI plus clearly labelled compatibility
  presets for DeepSeek, Anthropic Claude, Google Gemini, OpenRouter, Groq,
  Mistral, xAI, Qwen and Kimi, with an editable Custom API base. Known presets
  own their API base; custom endpoints reject final chat/models resource URLs.
  `Save and verify` exposes configuration save, supervised sidecar restart and
  model-access verification as separate states and automatically tests after a
  confirmed restart. Remote keys remain in macOS Keychain. URL composition now
  preserves provider bases such as `/api/v1`, `/openai/v1` and
  `/v1beta/openai` instead of guessing or duplicating `/v1`. Verification
  passed strict desktop TypeScript, 4 focused desktop files / 14 tests, 37
  focused Python tests and 6 scoped Rust tests. Per the requested acceptance
  boundary, no live vendor key was used and no provider/model combination is
  newly claimed as certified. ChatGPT Pro returned `A) ACCEPT` after the
  catalog-boundary, endpoint-ownership, state-machine, local-guidance and
  truthfulness revisions.

- 2026-07-27: Reframed the repository front page as a product-led portfolio
  narrative. The README now opens with a real learner failure-and-recovery
  story, distinguishes the Agent loop from a chat wrapper, shows verified
  native states, and separates current capability from unclaimed efficacy and
  release work. Added a product case study that records the author's role in
  problem framing, competitive analysis, interaction architecture, Agent
  boundaries, acceptance criteria, implementation orchestration, and native
  review without claiming sole manual authorship of the implementation.
  ChatGPT Pro reviewed the first draft and returned `REVISE`; the final
  structure applies its product-story, ordering, claim, and compression
  requirements and then received `ACCEPT README` in the second review. The
  public Block Buzz repository informed presentation structure only; no
  upstream code, copy, or assets were incorporated.

- 2026-07-27: Added the bounded Adaptive Prerequisite Intervention Loop without
  adding navigation, another Agent runtime or automatic plan mutation.
  Learning-core alone now projects an adaptive trigger when the same persisted
  Session has opening self-report `<= 0.4`, an incorrect deterministic Active
  Recall and a completed, profile/hash-matched validated Learning Intervention
  artifact. It can proactively request one source-grounded proposal only for
  the nearest future unstarted Unit. Server-derived idempotency binds Session
  revision, plan ID/version, target and complete trigger; start, artifact
  publish and Accept all revalidate the target and revision. Existing
  `agent_runs` and `study_plan_proposals` retain trigger evidence, learner
  Accept/Keep, versioned apply and Undo across cold restart. Deep Learn labels
  the proposal `Agent suggested · not applied`, explains `Why now`, reveals
  only an evidence count and waits for learner choice. Provider-backed
  explanation actions now disclose that the current answer and relevant cited
  excerpts are sent to the configured model provider; deterministic `Test me
  instead` does not. Recovery now also preserves the exact Practice if a crash
  occurs after Practice commit but before artifact checkpoint publication.
  Focused evidence passed 28 Python tests, 45 desktop tests and 9 API-contract
  tests; the six files that failed during an intentionally overlapped full
  desktop/build run passed 146/146 after the build completed. Strict
  TypeScript, ESLint, Ruff, `git diff --check` and the production frontend
  build passed. Pro returned `ACCEPT FINAL IMPLEMENTATION`; packaged adaptive
  provider evidence, pixel parity and learning efficacy remain unclaimed.

- 2026-07-27: Closed the isolated real-Provider Agent acceptance and stopped
  phase expansion. A fresh `com.keen.learning.provider-acceptance` profile
  saved only non-secret loopback configuration and natively tested
  `openai-compatible · deepseek-v4-flash` as connected. One real indexed
  Eigenvectors source advanced through a two-Unit focused path, Diagnostic
  `not_yet`, an incorrect Recall, host-selected `progressive-hint@1`, a
  contract-validated DeepSeek artifact and its canonical pending Practice.
  Immutable outcome lineage binds the exact Session, Unit, Recall, Agent run,
  artifact, Practice, Playbook definition and durable source handle/chunk/hash.
  Multiple full app/sidecar exits, rebuilds and cold starts retained one run,
  six events, one outcome, one Practice and one Recall with stable IDs and no
  duplicate provider execution or lineage. Native acceptance found two P1
  presentation/continuity defects: a successful artifact was hidden by its
  current Practice, and the real Agent's bounded Markdown math delimiters were
  displayed literally. The Agent artifact now restores beside its exact
  Practice, and inline/display notation reuses accessible native MathML.
  Focused desktop evidence passed 4 files / 52 tests with strict TypeScript,
  ESLint and `git diff --check`; final ad-hoc arm64 packaging and native cold
  restore passed. Pro returned `A) ACCEPT` and `STOP`: no further UI,
  navigation, schema, orchestration, Playbook, analytics or Agent-platform
  expansion is authorized without a new real product need.

- 2026-07-27: Closed the default packaged guided-learning vertical acceptance.
  In the dedicated `com.keen.learning.vertical-acceptance` profile, one real
  imported/indexed source advanced through a visible two-Unit focused path,
  Diagnostic, both lessons, two Recalls, source review, two Practices, Summary,
  scheduled Review, Home and History. Full app/sidecar restarts restored the
  exact Session during the journey and after completion; final read-only
  SQLite inspection retained one Session, two Recall runs, two Practice runs,
  one Review item, one Agent run and three adaptive actions with no duplicate.
  The provider-free profile truthfully exercised `provider_missing` after
  host-selected `progressive-hint@1`; no generated artifact or successful
  provider lineage is claimed. Native acceptance found and repaired stale
  intervention precedence during Practice restore, raw bounded inline formula
  delimiters, and mid-word deterministic source splitting. Focused desktop
  evidence passed 4 files / 50 tests with strict TypeScript and ESLint; focused
  Python evidence passed 20/20; `git diff --check`, sidecar freezing, the final
  ad-hoc arm64 app build and cold launch passed. Pro returned `A) ACCEPT` and
  limited the next package to real-provider acceptance without new UI, schema,
  coordinator, analytics or Agent-platform scope.

- 2026-07-27: Closed Slice 4's packaged service/durable-state restart gate.
  A fresh ad-hoc arm64 app/DMG rebuilt from current source with migrations
  `001–032`; DMG checksum, strict app/sidecar signature, bundled-sidecar and
  Review/FSRS restart/replay smokes passed. In an isolated temporary profile,
  public APIs created a real focused-study Task, non-empty Session, two-Unit
  plan and pending Diagnostic. A full packaged-sidecar stop/restart restored
  the exact Session, plan, revision 2 and pending checkpoint; History found the
  same Session, the creation command replayed, and all pre/post learning and
  Agent/lineage counts were identical. Pro returned `A) ACCEPT`. Native WebView
  hydration remains an independent non-blocking visual observation; Developer
  ID, notarization, provider execution and parity are not claimed.

- 2026-07-27: Added the single Slice 05-B deterministic built-in Playbook
  branch. The host selects immutable `progressive-hint@1` only when the exact
  current Unit has a persisted zero-weight Diagnostic `not_yet` user-report,
  the learner then independently fails Recall, and this is the first
  `explain_differently` intervention. Partial/confident/missing evidence and
  all successor explanation requests preserve the existing rephrase path;
  source-example and direct deterministic Practice are unchanged. The
  Diagnostic evidence ID/score, selected Playbook version/hash and outcome
  lineage persist without granting the model learning-state writes. A bounded
  legacy fingerprint restores pre-selection runs after upgrade and prevents a
  duplicate provider call. Pro accepted the contract; 58 focused tests and the
  full 1144-test Python suite passed, along with scoped Ruff and
  `git diff --check`. No new UI, API, migration, efficacy claim, learner trait
  or self-optimizing policy was added.

- 2026-07-27: Started Slice 5 with truthful intervention outcome lineage.
  Migration `032` records immutable exposure links from each durably published
  intervention artifact to the existing canonical Practice and, after Summary,
  that Practice's exact FSRS Review item lifecycle. Session queries hide
  lineage without the matching published checkpoint, use no raw learner
  response, and never select a session/course/concept-wide latest Review.
  Multiple steered artifacts may share one Practice; the record is explicitly
  non-causal. The public completion summary remains redacted. ChatGPT Pro
  accepted the architecture and Codex's revision-lifecycle correction.
  Focused intervention/migration/Summary/Review coverage passed 46 tests and
  the full Python suite passed 1141 tests; scoped Ruff checks and
  `git diff --check` passed. No UI, navigation, provider authority, mastery,
  grading, BKT, FSRS write authority or self-improving Playbook system was
  added.

- 2026-07-27: Continued Slice 4 Session Steward with one authoritative
  cross-page reconciliation path. Every confirmed Deep Learn Session change
  now invalidates the existing exact Session, adaptive state, per-course
  Session History, Learning Snapshot and due Review queries; no coordinator,
  duplicate store or background Agent was added. Home, Learning Feed and
  History withhold cached actions while those persisted reads are being
  revalidated, so a learner cannot briefly act on a stale phase after pause,
  resume, diagnostic, Recall, Practice, intervention or plan changes. A fresh
  QueryClient restart test restores the updated orphan Session from the
  existing service contracts rather than the prior in-memory value. Focused
  cross-page coverage passed 5 files / 76 tests and Review/Summary regression
  passed 2 files / 30 tests; strict TypeScript, ESLint and `git diff --check`
  passed. The current ad-hoc arm64 `.app` and unsigned DMG then rebuilt with
  migrations `001–031`; mounted-DMG verification, strict standalone signature,
  bundled-sidecar smoke and isolated Review/FSRS restart/replay passed. The
  packaged UI quit, released both processes, restarted from `binding` to
  `ready`, and restored the same real empty Home/Feed/History/Review state
  without creating a fixture in the user's profile. The DMG SHA-256 is
  `60e4535f15792b2d49184cc63fc24cb17dcf32a32979b4b48c4f8ed910e832f8`.
  A non-empty persisted Session across the packaged UI restart remains the
  Slice 4 completion gate; no user learning data, provider run, navigation,
  Developer ID signature or notarization was changed or claimed.

- 2026-07-27: Started the bounded Session Steward slice by repairing one real
  Home continuity gap. Learning Snapshot already exposed an incomplete
  `resume_study_session` candidate, but Home previously considered only
  persisted Tasks and could incorrectly show `Nothing queued` for a Session
  without an originating Task. Home now retains due Review and Task priority,
  then deterministically selects an existing resume candidate, resolves it
  against the exact current course Session, rejects missing, terminal or
  cross-course matches, and opens the precise Deep Learn route with the saved
  goal, progress and phase action. No Task, recommendation, Journey record,
  provider run or new navigation is created. Focused Home continuity/App
  coverage passed 3 files / 30 tests; strict TypeScript and ESLint passed.
  Cross-page restart reconciliation remains open before Slice 4 is complete.

- 2026-07-27: Closed the bounded Slice 3C Plan Proposal verification and moved
  the next Agent-native target to Session Steward. The existing implementation
  now has explicit Accept/Keep, transactional revision/plan/source
  revalidation, append-only accepted and restore plan versions, a truthful
  effective-after-current-step receipt, and safe Undo that fails closed after
  the learner advances or later evidence depends on the adjustment. Verification
  used isolated data and did not apply a decision to the user's saved learning
  records. The TypeScript response boundary was tightened to reject impossible
  ready/resolved apply states and accepted/rejected/undone receipt
  combinations. Python plan/migration tests passed 24/24, the desktop component
  passed 9/9, API-client contracts passed 5/5, and strict TypeScript and ESLint
  passed. The real-provider evidence remains the previously restored read-only
  proposal artifact; no real-provider decision, learning-efficacy, new
  navigation, vector expansion or unrestricted Agent authority is claimed.

- 2026-07-27: Completed the bounded supporting-page consistency pass without
  changing routes, runtime behavior or sample data. History and Review already
  use the same collection/queue empty-state composition and were retained.
  Knowledge Base course libraries are now compact 80 px filters rather than
  108 px dashboard-style cards, bringing the real Sources section 60 px
  earlier into the 1180×740 viewport. Browser interaction filtered five sample
  rows to two and restored all five; checks at 1180×740 and 1000×720 found zero
  horizontal overflow and zero unnamed buttons. Strict TypeScript, ESLint and
  3 focused files / 44 tests passed. Evidence is under
  `artifacts/ui-audit/2026-07-27/supporting-consistency/`. A fresh ad-hoc
  `Keen UI Audit.app` subsequently built with the bundled learning core; strict
  deep signature and bundled-sidecar smoke passed. Its native 1180×740 Dark
  window restored Knowledge Base, populated History and due Review with the
  service ready, then Command-Q left no app or sidecar process. Packaged
  reduced-motion, accepted-reference comparison, Developer ID/notarization and
  pixel parity remain unverified.

- 2026-07-27: Refined the bounded Deep Learn workspace without changing its
  persisted session, Recall, Practice or source contracts. The learning-path
  rail now fills the available desktop work area instead of ending mid-page
  and returns to content height in compact layout. Removed the repeated
  `sample · not saved` header copy because the shell already provides the
  single global Demo disclosure, compacted Pause, shortened the step label,
  and grouped Previous with the on-demand source action while retaining Next
  unit as the only primary action. The source remains after Recall in keyboard
  order. Browser inspection at 1180×740 and 1000×720 found zero horizontal
  overflow and zero unnamed buttons; the inspected run had no console errors.
  Strict TypeScript, ESLint and 3 focused files / 50 tests passed. This is
  Browser Demo verification, not packaged-WebView, Dark/reduced-motion,
  accepted-reference or pixel-parity evidence.

- 2026-07-26: Continued the bounded DeepTutor-informed UI convergence on
  Learning Feed and task detail without changing navigation, task state or
  runtime actions. The task rail is narrower and denser; current section and
  selected task use the restrained Keen teal state layer; titles no longer
  wrap around an absolutely positioned status badge; and the detail heading,
  metadata, learning sections and sticky next action share one reading
  alignment. Non-empty task sections now open their first task by default,
  closing detail returns focus to its task, and changing sections opens the new
  first item. The toolbar identifies the course with one compact close icon,
  Demo detail shows two connected steps, and Calendar is no longer duplicated
  in the empty context panel. Browser inspection at 1180×740 and 1000×720 found
  zero horizontal document overflow and zero selected-row overflow; the final
  1180×740 interaction pass found one selected row, one matching detail panel,
  zero unnamed buttons and no console errors. Strict TypeScript, ESLint and 2
  focused files / 44 tests passed. This is Browser Demo verification, not
  packaged-WebView, Dark/reduced-motion, accepted-reference or pixel-parity
  evidence.

- 2026-07-26: Reopened the bounded UI-shell phase at the user's direction and
  moved Home closer to the reviewed DeepTutor v1.5.1 visual structure without
  changing Agent or learning-core behavior. Direct screenshot comparison
  superseded the intermediate top-aligned draft. The final Keen adaptation uses
  a 192 px light sidebar, borderless Home toolbar, centered Keen mark and serif
  prompt, 680 px composer, Ask/Focused Study plus live source selection in the
  bottom toolbar, and one compact send action. DeepTutor branding, broad
  navigation and unavailable controls were not copied; saved-work Home remains
  unchanged. Added a restrained Keen-teal functional palette for primary
  actions, current navigation/mode and focus feedback, removed the textarea's
  duplicate internal focus line, and shortened the visible Demo status without
  removing its full disclosure. Browser inspection at 1180×740 found no
  wrapping or horizontal overflow. Strict TypeScript, ESLint and 3 focused
  files / 54 tests passed. Evidence is
  `artifacts/ui-audit/2026-07-26/home-deeptutor-accent-v3-1180x740.png`; this is
  not a packaged-WebView, captured Dark state, accepted-reference or
  pixel-parity claim.

- 2026-07-26: Completed the bounded UI closeout for the current guided-learning slice without adding another component direction or changing runtime behavior. The current sidecar-bundled `Keen UI Audit.app` retained its truthful 1180×740 minimum; native keyboard inspection confirmed that selecting the saved Upcoming task moves focus to its normalized heading and the next Tab reaches the sole `Open study` action. Browser-only responsive checks—explicitly not packaged-window claims—covered Feed at 1000×720 and 760×700, Deep Learn Summary at 1000×720 Dark, and History empty state at 760×700 Dark, all with reduced motion enabled, one main landmark, zero unnamed buttons and zero horizontal document overflow. A synthetic display-only long-title stress check also retained zero horizontal overflow at 1000 and 760 px. Evidence is under `artifacts/ui-audit/2026-07-26/release-closeout/`. No product code changed in this closeout; the prior 47 files / 538 tests, strict TypeScript, scoped ESLint, production build, ad-hoc Tauri bundle and Impeccable results remain the current implementation evidence. Packaged Dark/reduced-motion below the enforced native minimum, complete screen-reader coverage, accepted-reference diff, Developer ID/notarization and pixel parity remain open.

- 2026-07-26: Closed the supporting-page continuity gap between Learning Feed, Deep Learn and History. Generated task/session titles such as `Study very weak concept:` and `Study:` now receive one shared learner-facing display treatment without changing persisted values. Feed task detail replaces threshold/debug language and `Start / resume` with a clear learning reason, saved-step plan and one `Open study` action. Summary-ready History records suppress a repeated title/goal and misleading `0%` bar, expose the saved Recall/Practice result, and use the same `Finish and schedule review` handoff as Deep Learn and Home. A fresh sidecar-bundled `Keen UI Audit.app` was inspected at 1180×740 without opening a study or scheduling a review; evidence is under `artifacts/ui-audit/2026-07-26/supporting-continuity-native/`. The complete desktop suite passed 47 files / 538 tests; strict TypeScript, scoped ESLint, the production frontend build, ad-hoc Tauri app bundle and scoped Impeccable detection passed. Dark/reduced-motion, complete keyboard/screen-reader coverage, an accepted-reference diff, Developer ID/notarization and pixel parity remain open.

- 2026-07-26: Continued the HyperKnow-informed Deep Learn convergence without copying its crowded chat shell or changing the learning contract. Recall, targeted-practice and ready-to-finish results now use one flat outcome hierarchy: a short state label, a human-readable result, two restrained evidence rows and one truthful next action. Raw result headings such as `Incorrect`, and implementation-facing copy about local FSRS, mastery observations and unchanged schedules, were replaced with learner-facing guidance while preserved API writes, deterministic grading, recovery and scheduling behavior remain unchanged. The Agent phase is labeled `Learning Agent`; generated `Study:`/`Teach:`/`Review:`/`Plan:` prefixes are removed only from the Deep Learn display title, and the path rail's summary handoff now matches `Finish and schedule review`. A fresh sidecar-bundled `Keen UI Audit.app` restored the existing saved summary at 1180×740 and exposed the refined hierarchy without invoking the final review-scheduling write. The complete desktop suite passed 46 files / 531 tests; strict TypeScript, scoped ESLint, production frontend build, ad-hoc Tauri app bundle and scoped Impeccable detection passed. Native evidence is `artifacts/ui-audit/2026-07-26/deep-learn-flow-native/summary-ready-1180.png`. The existing Vite large-chunk warning remains; native Recall/Agent/Practice captures, packaged Dark/reduced-motion, complete keyboard/screen-reader coverage, accepted-reference diff, Developer ID/notarization and pixel parity remain open.

- 2026-07-26: Continued the bounded DeepTutor-inspired Knowledge Base convergence in a fresh sidecar-bundled `Keen UI Audit.app`. The selected PDF workspace now keeps reading primary: Availability and one `Start focused study` action share a restrained handoff row; indexing recovery appears only when relevant; course links and `Delete source` are grouped behind a collapsed `Source settings` disclosure instead of competing with the document. Native inspection at 1180×740 confirmed a live three-page PDF image, previous/next pagination, the primary study handoff, and both collapsed and expanded settings states without invoking a learning or destructive action. The complete desktop suite passed 46 files / 531 tests; strict TypeScript, ESLint, the production frontend build, the ad-hoc Tauri app bundle and scoped Impeccable detection passed. Evidence is under `artifacts/ui-audit/2026-07-26/knowledge-reader-native/`. The Debug PDF's initial page render took about 25 seconds and was not benchmarked; Dark/reduced-motion, complete keyboard/screen-reader coverage, an accepted-reference visual diff, Developer ID/notarization and pixel parity remain open.

- 2026-07-26: Completed the next packaged-WKWebView continuity pass with a fresh ad-hoc Debug `Keen UI Audit.app` built from the current frontend and the bundled learning-core sidecar. At the enforced 1180×740 native minimum, the sidecar reached ready and the app restored a saved Home action, populated History, a due Review, Learning Feed Upcoming/task detail, and a persisted Deep Learn Summary without making a learning write. Native Review inspection exposed matrices and vectors still rendered as raw bracket strings. Review prompts and expected answers now apply a dependency-free display layer for unambiguous matrices/vectors using native MathML, retaining the persisted source text and adding useful accessibility labels; the rebuilt app visibly renders the 2×2 matrix instead of `[[3, 0], [0, -2]]`. The complete desktop suite passed 46 files / 531 tests; strict TypeScript, ESLint, the production frontend build, the ad-hoc app bundle and scoped Impeccable detection passed. Evidence is under `artifacts/ui-audit/2026-07-26/packaged-supporting-v2/`. The exact Browser Demo zero-record states were not reproduced in the healthy packaged identity because its isolated fixture contains saved data; packaged Dark/reduced-motion, whole-product keyboard traversal, accepted-reference diff, Developer ID/notarization and pixel parity remain open.

- 2026-07-26: Continued the bounded DeepTutor Knowledge Center adaptation with a real selected-source workspace rather than the previous inline row expansion. Opening a source now enters a course-scoped two-column view with a persistent source rail, selected-file header, truthful availability/actions and a bounded content stage; switching files and returning to all libraries preserve the existing state. Browser Demo remains metadata-only and never requests file bytes. Live PDFs use the existing authenticated `getDocumentContent` boundary, PDF.js canvas rendering, safe 32 MiB response limit, cancellation, error/retry states and previous/next pagination; Markdown, text, image and unavailable content fail closed without invented previews. Browser checks measured a 248 px rail / 742 px preview at 1280×720 and a 220 px rail / 542 px preview at 900×700, with one `main`, zero horizontal overflow and zero unnamed buttons. Captures are under `artifacts/ui-audit/2026-07-26/deeptutor-knowledge/`. Strict TypeScript, ESLint, Impeccable detection and 3 focused desktop files / 70 tests passed. No DeepTutor code, brand, document content or screenshot asset was incorporated; a real packaged-app PDF run, Dark/reduced-motion, accepted visual diff and pixel parity remain unverified.

- 2026-07-26: Adapted the reviewed DeepTutor Knowledge Center overview into Keen's existing Knowledge Base without copying its retrieval-engine administration or adding unavailable capabilities. Real Keen course scopes now appear as a two-column `Course libraries` collection with truthful source/readiness counts; selecting a library filters the existing source table and synchronizes the course filter, while a second selection restores all sources. Browser Demo at 1280×800 measured the course grid at 992×226 px with the source table still beginning in the first viewport; the 900×700 layout retained two 377 px columns and exposed the Sources heading and first row in the same viewport. Both widths had zero horizontal overflow and zero unnamed buttons; the inspected desktop run had no console errors. Current captures are under `artifacts/ui-audit/2026-07-26/deeptutor-knowledge/`. Strict TypeScript, ESLint, Impeccable detection and 2 focused desktop files / 68 tests passed. DeepTutor code, routes, branding and screenshot assets were not incorporated. The selected-source entry above supersedes this checkpoint's earlier preview limitation; packaged WebView, Dark/reduced-motion and pixel parity remain unverified.

- 2026-07-26: Implemented the bounded Keen adaptation of the reviewed DeepTutor Chat/Home reference in the existing `New learning` state. The former heading, segmented-mode strip, source row and separate composer card are now one centered learning entry: Keen mark and serif prompt, one 720 px composer, Ask/Focused study controls and the truthful live source selector in the composer toolbar, plus one compact send action. The saved-work Home composition and all submission, source, persistence and recovery contracts remain unchanged; no DeepTutor code, logo, copy or runtime asset was incorporated. Browser Demo inspection at 1280×800 found zero horizontal overflow, zero unnamed buttons and no console errors; the current capture is `artifacts/ui-audit/2026-07-26/deeptutor-home/home-new-learning-browser-demo.png`. Strict TypeScript, ESLint and 3 focused desktop files / 45 tests passed. The upstream editable Figma replica remains In Review, packaged WebView and Dark/reduced-motion states were not re-run, and no pixel-parity claim is made.

- 2026-07-26: Rejected the first mixed DeepTutor/Keen Figma pair after visual review found it sparse, inconsistent and unlike the upstream product. Frames `238:157` and `238:327` are hidden and named Rejected. Official screenshot node `252:202` now provides the exact-aspect 1180×660 visual reference, and editable replica node `257:157` rebuilds its Geist/Lora typography, measured sidebar/composer geometry, vector controls, and reference-only upstream logo fills. Same-size ImageMagick comparison reported RMSE 0.0734996 and 2.12031% of pixels outside 5% fuzz; the white canvas limits that metric, so the replica remains In Review. No runtime, route or distributed asset changed, the Keen adaptation remains not started, and no pixel-parity claim is made.

- 2026-07-26: Completed Slice 3B's read-only Plan Proposal UI inside the existing Deep Learn Intervention flow. After a real Agent explanation, Keen can explicitly suggest one source-grounded prerequisite and show a flat current-versus-proposed sequence with durable citations; the artifact is visibly `not applied`, and Practice remains the only primary action. Restore, queued/running, exact uncertain retry, provider recovery, conflict refresh, stale and ready states are covered without an Apply/Keep control. Focused desktop tests passed 4 files / 61 tests; strict TypeScript, scoped ESLint, production build, ad-hoc Debug `.app` build, strict deep signature verification and the scoped Impeccable detector passed. The configured `deepseek-v4-flash` provider completed run `run-bc977440c88544de8af7063797f55ac7` and artifact `plan-proposal-artifact-7c0ad5c67df28f319ccde52c39be56bf`; the same diff restored after full app/sidecar restart. Read-only SQLite inspection retained Session revision 7, plan version 1 and two Units, with no apply-proposal row for that Session. Migration 032, Accept/Keep, deterministic CAS apply, receipt and safe Undo remain not started; no baseline, pixel-parity, Developer ID/notarization or learning-efficacy claim is made.

- 2026-07-26: Implemented Slice 3A's read-only Study Plan Proposal boundary without adding a fake Apply control or changing release navigation. An explicit authenticated request can propose one source-grounded prerequisite before a trusted unstarted Unit. The host freezes the exact Session revision, plan ID/version, Unit sequence, target and durable source snapshots; the purpose-specific Agent profile exposes only bounded source search and publishes a strict immutable artifact marked `unapplied` / `applyAvailable: false`. Exact retries reuse the run, while stale revisions, active targets and unissued source handles fail closed. Three backend vertical tests prove the proposal and provider-failure paths do not change plan versions/Units, Session state, mastery, Review, Tasks or misconceptions; three strict API-client tests cover source resolution, request validation and canonical routes. Ruff and strict desktop TypeScript passed. Plan Diff UI, real-provider acceptance, migration 032, Accept/Keep, deterministic CAS apply, receipt and safe Undo remain not started; no visual, package, parity or learning-efficacy claim is added.

- 2026-07-26: Completed the bounded selected-artifact lineage increment for Agent steering. A ready-state alternative now sends the visible predecessor run/artifact pair, preserves the exact pair and idempotency key across an uncertain retry, persists the predecessor in the new run input and immutable artifact, and rejects missing or stale predecessor identity. `Test me instead` applies the same current-head validation while reusing the existing Practice. Focused validation passed Python 12/12, desktop 14/14 and API-client 4/4, plus strict TypeScript and ESLint. The rebuilt ad-hoc `Keen Dev.app` then used the configured `deepseek-v4-flash` provider to create source-example run `run-fa4861c9139b4c11843a49c5aa8dd1ea` and artifact `artifact-11e7674c50be3d2f95790b2ca9d877a4`, both linked to the selected predecessor pair. The explanation and single existing Practice restored through a full app/sidecar restart; mastery events 4, Review items 1, Tasks 5 and misconceptions 0 remained unchanged. Strict deep ad-hoc signature verification passed. No new route, visual direction, learning-efficacy, Developer ID/notarization or parity claim is made.

- 2026-07-25: Rebuilt the current ad-hoc arm64 `Keen.app` after the Feed and Deep Learn anti-template fixes. The production frontend and release Rust binary built successfully; the app and bundled learning-core are arm64, and strict deep code-signature verification passed. A second native attempt to enable macOS Reduce Motion reached Accessibility but the System Settings control channel closed again when opening Display, including after reconnecting the control runtime. No system preference was changed, so packaged reduced-motion-on remains unverified and no native visual claim is added.

- 2026-07-25: Completed a bounded whole-product anti-template pass at 1180×760 across Home, Learning Feed empty/selected detail, Deep Learn, Knowledge Base, History, Review, and Settings. Two structural inconsistencies were repaired: Browser Demo Deep Learn now reuses the existing live two-column session header, reducing its header from 120 px to 74 px instead of placing Pause on a wasteful second row; Learning Feed's unselected detail no longer shows three empty pseudo-fields that looked like missing data. Both repaired pages retained zero horizontal document overflow. Focused validation passed 2 files / 50 tests, strict TypeScript and ESLint; the scoped Impeccable detector returned no findings. This is Browser Demo evidence only, not an accepted-reference diff, packaged-WebView closeout, or pixel-parity claim.

- 2026-07-25: Closed two Deep Learn usability edges without changing the learning contract. Browser Demo now has a regression-tested keyboard path from the session control through the learning-path rail to the recall action. The reading column inherits `overflow-wrap: anywhere`, so long formulas, identifiers, and source URLs wrap instead of being silently clipped. A temporary 1180×760 Browser stress case moved an unbroken source token from 1,794 px of internal overflow to zero, with zero document overflow; the page was reloaded to restore bundled content. The focused Deep Learn file passed 27/27 tests, strict TypeScript and ESLint. Native reduced-motion-on remains unverified because the macOS UI-control channel closed before the Display preference opened; no system preference was changed.

- 2026-07-25: Closed the first Dark/reduced-motion design-system defects found by the whole-product UI audit. Dark primary actions previously rendered white text on the light accent at 2.42:1, and the Deep Learn Demo disclosure rendered gray on a fixed cream surface at 2.11:1. Dark accent text now uses ink at 7.33:1; the disclosure uses semantic warning tokens at approximately 6.13:1. Added the four missing shared tokens already referenced by current CSS (`surface-subtle`, `radius-md`, `shadow-xs`, and `motion-inline`), so Practice surfaces and inline transitions no longer lose declarations. Replaced the intervention blockquote's accent stripe with the existing bounded surface treatment. Impeccable's scoped detector reports zero findings; 5 focused desktop files / 84 tests, strict TypeScript, ESLint, and the production frontend build passed. Browser Deep Learn/Feed/Settings at 1180×760 verified Dark, reduced motion at 0.01 ms, and zero horizontal overflow. A current ad-hoc `Keen.app` was rebuilt and native Home/Feed/Settings Dark states were inspected; strict deep signature validation passed. macOS appearance was restored to its original Automatic setting. Packaged reduced-motion-on, long persisted content, complete keyboard traversal, accepted-reference diff, Developer ID/notarization, and pixel parity remain open.

- 2026-07-25: Completed a first packaged-WebView pass across Home, Knowledge Base, Learning Feed, History, Review, and Settings. Native inspection exposed Learning Feed's no-course state as a misleading two-column task-detail shell with a prominent Calendar action and excessive empty space. That state is now one bounded onboarding composition—add a course, import a source, then start learning—with one truthful Knowledge Base action. The real/Demo task-detail workspace is unchanged. Three focused desktop files passed 90/90 tests; strict TypeScript, ESLint, the production frontend build, a current ad-hoc `Keen.app` rebuild, and strict deep signature verification passed. The full `.dmg`/bundled-sidecar/restart smoke completed before this final frontend-only empty-state refinement, so the current `.app`—not that earlier DMG—is the packaged visual evidence for this change. Dark, reduced-motion-on, complete keyboard coverage, Developer ID/notarization, accepted-reference diff, and pixel parity remain open.

- 2026-07-25: Corrected the Browser Demo New learning contract. Ask remains one editable draft handoff, while Focused study no longer accepts a goal and silently routes it into a generic Conversation; it now explains that a saved guided path requires an indexed desktop course and offers one truthful `View sample sources` action. Demo-specific orientation copy no longer asks users to select a source control that is absent. Knowledge Base now renders the singular `1 page` correctly. Three focused desktop files passed 90/90 tests, strict TypeScript and ESLint passed, and live 1280×720 inspection found no horizontal overflow in Home Ask, Home Focused study or Knowledge Base.

- 2026-07-25: Converged the History and Review empty states on one restrained shared composition instead of two unrelated layouts, added the missing Review state icon, and restored 13 px explanatory copy. Settings no longer repeats the global Browser Demo/Desktop-runtime label in every section header; current runtime truth remains in the global disclosure and the contextual Status rows. No route, runtime contract, stored data or provider behavior changed. Four focused desktop files passed 63/63 tests, including removal of stale future-integration assertions; strict TypeScript and ESLint passed. Live Browser Demo inspection at 1280×720 found no horizontal overflow on History, Review, Settings Status or Settings Capabilities.

- 2026-07-25: Continued the Appica-style supporting-page convergence without adding routes or a second component system. Learning Feed task rows now preserve two lines for meaningful titles instead of sacrificing the task name to a status badge. Settings uses section-specific orientation copy, limits provider/Keychain disclosure to the Capabilities action, and removes the non-actionable Calendar/Drive/Canvas placeholder inventory. Empty-state reading size was raised consistently. Strict TypeScript, ESLint and 3 focused desktop files / 31 tests passed. Live Browser Demo inspection at 1280×800 verified the full sample task title, selected-detail state, focused Settings Status/Capabilities layouts, and zero horizontal document overflow; packaged WebView, Dark, reduced-motion-on and pixel parity remain unverified.

- 2026-07-25: Adapted Appica UI's high-frequency visual language into Keen's existing design system at reviewed upstream commit `26de9b1e02d2fb48694ae52d2371b1bbd71ee9d6`. Button, Field, Card, Badge and Progress now share a 40 px control scale, non-wrapping labels, soft neutral surfaces, restrained elevation, clearer focus states and compact press/hover motion. The Appica package was not installed because it would add Tailwind/Base UI alongside Keen's current primitives; no Appica logo, screenshots, copy or Figma assets ship in Keen. The canonical Figma Recall actions were normalized to equal 128×40 instances with a 12 px gap. Strict TypeScript, ESLint, 79 focused tests, the production frontend build and fixed-viewport Home/Feed/Deep Learn inspection passed; evidence is under `artifacts/ui-audit/2026-07-25/appica-primitives/` and `artifacts/figma/2026-07-25-delta/deep-learn-recall-appica-buttons.png`. No component-instance parity, accepted-reference visual diff or pixel-parity claim is made.

- 2026-07-25: Extended the canonical Figma guided-learning delta with a continuous second row: closed-book Recall (`220:151`), inline Agent intervention (`220:451`), and targeted Practice (`220:751`). Recall was visually inspected and keeps plain-language/math input in the document flow; the latter two frames were written successfully but remain implemented-unverified because the Figma screenshot service began returning an internal error for both new and previously accepted nodes. Recall evidence is `artifacts/figma/2026-07-25-delta/deep-learn-recall-in-review.png`; no runtime or visual-baseline claim is made.

- 2026-07-25: Added a bounded guided-learning design delta to the canonical Keen Figma file without creating a parallel design system. It contains mutually exclusive Home saved-work and New-learning compositions plus a two-column Deep Learn reading state with one `New learning` entry, release navigation, a STIX Two Math chain-rule formula, and an inline recall handoff. Final nodes are section `200:108`, Home saved work `200:630`, Home new learning `202:660`, and Deep Learn reading `207:149`; screenshot evidence is under `artifacts/figma/2026-07-25-delta/`. This is a reviewed design delta, not an accepted runtime baseline or a pixel-parity claim.

- 2026-07-25: Added a truthful `Current mastery` snapshot to History using the selected course's existing persisted mastery rows. It shows at most six recorded concepts, weakest first, with current probability and attempt count; zero-attempt priors and unavailable/loading/Demo states remain hidden. New Focused Study requests now derive a concise deterministic concept label from the retrieval topic instead of storing the whole learner request as the concept name; existing evidence is not migrated, and legacy long names use a one-line visual fallback while retaining their full accessible name. No trend is implied because the current UI contract has no time-series read. History tests passed 20/20 and Focused Study API tests passed 19/19; strict TypeScript, scoped ESLint, frozen-sidecar rebuild, Debug `.app` build, strict deep signature verification and native inspection passed. Evidence is under `artifacts/ui-audit/2026-07-25/mastery-snapshot/`. No Lieflat Charts code or asset was copied, no dependency was added, and no efficacy or pixel-parity claim is made.

- 2026-07-25: Removed four untruthful or hard-to-read UI leftovers without changing routes or learning contracts. The Toolbar no longer repeats `New learning`; Task Detail no longer fabricates a completed goal-review step; History uses one action when both saved lists are empty; and Review presents persisted source-cloze prompts as a visible blank plus normalized equation instead of one raw heading. Focused tests passed 6 files / 100 tests; strict TypeScript, scoped ESLint, an ad-hoc Debug app rebuild and native Review/Feed inspection passed. Evidence is under `artifacts/ui-audit/2026-07-25/supporting-pages-refinement/`; no accepted visual baseline or pixel-parity claim is added.

- 2026-07-25: Refined the live Deep Learn rail and targeted Practice after user review. The rail now shares the reading background, identifies the current unit without a misleading standalone percentage, and shows a compact Recall → current → next sequence. Deterministic cloze Practice now separates the source sentence from its normalized formula, displays a visible blank, asks for one missing word or short phrase in a bounded single-line field, supports Enter submission, and labels its action `Check answer`.

- 2026-07-25: Audited the release pages and the guided Recall → Agent explanation → Practice interaction. Deep Learn now renders provider Markdown as inert React rich text (paragraphs, lists, emphasis, quotes and readable math), presents targeted practice as a short instruction plus bounded question surface, converts the saved chain-rule prose formula to `y′ = f′(g(x)) · g′(x)`, removes the programmatic-focus outline from result headings and tightens phase spacing. Home keeps one Next-up action; Feed adds a Knowledge Base recovery action and corrects interactive status semantics; Conversation retries saved transcripts and opens the exact provider settings section; Settings locks unknown provider state until a successful read. Focused desktop tests, strict typecheck, scoped ESLint and an ad-hoc Debug `.app` build passed. Real persisted-session evidence is under `artifacts/ui-audit/2026-07-25/feedback-flow/`; no pixel-parity or complete accessibility claim is made.

All notable changes to Keen are documented here. Status statements must reflect tested behavior; planned work belongs in `docs/IMPLEMENTATION_PLAN.md`.

## Unreleased

### In progress

- Mapped the first approved Deep Learn V3 behavior onto the existing production Recall state machine without adding a route, client-side grader, simulated result, backend contract or second learning-state model. A restored pending prompt now enters an explicitly labelled closed-book focus state, keeps answer-bearing lesson material and the Inspector unavailable, removes the redundant content-column divider/offset, and focuses the existing response field. A generated `Study: <goal>` title no longer repeats the identical goal as a second header line, while genuinely distinct title/goal pairs retain their context. Submitting through the typed learning-core client exits focus mode and preserves the existing result-heading focus, deterministic mastery write, Agent/source-review routing and Practice handoff. Focused Deep Learn/Recall validation passed **2 files / 40 tests**; strict TypeScript, scoped ESLint and `git diff --check` passed. A rebuilt ad-hoc Debug `Keen Dev.app` then restored persisted session `focused-session-151b83d4-fed0-5f35-88ab-2c359f0979b2` at pending Recall after a full app/sidecar restart. Native 1220×768 captures verify the deduplicated header, closed-book label, focused sole textarea, hidden lesson/Inspector and collapsible rail. Read-only SQLite confirmed the run remained pending with no answer, mastery event or Review handoff. Strict deep signature verification passed for the bundled arm64 app/sidecar. Evidence is under `artifacts/ui-audit/2026-07-25/recall-focus/`; no accepted visual baseline, complete accessibility audit, Dark/reduced-motion-on result, Developer ID build, notarization or pixel-parity claim is added.
- Built a design-only Deep Learn V3 interaction preview without changing the React/Tauri runtime, navigation, learning contracts or dependencies. The canonical Sidebar, collapsible path rail and 65–75ch document remain, while one Learning Dock owns Ask, closed-book Recall, deterministic feedback, misconception-targeted practice and next-unit handoff. Starting Recall hides the answer-bearing lesson from visual/accessibility presentation, focuses the sole textarea and compacts the path; the next step remains locked until correct completion. Empty/double submission, persistent one-hint disclosure, incomplete/source-review/retry, misconception/targeted-practice, correct completion and Step 4→5 advancement are implemented. Browser checks at 1510×1107, 1280×900 and 1180×800 found zero horizontal document overflow, one visible input and no console/page errors; source/Escape and collapsed-path compatibility also passed. This is preview evidence under `docs/design-previews/agent-composer/`, not a production implementation, accepted baseline, pixel-parity result, complete accessibility audit, packaged-WebView result, Provider run or persistence claim.
- Verified the first bounded Agent-native learning intervention without widening release navigation. A real `openai-compatible` `deepseek-v4-flash` run through the loopback LiteLLM gateway followed an incorrect Recall, produced a strictly schema- and source-handle-validated artifact, and rendered the Agent explanation, source and independently scored pending Practice inline in Deep Learn; Toolbar/Activity showed the run completed. The successful run was `run-09c9d2d2a4b843ba91eeb26d03ca4b66`. After a complete app/sidecar quit and restart, History restored that same artifact and pending Practice. Read-only SQLite evidence retains all five runs, including four truthful development failures: the successful run has exactly one checkpoint and one done event, and its only Practice (`practice-run:c53a6d38-ad61-5192-a893-4af24b02ec0b`) remains pending; pre/post-restart counts are unchanged at two tasks, three mastery events, zero state mutations and one tool invocation. Legacy raw checkpoints normalize to an envelope on read without rewriting SQLite. Provider compatibility now strictly bounds DeepSeek response extras/index/reasoning/cache tokens, uses JSON-object completion mode, persists checkpoint envelopes, and normalizes legacy payloads for latest GET/SSE; Activity restores terminal SSE only from a confirmed fresh run and fails closed otherwise. Seven selected backend test files passed, including the **63/63** checkpoint-contract review; focused frontend passed **6 files / 117 tests**. Full `npm run check` exited 0: frontend **43 files / 503 tests**, Python **1127**, Rust **49**, plus lint and strict typecheck. The current ad-hoc arm64 RC package passed DMG verification, strict deep signature, bundled-sidecar and restart/recovery smokes; its 34,197,306-byte DMG SHA-256 is `2133f67132507c34d2e57810a868052c70cd96b590faccbb7b9e096e03e5c5ec`. The host-frozen source excerpt/handle was used and no search tool ran in the successful path (a prior failed attempt used read-only search). The gateway credential remains plaintext mode `0600`, not Keychain-backed, and loopback gateway local authentication remains a limitation. No vector, learning-outcome, pixel-parity, Developer ID, or notarization claim is added.
- Distilled the verified intervention's steering into a Practice-primary product state without changing API, Provider, artifact, or learning-state contracts. Eligible state keeps one explicit policy-selected Agent action; completed artifacts expose the existing typed intents only through one restrained `Try another approach…` disclosure, while an uncertain write exposes only its frozen idempotent Retry. Restored `practice_ready` hands off exactly once after ordinary restore, SSE completion, or disconnect recovery. Paused sessions prevent new intents but retain cancellation of an already-running Provider call. The disclosure target remains mounted and hidden while collapsed; a rebuilt native Debug app caught and corrected the CSS override that initially leaked hidden alternatives into the macOS accessibility tree. Final native inspection showed only the collapsed trigger, then the three real choices after expansion, with the existing pending Practice still primary and no new run or Practice created by inspection. Independent re-review found no P0–P2 findings. Focused tests passed **2 files / 27 tests**; the complete desktop suite passed **43 files / 508 tests**, strict TypeScript, ESLint, Debug app build and `git diff --check`. The current RC package evidence is recorded above. This does not yet add selected-artifact lineage or prevent a learner from intentionally creating a later alternative artifact.
- Replaced the completed intervention's raw JSON presentation in the global Activity drawer with one bounded learner-facing `Learning record`. Only an exact `completion_published` checkpoint that passes the strict `learningInterventionArtifactSchema` can project its summary, source document/page/section labels and next-step label; full explanation text, source quote, metadata, hashes, identifiers and private Practice prompt are not projected into this view. Invalid or unrelated checkpoints and ordinary Conversation runs retain the existing response behavior, while a valid record suppresses the accumulated JSON response only in the panel. The narrow drawer uses the existing flat dividers and type scale, with long source labels allowed to wrap and no duplicate learning action. A rebuilt bundled Debug app restored the real DeepSeek record from History and exposed `Learning record`, `Chain rule study note.md · p. 1 · Chain rule`, and the pending independent Practice next step without raw JSON in the macOS accessibility tree; no Agent choice or Practice submission was made. The before/after QA baseline remained unchanged at five Agent runs, 25 Agent events, two Practice runs, three mastery events, zero state mutations and one tool invocation, and the named Practice remained pending. Independent review found no P0–P2 issue. Focused Activity tests passed **35/35**; the complete desktop suite passed **43 files / 512 tests**, strict TypeScript, ESLint, bundled Debug app build and `git diff --check`.
- Added `docs/AGENT_NATIVE_USER_RESEARCH.md`, a directional qualitative synthesis of public learner/PKM discussions, current AI-tutoring studies, and human–AI interaction guidance. The research is explicitly not representative demand or efficacy evidence. It narrows the planned Agent-native phase to one mixed-initiative, source-inspectable intervention after a real Recall attempt, with a compact `why now / source scope / then` contract, learner activation for the first rollout, capability-bounded evidence disclosure, subsequent independent Practice, and restart continuity. Product Positioning, the Agent architecture, the Implementation Plan, and the visual backlog now reject bulk generation, hidden review debt, transcript-based learner Memory, generic Agent chrome, and model-authored learning truth. This is documentation only: no runtime, migration, endpoint, provider call, UI state, test result, or learning outcome was added.
- Documented the planned bounded Agent-native learning phase in `docs/AGENT_NATIVE_ARCHITECTURE.md` and linked its binding product and delivery constraints from Product Positioning and the Implementation Plan. Product Positioning now narrows the initial audience to self-directed university/postgraduate learners and knowledge workers studying a bounded course, certification, or professional subject, while explicitly keeping general assistance, open research, cohort/LMS administration, and broad Personal Learning OS scope out. The plan extends the existing durable Agent Runtime through a source-grounded Deep Learn intervention, deterministic state ownership, versioned Teaching Playbooks, persistence, steering, and later reviewed procedure candidates. This is documentation only: no Agent intervention, model tool call, migration, endpoint, UI state, automatic Skill extraction, or learning outcome has been implemented by this change.
- Started the recoverable local learning-loop milestone on `codex/milestone-learning-loop`; the evidence-gated design covers durable Conversation, audited single-Agent tools, Deep Learn, Assessment, deterministic mastery/misconception updates, FSRS Review, explainable Learning Feed, restart recovery and packaged E2E. These capabilities are not claimed as implemented until their dated acceptance evidence is added.

### Changed

- Closed the Provider-missing recovery loop for the bounded Agent intervention. Deep Learn now has one primary `Configure learning provider` action, carries only a canonical exact Session/Course return target into Settings, and keeps the persisted source-review fallback secondary. Settings opens Capabilities contextually, prevents a dirty draft from testing an older runtime, requires a newly healthy sidecar generation after save, exposes an explicit retry after terminal restart failure, and unlocks `Return to learning` only after the current generation passes its Provider test. The Python intervention read contract re-offers a previously failed `provider_missing` run as eligible only after the replacement runtime is actually configured; it never auto-runs the Agent, while other failures remain unchanged. Agent Activity also resumes from its durable cursor after connection-generation rotation instead of incorrectly treating the stream as complete. The full desktop suite passed **43 files / 500 tests**; the four repaired lifecycle/harness files passed **54/54**; strict TypeScript, ESLint, the production Vite build and `git diff --check` passed. Focused Python Provider/intervention tests passed **107/107**; Rust format/check/Clippy and **49/49** tests passed. `npm run package:macos` produced an ad-hoc arm64 `.app` and unsigned DMG (`f7758e34cfe8ca86b580cc9d29766c8b61550cb65db7ba4abeb262a6de5b7276`); DMG verification, deep code-signature verification, bundled-sidecar smoke, token rotation, Review/FSRS restart recovery and replay passed for both package stages. Read-only native inspection in the host's dark appearance found no clipping in Settings → Capabilities and verified Endpoint → Model keyboard order with the disabled save action skipped. At that recovery-only closeout the installed Ollama runtime reported no models; the later bounded DeepSeek acceptance is recorded above. A real Keychain credential, Developer ID, notarization, accepted visual baseline, and pixel parity remain unverified.
- Added a bounded first-run Provider configuration path for loopback Ollama and loopback or remote-HTTPS OpenAI-compatible chat. Remote keys are stored by canonical HTTPS origin in macOS Keychain and are sent to the Python sidecar only through length-prefixed stdin frames, never configuration JSON, argv, environment variables, or logs. Saved configuration takes precedence over stale `KEEN_CHAT_*` values; a save during sidecar startup forcibly replaces the in-progress generation so the next process reads the new provider; IPv4/IPv6 loopback validation now agrees across Rust and Python; and Settings reuses or labels a saved key only for the current endpoint origin. `keyring` 3.6.3 is exact-pinned with the project Rust floor raised to 1.85, and its provenance/notice boundary is recorded. Focused provider Python tests are included in the combined **219/219** result above; strict TypeScript, scoped ESLint and Rustfmt pass. Rust format/check/Clippy and **49/49** tests now pass, and the current ad-hoc package verifies restart/token-rotation behavior. Lockfile-based license scanning, a real Keychain interaction, positive Provider connectivity and Rust 1.85 host compilation remain unverified.
- Completed the visible New learning → saved path → opening reflection contract without adding a route, state table or second plan model. Before the first write, Deep Learn now shows the validated persisted unit order, objective and minutes plus the exact unique source-chunk count; units remain read-only and source body/chunk identities remain hidden until the reflection completes. The user-facing label is consistently `Opening reflection`. Continuity hardening also prevents autonomous sessions from binding arbitrary same-course source to a weak concept, preflights Focused Study units through the existing deterministic Recall and distinct-Practice generators before creating learner state, and carries a uniquely matched due-Review task from Home through the existing atomic Review completion contract. Ambiguous Review-task matches fail closed to the unbound Review queue. These changes add no schema migration or Provider call. The combined Python regression passed **219/219**, the combined desktop product regression passed **127/127**, and the production Vite build passed with the existing large-chunk warning.
- Tightened the current Shell → Learning Feed / Task Detail → Deep Learn product slice without adding routes, future navigation, fixtures, or a second design system. The Sidebar keeps the canonical Keen mark, one `New learning` action and only Home, Knowledge Base, Learning Feed, History, Review and Settings; its collapsed brand/toggle state no longer overflows. Feed now defaults to Today / Upcoming / Completed, keeps overdue work visibly labelled inside Today, and shows one task-detail action with goal, expected steps and progress. Deep Learn keeps its collapsible path rail plus bounded 72ch document at the 1180–1280px contract, opens Sources and Activity only through the existing on-demand drawer, and makes Explanation → Recall → Intervention → Practice explicit. Strict TypeScript, scoped ESLint and the production Vite build pass; the build retains its existing large-chunk warning. Browser Demo Home at 1180×740 and Deep Learn at 1280×720 were inspected with zero application-console errors and two existing React Router future warnings. Those captures verify only the request-free Demo shell, not the new live path/intervention states. Packaged Tauri verification, accepted visual references, mismatch measurement and pixel parity remain unverified.
- Adopted the user-selected flat Keen mark as the packaged application identity. A clean three-path SVG was redrawn from the selected reference with stable dark `#161E25` and teal `#11ADA2` fills; the Tauri source icon places that mark on a quiet `#F7F8FA` rounded-square field, replacing the unrelated provisional line-art `K`. The checked-in macOS, Windows, iOS and Android icon outputs were regenerated from that single SVG source. `docs/BRAND_ASSETS.md` now names the only current mark, wordmark and packaged-icon masters; repository instructions and the root README link that map, and the unused low-resolution `keen-icon.png` was removed to prevent it being mistaken for current artwork. This is an implementation asset derived from the supplied mark, not a claim of an original vector master or a newly packaged/signed/notarized release.
- Froze the current guided-learning and lightweight-continuity phase without adding a Journey model, navigation item, provider behavior or visual direction. Home now fails closed while due Review truth is pending or the learning core is unhealthy; due Review, Learning Snapshot and Session History caches share authoritative invalidation after Review/Summary writes; completed Summary navigation remains locked until all continuity refetches settle; Review due time updates in place; invalid schedules and IME composition fail safely. Focused continuity/Home/Review/Summary tests passed 54/54, the complete desktop suite passed 35 files / 440 tests, strict TypeScript, ESLint, production build and `git diff --check` passed, and independent Sol-high acceptance found P0 = 0 and P1 = 0. The current Alpha remains deliberately lexical-only; no hybrid/vector expansion, packaged Dark/reduced-motion/long-content evidence, accepted visual baseline, Developer ID signature, notarization or pixel parity is claimed.
- Completed the approved lightweight learning-continuity slice without adding a Journey model or navigation item. Home now combines the existing authenticated, snooze-aware per-course Learning Snapshots instead of limiting `Next up` to the first course, selects deterministically by saved priority/due time/identity, and shows the real course. It resolves the task's exact persisted Study Session, displays its saved goal, progress and phase-specific action, and resumes the exact Deep Learn route; non-Session work retains the scoped Feed handoff. Strict TypeScript, ESLint, production build, and the focused Home/continuity/Feed/Deep Learn/API-client matrix passed 5 files / 140 tests. A packaged UI-audit restart restored the same existing Alpha Bayesian task/session and exact `Finish summary` handoff before and after restart; no fixture was created. The existing optional hybrid/vector retrieval implementation remains reuse-only pending a bounded real-query comparison; no embedding provider, vector-success result, new RAG framework, or retrieval UI was added.
- Fixed the Alpha packaging workflow so one run leaves both the standalone ad-hoc `.app` and unsigned `.dmg`, and made the frozen-sidecar Review restart smoke read lifecycle lines without text-buffer timing loss. The mounted DMG and standalone app passed deep signature, service lifecycle, Review restart/replay and FSRS persistence checks. A temporary installation copied from the read-only DMG reached ready and exited cleanly; the repository app also quit, restarted and released its sidecar/lock cleanly. Focused backend acceptance passed across eight workflow files and the matching desktop flow passed 124/124 tests. `docs/ALPHA_RC.md` records artifact hashes and known limits. No Developer ID, notarization, public release, new visual baseline or pixel-parity claim is added.
- Rebuilt the current source into a local ad-hoc `Keen UI Audit.app` after freezing the learning-core sidecar with migrations `001` through `030`. Deep signature verification, the isolated bundled-sidecar smoke, and the Review restart/replay/FSRS smoke all passed; the bundled executable matches the freshly frozen binary and includes migrations `028`–`030`. This is a local lifecycle verification only: it creates no release `.dmg`, Developer ID signature, notarization, native visual capture, accepted visual reference, or pixel-parity claim.
- Stabilized the current source tree before submission by updating seven legacy migration assertions to include this guided-loop implementation's additive migrations `029` and `030`; the stabilization repair adds no schema or runtime learning behavior beyond the staged implementation itself. The full configured Python suite, 33-file / 425-test desktop suite, Rust 40-test suite, ESLint, strict TypeScript, Ruff check/format, Rust fmt/Clippy, production build, and `git diff --check` now pass. This is source validation only: the current worktree has not yet been re-packaged into a new signed/notarized release or a new visual-reference result.
- Corrected the existing Learning Feed → Review handoff so a due Review task opens and completes only its exact persisted task. The typed all-or-none task context is stored on the Review attempt by additive migration `029`; initial rating validates the exact course/source/item task and completes it with revision CAS in the same transaction as FSRS, while direct Review deliberately leaves Feed tasks untouched. Partial links fail closed without reading a substitute queue; task-bound 404/non-retryable 409 close scoring, preserve the backend reason, and return to Feed without falsely selecting Completed. Replayed payload/task mismatches now return the explicit non-retryable `review_attempt_idempotency_conflict`, and the client preserves Review error codes. Independent final acceptance found P0/P1 = 0. Focused backend review/migration tests passed 34/34, desktop Feed/Review/API tests passed 111/111, and TypeScript, ESLint, relevant Ruff checks and `git diff --check` passed. No provider behavior, synthetic card/citation, new navigation, Figma work, packaged-app evidence, visual baseline, signing/notarization, or pixel-parity claim is added.
- Replaced Home Study's recommendation-first handoff with an explicit, goal-owned focused-study command. A selected indexed course and canonical learner goal now determine a lexical-only current-source selection and atomically create the existing task, Study Session and persisted plan; the exact source chunk IDs remain in task provenance, session scope and plan units. The Home action no longer lets an unrelated active or completed autonomous recommendation consume the goal. Additive migration `030` stores only the bounded request-identity pair for no-match recovery: blocked requests create no concept, mastery, task, session, plan, review or assessment record; identity/key/payload drift fails with `409`, and the same blocked request can atomically create its path only after matching material becomes available. NFKC/control-character normalization and closed blocked-reason contracts now agree between Python and TypeScript. Independent final acceptance found P0/P1/P2 = 0; focused backend/request-guard/migration tests passed 39/39, focused desktop client/Home tests passed 23/23, and the proportional Deep Learn/Home regression run passed 67/67. This changes no navigation, visual direction, provider/RAG capability, Figma state, packaged-WebView evidence, accepted visual baseline, signing/notarization, or pixel-parity boundary.
- Added the first bounded adaptive Study Session branch without creating a new Agent surface, navigation item, session status, provider call, or synthetic lesson. New production sessions opt into `adaptive-session-policy/1.0.0`; after persisted Active Recall, a correct result creates one pending Practice action and an incorrect result creates one pending source-review action. The Session remains in its existing `practicing` phase. Completing that review atomically reveals the existing Practice action; unresolved remediation is server-gated so the client cannot skip it. Legacy sessions retain the canonical flow. The action ledger is additive migration `028`, uses one transaction with Recall evidence and existing idempotency/replay semantics, survives reload/restart, cancels terminal pending actions, and does not mutate the plan, BKT/mastery, FSRS/Review, task, source, Provider, or citations. Deep Learn renders the review inline in the existing reading column using only the persisted unit/source chunk identities, with one `Continue to practice` control, factual Current/Next context, loading/error Retry, pause/conflict recovery and focus handoff. Focused Python tests passed 123/123 (one existing Starlette/httpx deprecation warning); focused desktop tests passed 54/54; strict TypeScript, ESLint, production build, scoped Ruff/format and `git diff --check` passed. Independent re-acceptance found P0/P1/P2 = 0 after repairing strict late-replay parsing, adaptive-read recovery and pause-conflict continuation. This is not a provider, packaged-WebView, accepted visual-baseline, full accessibility, signing/notarization, or pixel-parity claim.
- Closed the internal Alpha guided-learning loop in a freshly rebuilt ad-hoc packaged Tauri app. A real persisted Alpha course now verifies Completed Feed → read-only Task Detail → History → completed Deep Learn Summary, with the same FSRS Review item and indexed Knowledge Base source visible after a full app/sidecar restart. The completed session persisted two completed units, two active-recall runs, two practice runs, one completed originating task, one Review handoff/item and one Review attempt without duplicate same-day recommendation rows. Focused desktop tests passed 107/107 and Python tests passed 38/38; strict TypeScript, ESLint, Ruff, `git diff --check`, the sidecar build, packaged app build and bundled-sidecar smoke passed. Native evidence is under `artifacts/ui-audit/2026-07-22/alpha-golden-path/`. The app remains ad-hoc, Light/reduced-motion-off is the only packaged visual state verified here, embeddings remain unconfigured/lexical-only, and no pixel-parity, Developer ID signature or notarization is claimed.
- Final independent Alpha acceptance found no P0/P1 and made no product-code change. The current focused recheck passed Python 40/40, desktop 106/106 and Rust 40/40. Two non-blocking limits remain deferred: an extreme same-day completion set can exceed the completed-snapshot window, and legacy noncanonical timestamp text is outside the production replay write-path guarantee. These do not reopen Alpha, but they also do not turn the broader packaged visual closeout into a full Dark/reduced-motion/long-content or pixel-parity result.
- Closed the current UI implementation with a bounded packaged-WKWebView pass rather than another visual-direction change. A fresh ad-hoc `Keen UI Audit.app` rendered the current Home, empty Feed, secondary Calendar and empty History at 1440×900, 1220×768 and the enforced 1180×740 native minimum; keyboard traversal was continuous, Calendar returned focus to its opener, a supervised sidecar replacement returned the UI to ready, deep signature verification passed and bundled-sidecar smoke passed. The pass fixed a real no-course Home loading collision, connected macOS appearance to the existing Light/Dark token set, removed the unconditional Figma capture script from application HTML, relabelled Demo completion as a no-write visual preview, restored visible Task Detail focus and corrected the Deep Learn rail icon selector. The complete desktop suite now passes 29 files / 390 tests; strict TypeScript, ESLint, production/audit builds and `git diff --check` pass. Evidence is under `artifacts/ui-audit/2026-07-22/packaged-ui-closeout/`. The current host remained Light with reduced motion off and the isolated audit identity had no safe long study record, so packaged Dark, reduced-motion-on and long-content captures remain unverified. The visual harness still exits non-zero with 16 missing references, eight provisional comparisons and two superseded zero-tolerance Home/Feed failures; no pixel parity, Developer ID signature or notarization is claimed.
- Completed the bounded runtime UI closeout without changing Keen's learning-core, persistence, Provider or capability boundaries. The Shell now has one `New learning` entry and stable Home, Knowledge Base, Learning Feed, History, Review and Settings destinations. Learning Feed defaults to the task list and a quiet task-context panel; Calendar is an explicit secondary view, while compact windows switch between list, detail and calendar and restore focus to the originating task. Deep Learn adds a labelled collapsible path rail, keeps a bounded document column, reflows to one column at 680 px, and separates inline prompt actions from the single sticky lesson-navigation row. No future navigation, model reply, citation, task result, calendar write or integration success was added. Browser checks found no horizontal overflow at 1220×768, 1000×720, 760×700 and 680×740. The complete desktop suite passed 389/389, Rust passed 40/40, and ESLint, strict TypeScript, production build, `git diff --check` and scoped Impeccable detection passed. The refreshed visual harness still reports 16 missing references, eight comparison-only Figma results and two expected failures against superseded zero-tolerance Home/Feed self-baselines (2.222% and 2.612%); no packaged WebView rerun, accepted replacement baseline, pixel parity, Developer ID signature or notarization is claimed.
- Froze the next UI closeout as a documentation-only product contract: one `New learning` entry, task-list-first Feed, one primary task action, two persistent Deep Learn columns at normal desktop widths, learning-step treatment for Quiz/flash cards/visual explanation, and release-undiscoverable future/demo routes. `AGENTS.md`, product positioning, the implementation plan, and the visual backlog now share the fixed order Shell/Sidebar → Feed/Task Detail → Deep Learn → supporting-page consistency → packaged Tauri verification → stop. This entry records planning constraints only; no runtime UI, capability, test result, visual baseline, or parity status changed.
- Completed the persisted Study Session pause/resume slice without adding a page, navigation item, migration, provider behavior or synthetic learning result. Authenticated course-scoped `pause` and `resume` commands use revision checks plus durable event-ledger idempotency, preserve the exact pre-pause phase across a database reopen, and return authoritative replay state after an uncertain write. The Deep Learn header now has one standard Pause/Resume action; paused diagnostic, studying, practice and summary states route by persisted `resume_from_status` instead of probing another endpoint, while missing-plan recovery can still resume a paused session. Unknown transport outcomes retain the same command identity, 409 responses restore current state, and pause/resume creates no mastery evidence, review item or review attempt. The existing flat header, shared Button states and responsive/reduced-motion tokens were reused; no Figma frame or visual direction changed. ESLint, strict TypeScript, Ruff, `git diff --check`, the 389-test desktop suite, the 1045-test Python suite and the production Vite build passed. The build retains its existing chunk-size warning, and tests retain existing React Router and Starlette/httpx deprecation warnings. No packaged WebView screenshot, accepted reference, visual diff, pixel-parity result, signed/notarized package or external write was produced for this slice.
- Completed the first real two-unit Study Session progression without adding a new route, migration, navigation item, or learning model. An answered targeted-practice step now atomically completes the current unit and activates its contiguous successor, updates the authoritative current-unit pointer and progress, and returns the same persisted session to `studying`; it creates no Review handoff and does not complete the originating task. Only the final unit may enter Summary, create the existing FSRS handoff, and complete the task/session. Deep Learn keeps the existing path rail, treats the persisted pointer as authoritative, makes future units non-interactive, permits read-only review of completed/skipped units, isolates recall/practice state by unit, and shows a deliberate `Unit complete` handoff before reloading the next unit. Corrupt pointers, multiple active units, ordinal gaps, invalid successors, late unit-one responses, and partial-write failures fail closed. Independent mechanical validation passed 147 Python tests; the final combined rerun passed 48 Python and 112 desktop/API tests with strict TypeScript, while Ruff check/format and `git diff --check` passed. Sol-high acceptance initially found two P2 guards—multiple-active replay validation and paused-Summary error classification; both received focused repairs, and their final reruns passed 7 desktop/API and 6 Python tests respectively. The only Python warning is the existing Starlette/httpx deprecation. No packaged WebView run, screenshot, visual baseline, signed/notarized package, Provider call, external write, dependency, or upstream source belongs to this slice.
- Polished the current learning workspace without changing navigation, data contracts or provider behavior. Home's New learning state now uses a flat source row and document-like question field instead of a rounded workbench around a native glossy select; normal ready state no longer repeats source instructions, while loading and missing-source copy remains explicit. Saved-work actions form one compact action group. History and new Conversation course selectors share the same single-chevron desktop treatment, saved Conversations show a fixed course label instead of a disabled form control, completed History rows no longer repeat their success badge in prose, and short completed transcripts no longer scroll their question out of view. Core Sidebar, Feed and Conversation labels moved to a readable 12–14 px floor. Task Detail formats stored ISO timestamps as product dates and limits pathological generated titles to two visible lines while retaining the full accessible heading. The final focused Home/core set passed 84 tests; History, Feed, Conversation and learning-core files passed 99 tests; ESLint, strict TypeScript, production build, scoped Impeccable detection and `git diff --check` passed. A rebuilt ad-hoc Debug `Keen Dev.app` was inspected at 1220×768 with source/prompt keyboard focus and real persisted Home, Task Detail, History and Conversation data; captures are under `artifacts/ui-audit/2026-07-22/ui-polish/`. Packaged reduced-motion-on, the 1180×740 minimum and fixed-reference visual diffs were not rerun, so no complete responsive, accepted-baseline or pixel-parity claim is added.
- Connected the existing openai-compatible provider boundary to a user-configured local LiteLLM gateway for the real DeepSeek `deepseek-v4-flash` API. The gateway is loopback-only and lives outside the repository; the user-selected API-key file is plaintext mode `0600`, not Keychain-backed. A real Debug-app failure revealed that the strict frontend retrieval-event schema omitted the backend's existing `chunkId`, causing reader cancellation and durable `client_disconnected` interruptions. The schema now requires `chunkId`, matches it to `chunkIds[0]`, rejects unknown keys, and has 130 ms slow-stream plus React StrictMode single-request/no-abort coverage. Focused desktop validation passed 86/86, the direct provider suite passed 27/27, and independent Sol-high review found no P0/P1 blocker. A rebuilt ad-hoc Debug `.app` then completed both Conversation Retry and Home Ask → Conversation with the indexed Calculus I source; SQLite recorded `completed`, `openai-compatible`, `deepseek-v4-flash`, one durable citation and zero new-flow `client_disconnected`, and History restored the completed record after a full app/sidecar restart. Screenshots are under `artifacts/ui-audit/2026-07-22/deepseek-provider/`. Retrieval remained truthfully lexical-only because no embedding provider is configured. No signed/notarized package, tool-capable Agent, vector-provider success, accepted baseline or pixel-parity claim is added.
- Closed three small continuity slices without changing the visual system or external capability boundaries. Global `New learning`, including the native menu wording, now opens Home Ask; its focused frontend evidence passed 64 tests and Rust passed 40 tests. Knowledge Base passes a course to Home only from a currently linked, indexed source and fails closed for stale selection or source-read failure; its focused evidence passed 84 tests, with strict TypeScript, ESLint, production build and `git diff --check` passing. Deep Learn return actions preserve `course_id` to Learning Feed and History and isolate tasks across courses; its initial focused run passed 55 tests and corrected follow-up passed 40 tests, with independent Sol high acceptance reporting P0/P1/P2 = 0. These are separate focused runs, not an additive full-suite total. No Figma or visual redesign, packaged WebView evidence, Provider/citation capability, Apple Developer ID signing status, accepted baseline or pixel-parity claim changed.
- Reworked Home into two exclusive, task-oriented states. Saved work now shows one `Next up` task with direct continuation into `/feed?task=…`, while empty or explicit new-learning entry shows intent selection before source scope and uses mode-specific actions instead of generic `Continue`. Independent acceptance also fixed Demo continuation to use the first unfinished stored sample instead of a hard-coded or list-only destination, while keeping Demo and live task IDs isolated. The existing Ask/Study persistence and Provider boundaries are unchanged. The canonical Figma file was updated in place with frames `196:328` and `197:351`; current reduced-motion browser captures cover 1180×740 and 760×700. The final focused Home/Feed run passed 30/30, ESLint, strict TypeScript, production/Debug app build and `git diff --check` passed, and a healthy unsigned Debug app at 1220×768 verified both Home states, exact persisted Task Detail continuation and native intent/source/prompt Tab order. The earlier full 361-test run and scoped Impeccable result cover the layout revision. Figma's older Sidebar is not treated as a whole-shell match; no packaged reduced-motion-on, accepted baseline, real Provider/citation or pixel-parity claim is made.
- Added persisted Conversations to the existing History surface without creating a second history store or changing the accepted UI foundation. History now separates Questions from Study sessions, loads each section independently, and opens a selected Question on its authoritative `/conversation/:id` route so the existing durable transcript, status, and citations can restore after refresh or restart. The authenticated Conversation list uses bounded cursor pagination ordered by `updated_at DESC, id DESC`, explicit course/all-indexed scope, authoritative latest-assistant status, a learner-facing preview, and message count without returning full message or provider payloads. Cursors are versioned, bound to the active course filter, and reject malformed datetime/UUID semantics. Missing courses fail closed as `Course unavailable`; Browser Demo remains request-free and contains no fabricated rows. Focused desktop tests passed 108/108, the complete desktop suite passed 360/360, focused Python API tests passed 5/5, and ESLint, strict TypeScript, Ruff, Debug app bundling, `git diff --check`, scoped Impeccable detection, and independent Sol acceptance passed. The rebuilt Dev.app reached only the truthful unavailable state, so healthy native list/open/restart remains implemented but not reproduced in this turn; no provider success or live citation is claimed.
- Unified Home Ask with the durable Conversation path without changing Study, provider, retrieval or the accepted UI foundation. Home now requires an explicit indexed course or user-selected all-indexed scope, creates one Conversation with stable client IDs under a single-flight guard, reconciles uncertain creates through the authoritative Conversation id, and hands the question to `/conversation/:id` exactly once for the existing durable answer stream. Ask no longer starts AgentRuntime or creates `agent_runs`; Browser Demo remains request-free. Independent acceptance found and the final repair closes three Home recovery defects: an unmounted 404 no longer triggers a background second create, an authoritative result found after unmount retains a one-shot route recovery, and Ask retry/error state cannot leak into Study or invite an impossible retry after conflict. The final Home and Conversation focused tests passed 34/34, the API-client suite passed 59/59, and one focused durable API test passed while explicitly verifying zero `agent_runs`. Final ESLint, strict TypeScript and `git diff --check` passed; scoped Impeccable detection and a Debug `Keen Dev.app` build passed before the final nonvisual recovery repair and were not repeated afterward. The most recent complete desktop run is 326/347: the same 21 pre-existing `learningCore.integration.test.tsx` assertions still target superseded Feed, Knowledge Base and core-status copy/state outside this slice. That rebuilt Dev.app rendered Home but its bundled learning core was unavailable, so a healthy native Home Ask create/stream/reload flow is implemented but not visually reproduced in this turn.
- Added the first durable Conversation vertical slice without changing the provider or retrieval pipeline. Additive migration `027` extends the existing Conversation tables with explicit course/all-indexed scope, reply identity, retrieval/timing fields, terminal answer state and locked citation version/hash snapshots. Authenticated create/read/message/stream/cancel routes now persist one user/assistant turn around the existing `AnswerService`; completion content, citations and metadata commit atomically before `done`, cancellation is server-authoritative, restart recovery terminalizes unfinished answers as `interrupted`, and client-only unknown outcomes reconcile through message reads with same-key retry only when the turn was never created. `/conversation/new` adopts the authoritative UUID route before streaming and saved routes restore the transcript; Browser Demo remains request-free. Backend focused tests passed 33/33, Request Guard passed 8/8, desktop Conversation/API-client tests passed 79/79, and the final complete Python suite passed 1034/1034. ESLint, strict TypeScript, Ruff check/format, `git diff --check`, the scoped Impeccable detector, the latest ad-hoc Debug `Keen Dev.app` bundle and independent Sol acceptance passed. The most recent complete desktop suite remains 321/342 because 21 pre-existing `learningCore.integration.test.tsx` assertions target superseded cross-page UI copy/state; final Conversation repairs are covered by the later 79/79 focused run. The rebuilt Dev.app launched but its bundled learning core became unavailable, so normal native Conversation persistence/restart was not reproduced; same-database restart is verified at the authenticated API layer, not claimed as live-app evidence.
- Completed the scoped cross-page core-loop acceptance pass with only Conversation truthfulness and state-isolation repairs. Switching Conversation routes now aborts the prior transient run, hides old route state before the reset effect commits, clears messages, citation/PDF and Inspector context, reloads only that route's unsent draft, and cannot write an old draft into the new route. A completed answer with no source support now says so without showing a citation; citations use the existing PDF modal as the single preview; Retry, Ask again and Reuse question now reflect their actual behavior. The compact Sidebar keeps a visible `Demo` disclosure at 900 px even after a persisted collapsed-rail state. No Conversation transcript persistence, API, SQLite, provider, RAG, navigation, Figma or dependency work was added. Current Browser evidence is `artifacts/ui-audit/2026-07-21/core-loop-handoff/01-conversation-demo-1180x740.png`, `02-conversation-demo-900x700.png`, and the visually checked collapsed disclosure capture `06-conversation-demo-900x700-collapsed-disclosure-fixed.png`; the latest rebuilt Debug app reached only the truthful Home unavailable state at `03-home-unavailable-real-dev-app.png`. The final focused cross-page desktop suite passed 10 files / 110 tests, ESLint, strict TypeScript, Debug `.app` bundling, `git diff --check` and scoped Impeccable detection passed. A healthy real Dev.app normal state was not reproduced this turn.
- Converged History and Review on the frozen desktop foundation without changing learning-core contracts or FSRS behavior. History now identifies its course-scoped, at-most-50 Study Session window, sorts by real `updated_at`, uses learner-facing status-specific actions, English product dates and one row tab stop, and explicitly excludes Conversation transcripts. Review now uses one flat recall/reveal/rating flow, removes queue percentages and implementation labels, describes provenance without pretending the expected answer is source content, and refuses to rate missing or unsupported expected answers. Unknown Review write outcomes now refetch the authoritative queue and retain the exact idempotency key, revision, response and rating for a safe same-submission retry; global Space/1–4 shortcuts ignore interactive targets, modifiers and repeats. Browser/component captures and real unavailable-state Debug-app captures are under `artifacts/ui-audit/2026-07-21/history-review-foundation/`. Four focused files passed 88 tests; ESLint, strict TypeScript, Debug `.app` bundling, `git diff --check` and the Impeccable detector passed. Healthy populated History and due/revealed Review were not reproduced in the real Dev.app this turn and remain component-test evidence.
- Converged Conversation on the frozen editorial learning-record foundation without changing the answer API or persistence model. The page now leads with the current question and course scope, uses one 65–75ch flat Question/Answer flow, keeps the composer in the document frame, and exposes a labelled Ask/Cancel action instead of a floating chat affordance. Retrieval-mode badges, repeated Demo/provider/local copy, defensive cancellation internals and the learner-question bubble were removed; real warnings, retry, source citations, PDF viewing and provider recovery remain inline. Citation Inspector state now clears on route change, cancellation and stream failure, and completion/error returns programmatic focus to the affected answer. Browser Demo stays request-free, shows an individually labelled sample answer only on the explicit sample route, and persists only the unsent draft. Browser captures at 1180×740 and 900×700 plus the real Debug app recovery state are under `artifacts/ui-audit/2026-07-21/conversation-foundation/`. Focused Conversation/AgentActivity/AppShell tests, ESLint, strict TypeScript, Debug app bundling, `git diff --check` and the Impeccable detector passed; a healthy real provider completion and persisted transcript are not claimed.
- Converged Knowledge Base and Source Scope on the frozen desktop foundation without changing learning-core contracts. The page now leads with compact course scopes and one flat `Your sources` list, removes the redundant Grid mode and bottom sample/index metadata, limits rows to filename/type, course, one truthful status and changed time, and opens real course/index actions only inside the selected live row. Browser Demo uses one global disclosure and renders every seeded or newly chosen file as `Sample`; selecting a file never claims upload or indexing. Loading now preserves course/filter/list geometry, while unavailable and filtered-empty states stay local to the affected region. Browser checks at 1180×740 and 900×700 found no document overflow or unnamed buttons and verified reduced-motion; current captures are under `artifacts/ui-audit/2026-07-21/knowledge-source-scope/`. The rebuilt Debug `Keen Dev.app` reached only the truthful source-unavailable state because its bundled learning process exited unexpectedly, so a healthy live list was not visually reproduced this turn. Focused Knowledge tests passed 16/16; ESLint, strict TypeScript, Debug `.app` bundling, `git diff --check` and the Impeccable detector passed. No source-scoped Ask/Study CTA was added because the current destination routes do not consume a source/course handoff.
- Converged Learning Feed and inline Task Detail on the frozen desktop foundation without changing routes or learning-core contracts. The Feed now uses a 332 px flat task rail, ordered Today/Upcoming/Overdue/Completed controls, compact course filtering, single-status task rows and a quiet month calendar; selecting a task preserves the rail and replaces the calendar with goal, due/estimate/progress, steps and one real next action. Repeated Demo/testing/evidence copy, nested rationale cards, concept badge stacks, unavailable action placeholders and the Demo snooze mutation were removed. Browser checks at 1180×740 and 900×700 found no horizontal overflow, verified ellipsis, focus entry/return and the compact single-column reflow. The rebuilt Debug `Keen Dev.app` reached only the truthful Learning queue unavailable state, saved at `artifacts/ui-audit/2026-07-21/feed-task-detail/04-feed-error-real-dev-app.jpeg`; a healthy normal Feed and Task Detail were not reproduced in the real app this turn and remain browser/component evidence. Focused tests passed 33/33; ESLint, strict TypeScript, Debug `.app` bundling, `git diff --check` and the Impeccable detector passed with the existing Vite chunk-size warning.
- Reordered Home around the learner's next real action without changing its data contracts: a non-empty saved queue now leads the page and keeps the learning-request workbench secondary, while an empty queue or explicit request mode promotes that workbench to the primary heading and DOM order. Restore and unavailable states preserve the same flat queue/workbench geometry and use action-specific recovery copy. The latest real Debug `Keen Dev.app` reached restore and unavailable states, captured under `artifacts/ui-audit/2026-07-21/home-task-first/`; a healthy normal queue was not reproduced in the real app during this turn, so normal and empty ordering are supported by focused component/integration tests only and are not claimed as live-app verification.
- Converged the shared desktop shell and the Deep Learn pilot on the frozen HyperKnow-led foundation without changing the learning engine. Sidebar rows now use compact macOS density and consistent 16–18 px Lucide geometry; Toolbar and AppShell reserve the Inspector for a real selected source/citation/task; and shared motion tokens resolve to 150 ms controls, 190 ms inline state changes and 300 ms coordinated panels with one easing and the existing reduced-motion override. Deep Learn now keeps one 204 px learning-path rail plus a centered editorial column, uses geometry-preserving restore skeletons, flat source/checkpoint separators, one primary task action and the existing flat completed result. Protected recall/practice/summary states clear source context instead of opening a synthetic “details hidden” Inspector. A follow-up polish keeps the original session title/goal in the page header, leaves the current/next stage in the rail, removes repeated phase badges and hidden/protected implementation copy, keeps healthy core status only in the Sidebar, and formats completed review dates with Keen's English document locale without seconds. The canonical Figma file was updated in place at Provisional frame `65:2` (`Shell + Deep Learn · In Review / Provisional · 1180×740`); it was not promoted to Approved. A rebuilt unsigned `Keen Dev.app` restored a new persisted session `autonomous-session-7ebd97b7-7f59-5d52-aff6-0c8dcb6025ca` from History at pending active recall, while the earlier completed session still restored as one flat result. Real screenshots are under `artifacts/ui-audit/2026-07-21/ui-foundation-deep-learn/` and `artifacts/ui-audit/2026-07-21/deep-learn-polish/`. ESLint, strict TypeScript, 6 focused files / 58 tests, the complete 29-file / 317-test desktop suite, the follow-up 5-file / 80-test focused suite, Debug `.app` packaging, `git diff --check` and the Impeccable detector passed; the build retains the existing Vite chunk-size warning. Packaged `prefers-reduced-motion: reduce`, a deterministic long-content fixture, an accepted Deep Learn baseline and pixel parity remain unverified.
- Completed the same real local Study Session through deterministic targeted practice, Summary, persisted Review handoff and one FSRS rating. Session `autonomous-session-d626a3fe-7ec2-593f-9437-dab80e1902be` generated a source-derived fill-in prompt, recorded the correct response `composition` as one practice mastery observation, finalized at revision 10, completed its originating task and created one `fsrs-6.3.1-keen-v1` Review item. The Review route exposed that due item, stored the learner recall plus a `good` rating, and advanced its schedule to `learning`, revision 1, one repetition and zero lapses. A full `Keen Dev.app` and sidecar restart restored the completed History record, Summary handoff and post-rating empty due queue from the same dev SQLite database. The completion UI now distinguishes 100% session completion from 1 of 2 planned units completed, describes the original review handoff without implying its snapshot is the current schedule, offers real Review/Feed next actions, and clears the protected Summary Inspector when leaving the page. Desktop ESLint, strict TypeScript, 29 files / 316 tests, 23 related Python tests, Debug `.app` build, `git diff --check` and the Impeccable detector passed; the build retains the existing Vite chunk-size warning and Python retains one existing Starlette/httpx deprecation warning. No provider, signed/notarized package, accepted visual baseline, complete accessibility audit or pixel-parity claim is added.
- Closed the real local Home → Deep Learn → History → restart loop for an existing course task that already covered the recommendation target. A user-entered focused-study goal now provides the explicit initiation needed to turn that active local task into a source-grounded Study Session, while task-only autonomous starts still reject tasks without recommendation provenance. This fixes the live `covered_by_active_task` → `task_not_autonomous` dead end without changing provider, retrieval, citation, calendar, cloud or Demo behavior. A local unsigned `Keen Dev.app` used the isolated `com.keen.learning.dev` SQLite database and a clearly local test source: it created session `autonomous-session-d626a3fe-7ec2-593f-9437-dab80e1902be`, completed diagnostic plus one correct active-recall response, found the same session in History, then restored its `practicing` state and recorded recall after a full application restart. Desktop ESLint and strict TypeScript passed; 29 files / 316 desktop tests passed; 87 related Python tests passed with one existing Starlette/httpx deprecation warning; focused Ruff lint/format passed; and the unsigned Debug `.app` build passed with the existing Vite chunk-size warning. This is live local-session evidence, not a provider, signed package, visual-regression, complete accessibility or pixel-parity claim.
- Connected the first source-scoped guided-study loop without replacing the existing learning engine. Home's **Start focused study** request selects a real course, reuses autonomous recommendation → persisted Study Session → Deep Learn, and stores a normalized learner goal only when a session is first created. A new History page reads the existing SQLite study-session records through a course-scoped list contract and resumes them through the existing Deep Learn route; Browser Demo shows no substitute records. Primary navigation and the command palette now expose History. Focused validation passed 83 desktop tests, 21 autonomous-session API tests, full desktop ESLint, strict TypeScript, targeted Ruff, production build and `git diff --check`; the build retains its existing chunk-size warning. This adds no provider, Conversation persistence, visual-baseline, packaged or pixel-parity claim.
- Added a canonical product-positioning and anti-drift contract for the current phase. Product work now prioritizes one guided learning loop—from source scope and request through a visible path, study/recall, and a persisted Feed/History return point—before additional audit infrastructure, packaging probes, broad visual-baseline work, or future-integration previews. Local-first, security, provenance, and truthfulness remain hard implementation constraints but are no longer treated as the primary interface story.
- Established the first two Keen-owned visual regression baselines, limited to Browser Demo Home and Learning Feed at 1180×740 CSS px, DPR 1 and `prefers-reduced-motion: reduce`. Fresh 1× exports of Approved Figma Home `155:187` and Feed `164:179` are dimension-matched but remain separately measured provisional design references (Home 2.0290884104443423%; Feed 1.5566880439761797%), not pixel-parity targets. The existing harness passed both Keen self-reference checks at a strict 0% threshold; its Browser Demo routes made no application fetch/XHR, Tauri, learning-core or provider request. The report now has 2 passed baselines, 16 `missing_reference` and 8 provisional comparisons. Knowledge Base, Conversation, Deep Learn and all other states remain unapproved. `references/visual-baselines/README.md` records the fixture, scale, motion, hashes, Figma nodes and residual boundary.
- Added an isolated, build-time-only packaged WKWebView audit path without widening normal production behavior. `VITE_NATIVE_UI_AUDIT=true` and `tauri.ui-audit.conf.json` produce a separate ad-hoc `com.keen.learning.ui-audit` app whose visible probe reports live CSS and motion media-query values. It measured equal `inner`, root-client and `visualViewport` sizes of 1440×920 CSS px at launch and 1180×740 after native edge resizing, with DPR 2; a further shrink attempt remained at the configured 1180×740 minimum. The current packaged query reported `reduced=false`. System Settings control closed before the preference could be read or changed, so packaged `reduced=true` remains unverified and no system setting was modified. Desktop typecheck, ESLint, normal production build, audit packaging, strict deep ad-hoc signature verification and `git diff --check` passed; the normal build was checked to exclude the audit probe. Evidence is in `artifacts/ui-audit/2026-07-21/packaged-webview/`. These measurements are not deterministic visual fixtures or packaged accepted references; the separate browser baseline work does not change their residual 16 `missing_reference` plus eight `provisional` comparison boundary.
- Hardened the packaged-audit launch boundary without changing production Keen identity or UI. The macOS application menu derives its label from the active Tauri config, so the audit bundle visibly uses `Keen UI Audit`; the new fixed-path launcher derives and verifies the repository's expected version plus its local `Info.plist` display name and bundle id before `open -n`. Rust regression tests now lock release, development and audit identities as pairwise distinct. This prevents an installed same-name app from being selected as audit evidence; it does not delete, overwrite or infer the source of that app. Production remains `Keen` / `com.keen.learning`.
- Rebuilt and opened a local ad-hoc packaged `Keen.app` for a bounded WebView smoke. Home, Learning Feed, Knowledge Base, empty Conversation and no-course Study entry rendered with the authenticated sidecar ready; native keyboard focus reached Review due; and the labelled Sidebar control collapsed and restored the rail. Terminating the current sidecar child produced a supervisor-created replacement and the UI returned to ready; packaged long-learning-content and the transient unavailable UI were not captured and remain unverified. The bundled-sidecar smoke passed. The capture images are 1203×768 px, while their CSS viewport is unverified and no compact-window conclusion is claimed. Captures plus the required viewport/fixture/state/reduced-motion/Figma-node matrix are in `artifacts/ui-audit/2026-07-21/packaged-webview/`. The packaged candidates are deliberately not approved: CSS viewport and fresh-data fixture determinism, as well as packaged reduced-motion evidence, are incomplete, and only Home/Feed have current Approved Figma nodes. The visual report remains 16 `missing_reference` results plus eight `provisional` comparisons, so no accepted visual-regression or pixel-parity claim is made.
- Froze the current UI framework around a task/source/progress learning workspace instead of continuing reference-driven route changes. `docs/DESIGN_DIRECTION.md` now records the decision hierarchy, stable navigation and page structures, Figma status contract, and the bounded HyperKnow adoption matrix. The existing Figma file marks Home and Learning Feed as `Approved`, implementation snapshots as `Provisional`, and HyperKnow studies as `Reference`; Approved Home copy now uses the same truthful Browser Demo boundary as runtime. A wide-window Sidebar that had been manually collapsed now retains an accessible `Expand sidebar` action instead of becoming unrecoverable. Browser checks at 1440×920, 1180×740 and 900×700 covered Home, Feed, Knowledge Base and Conversation, found no document-level horizontal overflow, verified the compact Feed/list adaptations, captured a visible focus ring, and measured reduced-motion animation/transition durations at 0.00001 s. Desktop ESLint, strict typecheck, production build, 28 files / 313 tests, `git diff --check` and the Impeccable detector passed. Current screenshots are under `artifacts/ui-audit/2026-07-21/ui-framework-closeout/`. Packaged Tauri verification and accepted visual baselines remain open, so no pixel-parity or complete visual-regression claim is made.
- Reduced decorative icon and Demo-surface density across the current UI boundary. The hidden Learner Memory route no longer presents repeated brain glyphs, seeded learner traits, inference-like evidence, search/filter controls, browser-local enable/delete mutations or disabled edit/settings affordances; it now states the unimplemented Agent-memory boundary in a flat text-and-divider layout with one real Knowledge Base route. A new Browser Demo learning request now opens empty instead of inheriting the fixed eigenvector transcript, while the fixed transcript remains available only through an explicit non-new route and still makes no provider or retrieval request. The global Inspector no longer fabricates fallback mastery, source labels or an insight and appears only after contextual evidence has been selected. Settings combines provider and external integrations into one Capabilities section and removes redundant row/explanation icons while preserving truthful provider, Calendar, Drive and Canvas boundaries. The browser Review boundary also drops its decorative empty-state icon. At 1280×800 and 900×700 the scoped captures show no horizontal overflow; Memory, new Conversation and Settings each have one `main` and zero unnamed buttons. Desktop ESLint, strict typecheck, production build and all 312 Vitest tests passed; Impeccable detection returned no findings. The visual harness completed with 16 missing references and eight provisional comparisons, so no accepted pixel-parity claim is made.
- Distilled Agent Activity from an elevated dashboard card into a flat learning-run record. The outer radius and shadow, filled status block, nested mutation cards and 9–10 px microcopy were replaced by continuous dividers, transparent status treatment, 11–13 px supporting text and a 13 px response measure. Warnings and errors retain restrained semantic surfaces because they require emphasis. A development-only empty Activity fixture is available only with explicit Browser Demo visual-test parameters and states that no Agent ran or data changed; it is now captured at 980×900 by the visual harness. Browser inspection found one `main`, zero horizontal overflow, zero unnamed buttons, reduced-motion coverage, no panel radius/shadow and a transparent state row. The complete desktop suite passed 310/310; strict typecheck, ESLint, production build and Impeccable detection passed. The new screenshot has no accepted reference and reports `missing_reference`, so no pixel-parity claim is made. The refreshed ad-hoc arm64 DMG is 34,084,482 bytes with SHA-256 `ba49211cd61d98591054a501b1a6b010bd17fcbf1ed2e8d4eaa45c0458d21df6`; the first DMG script attempt failed after the build-tree smokes, a bounded direct retry succeeded, and mounted checksum/signature/architecture/service/restart verification passed. The exact retry record is under `artifacts/package/2026-07-21-agent-activity/`.
- Corrected the Home request action during non-cancellable Agent transitions. `creating`, `recovering`, terminal `streaming`, and `cancelling` now use the shared labelled loading treatment (`Starting…`, `Reconnecting…`, `Finishing…`, and `Cancelling…`); the square stop icon and clickable `Cancel` action appear only while a real run is active. Home owns that one cancellation control, so Agent Activity no longer duplicates it; the reusable panel also omits a cancel action when no executable callback exists instead of rendering a disabled affordance. Component regressions cover creating, cancelling and missing-action boundaries. The complete desktop suite passed 309/309; ESLint, strict typecheck and production build passed.
- Simplified Home's service-error hierarchy without changing its capability boundary. The split right-side `Local workspace` disclosure and duplicate live `Local evidence` badge were removed; local/provider provenance now reads once in the Today introduction. Startup/restart states wait for automatic recovery, while terminal local-service states show one queue error and one real supervised Retry action. `Open schedule` is hidden when the queue cannot be read, and no sample task is substituted. The request composer remains available for a local draft, states that the service must be restored before sending, and exposes that explanation through `aria-describedby`; its button and Enter path now share the same healthy-service gate, so offline Enter retains the draft and creates no Agent run or second error panel. A WKWebView-only failure was also traced to an unbound `Window.fetch`; the typed client now binds an injected fetch to `globalThis`, with a receiver-sensitive regression test. The complete desktop suite passed 308/308; ESLint, strict typecheck and production build passed. Browser inspection at 1440×900 and 980×700 confirmed the compact error composition and visible keyboard order; a browser interaction verified the offline draft remained after Enter with zero activity panels. The visual harness completed with a 2.018% comparison against the provisional Home Figma export; this is not an accepted baseline or pixel-parity claim. Current-product screenshots are recorded under `artifacts/ui-audit/2026-07-20/home-status-cleanup/`.
- Connected the first real local Review surface without widening Keen's external-integration scope. Authenticated `GET /v1/reviews/due` returns only persisted SQLite review items that are currently due, and `POST /v1/reviews/{item_id}/attempts` applies the pinned deterministic FSRS scheduler with revision checks and idempotent replay. Ratings persist the optional recall response and next schedule but do not grade the response or mutate mastery. The previous bundled Flashcards deck, sample intervals, in-memory ratings and disabled edit control were removed. `/review` now provides truthful desktop-service, loading, unavailable, error, empty, reveal, writing, cancellation-uncertainty and success states; Browser Demo shows no substitute cards. Review is discoverable as one compact Study action in the Sidebar and command palette, while Planner, Memory, Visualize and external calendar/cloud connections remain unchanged. A connection-generation guard now aborts an in-flight Review write when sidecar credentials rotate and prevents the old request from painting success or cancellation copy into the recovered queue. A same-database application restart test verifies that the persisted FSRS due time, revision and single attempt survive recreation; Rust supervisor tests verify generation-isolated lifecycle state and token invalidation. The macOS packaging gate now derives its expected migration span from the repository and runs a dedicated frozen-sidecar Review restart smoke against both the build-tree app and the app mounted from the final DMG. Across three real process generations it verifies rotated-token rejection, due read, one FSRS rating, post-restart queue recovery, same-key idempotent replay and exactly one persisted attempt. The resulting ad-hoc arm64 `Keen_0.1.0_aarch64.dmg` is 34,084,916 bytes with SHA-256 `01c4310ebdef3ea7ed9cb90b71bda1bbc0965ac44f929adf2afb531753642769`; `hdiutil verify`, strict deep code-signature validation and mounted-app smoke passed. It is not notarized or Developer ID signed. The full Python suite passed 1021/1021 with one existing Starlette/httpx warning; Ruff check and format check passed. The complete desktop suite passed 309/309; ESLint, strict typecheck and production build passed. Rust tests passed 38/38. Browser checks at 1280×800 and 760×700 found one `main`, zero horizontal overflow and zero unnamed buttons. The collapsed Review link has an explicit accessible name, keyboard reveal/rating is component-tested, and an emulated reduced-motion loading state reported two skeletons with animation disabled. A bounded isolated-release WebView check exercised due, restart, recovery, rating, post-restart empty and persisted single-attempt states; it is manual GUI evidence, not automated CI. The visual harness captured Review boundary/loading states but both references are missing, so no pixel-parity claim is made. Automated `.app` GUI interaction remains open.
- Replaced the Study Planner's simulated weekly schedule with an honest capability boundary. Fixed July 2026 sessions and derived-looking totals, in-memory completion toggles, disabled import/add/navigation/rationale controls and the calendar-confirmation preview were removed. The direct development route now states that planning is not implemented, links to the existing Learning Feed, and records three future activation gates: validated course inputs, a typed durable local planning contract, and a calendar adapter with Level 3 confirmation. These gates are informational, not clickable setup steps. Browser inspection at 1280×800 and 760×700 found one `main`, zero horizontal overflow, zero disabled controls and zero dialogs. The complete desktop suite passed 299/299; strict typecheck, ESLint, production build and Impeccable detection passed. The visual harness now captures `planner-boundary`, but its reference is missing, so no mismatch percentage or pixel-parity claim is made.
- Replaced the Visualize prompt-builder Demo with one truthful bundled teaching specimen. The editable generation prompt, Diagram/SVG/Animation selector, fake timer-based progress, simulated ready state, disabled source/export/motion controls and future Manim setup card were removed. The direct development route now renders an accessible responsive eigenvector SVG, a concise explanation, one local/offline availability row and explicit not-implemented rows for source grounding, generation/animation and export/narration. It reads no course file, calls no model or service, and creates no artifact. Browser inspection at 1280×800 and 820×720 found one `main`, zero horizontal overflow, zero unnamed buttons and zero disabled controls; compact content scrolls inside the existing page viewport. The complete desktop suite passed 298/298; strict typecheck, ESLint, production build and Impeccable detection passed. Visualize is now captured by the visual harness at 1280×800, but its baseline is missing, so no mismatch percentage or pixel-parity claim is made.
- Distilled the command palette to Keen's available primary workflows: new learning request, new study session, Home, Learning Feed, Knowledge Base and Settings. UI-only Quiz, Flashcards, Planner, Learner Memory and Visualize routes remain available for direct development checks but no longer appear as ordinary product commands; their toolbar title now carries a visible `UI demo` label. The palette now groups actions and pages, searches task-oriented keywords, provides a meaningful no-result state, supports wraparound Up/Down selection, Enter activation, Escape close, trapped Tab navigation and focus restoration. Browser checks at 1280×720 and 720×520 found no horizontal overflow, correct initial search focus and truthful Demo labeling. The complete desktop suite passed 297/297; strict typecheck, ESLint, production build, Impeccable detection and the visual harness passed. Screenshots are current-product evidence only; the visual harness still contains provisional and missing references, so no pixel-parity claim is made.
- Consolidated Button, IconButton, Badge, Progress and form-control behavior into the shared UI/runtime layer without changing feature capabilities. Button now supports compatible secondary/primary/danger/ghost variants, 30/34 px sizes and an accessible loading state; feature-owned `aria-busy` is preserved. IconButton defaults to non-submitting `type="button"`; Badge accepts semantic span attributes; Progress remains clamped and transform-driven. Shared hover, focus, active, disabled and loading behavior now lives in `styles.css` instead of duplicated `ui-refresh.css` overrides. Literal accent/status colors were replaced with semantic tokens, and Light/Dark `--warning-soft` now closes the recorded Figma/runtime token gap. The implemented component contract is documented in `docs/figma/COMPONENT_RUNTIME_SPEC.md`. Browser checks across Home, Feed, Knowledge Base, Conversation and Settings at 1280×720 found one `main`, no horizontal document overflow and no unnamed buttons on every route; visible Badges measured 22 px/11 px and shared Buttons 34 px/8 px radius. New component tests cover variants, loading, consumer-owned busy state, IconButton naming/type, semantic Badge attributes and Progress bounds. The complete desktop suite passed 295/295; strict typecheck, scoped ESLint, production build and Impeccable detection passed. The visual harness completed with only provisional or missing references; measured provisional changes remain comparison-only and do not establish pixel parity.
- Reframed Settings as a read-only capability ledger instead of a collection of simulated preferences. Six mixed real/placeholder sections are reduced to Status, Model provider, Connections, Privacy & data and Open source. The nested `main`, permanent `Saving unavailable` action, unsaved API-key/provider form, disabled generation switches, repeated unavailable viewer buttons and in-memory `Sample connected` previews were removed. Runtime status now comes from the existing Learning Core context; a real supervised Retry appears only for a retryable desktop-service failure. Google Calendar, Drive and Canvas are stated as not implemented with no OAuth or success simulation. At 1280×720 the inspected page had one `main`, no horizontal document overflow, one selected settings section, zero unnamed buttons and zero disabled form controls; keyboard order between sections is covered by a regression assertion, reduced-motion CSS remains present, and 900×700/1100×760/1440×920 captures remain readable. Focused App/Learning Core tests passed 54/54 and the complete desktop suite passed 291/291; strict typecheck, scoped ESLint, production build and Impeccable detection passed. The visual harness captures Status and Connections at default and compact sizes, but all four Settings references remain missing, so no mismatch percentage or pixel-parity claim is made.
- Refined Conversation from a generic assistant transcript into a bounded study record without changing the local RAG, stream, citation, PDF, retry, edit, stop, course-scope or service-recovery contracts. The page now uses a compact document heading, explicit Demo/live/unavailable status, separate Question and Answer records, a bounded reading column, grouped Sources and a fixed bottom workbar. Avatar bubbles, the floating gradient composer and permanently unavailable copy/branch/attachment controls were removed; Demo retains only the working local-note and edit paths and makes no provider or retrieval request. Static retrieval limitations no longer announce as transient status updates. Focused Conversation/Learning Core tests passed 46/46 and the complete desktop suite passed 291/291; strict typecheck, scoped ESLint, production build and Impeccable detection passed. Browser checks at 1510×950 and 900×700 found one `main`, zero document-width overflow, zero unnamed buttons, reduced-motion CSS and a bottom-pinned composer; a QA-only long Demo note produced independent transcript overflow without horizontal clipping. The visual harness now captures default and compact Conversation states but reports both as `missing_reference`, so no mismatch percentage or pixel-parity claim is made. Live provider, streaming, error and PDF-viewer screenshots and packaged-app verification remain open.
- Refined Knowledge Base into a source-first local workspace without changing ingestion, retrieval, provider, course or persistence contracts. Import is now one explicit page action that expands an in-place file panel; course creation is progressively disclosed; Browser Demo removes nonessential add-link controls while retaining sample unlink behavior and its no-upload/no-index disclosure. List rows keep source identity, course membership, verified indexing status and import time stable across captured 1510×950, 1100×760 and 900×700 layouts. The collapsed import panel remains mounted for existing form/state continuity but is hidden from the accessibility tree and keyboard order. Current screenshots are under `artifacts/visual-diff/current/knowledge-base*.jpg`. Focused Knowledge/course behavior passed 50/50 after adding a keyboard-path regression test; the complete desktop suite passed 291/291; strict typecheck, scoped ESLint, production build and Impeccable detection passed. The visual harness completed but still has no Knowledge Base baseline. The screenshots are current-product evidence only: no accepted Knowledge Base baseline, mismatch percentage, HyperKnow pixel parity, live ingestion success, packaged-app result or complete accessibility audit is claimed.
- Refined the existing Deep Learn reading workspace without adding a new capability. The session header, learning path and illustrative mastery rail now remain available while long lesson content scrolls; unit navigation moves keyboard focus to the newly selected lesson heading, and demo answer submission moves focus to the resulting status message instead of dropping focus to the document. A follow-up accessibility/distillation pass raises the shared light tertiary text token from 4.26:1 to approximately 4.70:1 on white, removes the nested Deep Learn `main` landmark, consolidates repeated Browser Demo disclaimers, automatically condenses the primary Sidebar to its labelled icon rail below 1000 px, and changes shared progress motion from layout-triggering width animation to transform animation. Browser checks at 1585×950 and 900×700 found one `main`, zero horizontal overflow, reduced-motion support and correct next-heading focus; the 900 px shell now uses a 60 px primary rail. The deterministic Browser Demo remains explicitly non-persistent and its source/mastery/review content remains illustrative or unverified. Focused App/Deep Learn validation passed 34/34 with strict typecheck and ESLint; the scoped Impeccable detector returned no findings after the progress fix. The updated same-size 1585×950 comparison against editable Figma node `65:2` measured 1.602% mismatch; the Figma export is provisional, so this is not an accepted-baseline or pixel-parity claim. The visual harness also refreshed the existing 1100×760 compact Summary capture. QA-injected long copy was not product/demo data.
- Added four development-only, browser-only Learning Feed visual fixtures for local startup, unavailable service, no courses and invalid live data. They require both `visualTest=true` and an explicit `visualCoreState`; they invoke neither Tauri nor fetch and do not alter packaged/live behavior. The real Feed UI now hides `Find next task` until a validated snapshot and eligible course exist, no longer renders the authenticated fallback task filter beneath the no-courses state, and uses one compact left-aligned operational-state layout with recovery actions on their own row. Chrome checks at 1180×740 found no horizontal overflow, honored reduced motion and reached both recovery buttons by Tab with the shared focus ring. Provisional Figma comparisons measure unavailable 1.314%, loading 1.218%, empty 1.057% and error 1.368%; seven older routes still lack references, so these are not accepted-baseline or pixel-parity claims.
- Continued the bounded React implementation of the approved compact Home, Learning Feed and task-detail Figma direction. Home now uses the 720 px reading/composer measure from Figma node `155:187`, removes a duplicate Browser Demo disclosure while retaining toolbar/footer and per-task Sample provenance, and uses the quieter text-only Continue action. The app shell now permits its main flex column to shrink to the actual window height; selecting a lower Feed task scrolls only `.feed-task-scroll` instead of moving the whole desktop surface. The task-detail compact layout now matches the Figma panel inset/content measure more closely, keeps its footer visible, and suppresses the decorative focus ring on the programmatically focused non-interactive heading while retaining focus transfer. An explicit Browser Demo-only `visualTest=true` fixture freezes the July 2026 calendar and can select a task deterministically; it does not affect Tauri/live service behavior. The Figma Button set `33:2` was repaired from unavailable `SF Pro Medium` labels to `Inter Medium` across all 18 variants; Code Connect remains unavailable because the current Figma account lacks the required Organization/Enterprise Dev or Full seat. React captures and exact limits are under `artifacts/ui-audit/2026-07-20/react-figma-convergence/`. Desktop strict typecheck, ESLint, 19 focused Home/Feed tests and `git diff --check` passed. The 1180×740 provisional Figma comparisons currently report Home 1.988%, Feed 1.555%, and selected task detail 3.158%; seven older routes still lack references. These are measured comparisons against provisional Figma exports, not accepted baselines or pixel-parity claims.
- Extended the Figma-only Phase-4 Learning Feed work with selected-task default `172:262` and compact `176:340` screens plus four truthful 1180×740 operational states: learning-core unavailable `178:418`, authenticated-health loading `179:596`, no local courses `179:998`, and live-data validation failure `179:1400`. The task-detail hierarchy follows the current React `TaskDetailPanel`, retains the selected task in the rail, labels 68% mastery as an illustrative Browser Demo value, and states that sample Snooze/Complete creates no real session. Operational copy comes from the current React branches, keeps the Month calendar empty, and never substitutes Demo tasks during local failure. Figma QA found zero missing fonts, non-finite geometry or horizontal root overflow. Direct exports, a four-state contact sheet and a qualitative current-screenshot/Figma comparison are under `references/keen/2026-07-20-figma-phase4/`. No React/runtime behavior, accessibility result, accepted baseline or pixel-parity claim changed.
- Completed the Figma-only Phase-4 compact Home and componentized Learning Feed slice in `ugwiIPdF43v2woYsLM3b9R` without changing React or runtime behavior. Home `155:187` reflows the existing Browser Demo design to 1180×740 with a 212 px Sidebar and 720 px reading column. Learning Feed now has reusable Feed Task Card `159:220` and Calendar Event `160:111` sets, a componentized 1585×941 default screen `162:97`, and a 1180×740 compact screen `164:179` whose calendar preserves all seven columns and truncates narrow event labels. The exact runtime/Figma boundary is in `docs/figma/LEARNING_FEED_DATA_MAPPING.md`; direct exports and Plugin API QA are under `references/keen/2026-07-20-figma-phase4/`. Final checks found no missing fonts, non-finite geometry, or horizontal root overflow; the compact task rail intentionally continues below the viewport for scrolling. Month view only, Browser Demo sample provenance, no external calendar write/connection, and no fabricated provider/session/result state remain explicit. Runtime keyboard/accessibility/reduced-motion checks, React implementation and accepted visual baselines remain pending, so no pixel-parity or visual-regression claim is made.
- Corrected the Figma-only Phase-3 Home exports after fresh 1585×907 inspection confirmed real clipping and hierarchy defects. The `⌘N` shortcut exceeded its clipped control by 1 px in all three frames, the default Sidebar status retained stale Figma text bounds and exceeded its parent by 9 px, page-level operational copy floated away from its state, and fixed-height offline/provider panels carried unnecessary space. Frames `113:3`, `130:52`, and `131:195` now have no visible text outside a clipping ancestor or non-positive text geometry; redundant floating disclosures and the stale default footer row were removed, the intro measure was widened, and empty/error panels hug content. Browser Demo, offline and provider-missing boundaries remain explicit through toolbar/footer, sample-task and state/error copy. Before/after evidence is under `references/keen/2026-07-20-figma-phase3-screenshot-audit/`; the canonical Phase-3 PNGs were refreshed. No React/runtime behavior or pixel-parity claim changed.
- Completed the Figma-only Phase-3 Home reconstruction in the existing file `ugwiIPdF43v2woYsLM3b9R` without changing the React runtime. Home Screen `113:2` now contains componentized Browser Demo `113:3`, learning-core offline `130:52`, and provider-not-configured `131:195` frames at 1585×907. Queue Row `121:26` adds Default, Hover and Focus variants while the screen reuses local Nav Row, Badge, Button and icon components. The exact runtime/Figma mapping is in `docs/figma/HOME_DATA_MAPPING.md`; final screenshots and Plugin API QA are under `references/keen/2026-07-20-figma-phase3/`. All visible text uses SF Pro, no bad text geometry or temporary lorem/shimmer artifact remains, and the operational states do not claim completion, provider success or substituted sample data. Redundant operational badges were removed after visual QA exposed Figma SF Pro overlay wrapping. Compact/dark/collapsed layouts, runtime keyboard/accessibility/reduced-motion checks, React implementation and accepted visual baselines remain pending; Code Connect was unavailable in the current Figma seat/tool context, and no pixel-parity claim is made.
- Completed the next Figma-only component and motion slice in `ugwiIPdF43v2woYsLM3b9R` without changing the React runtime. Nav Row `80:56` now covers expanded/collapsed Default, Hover, Focus and Current states with real 140 ms hover/click prototype reactions; Progress `89:46` covers deterministic 0/25/50/75/100 geometry; Segmented Control `91:54` / `92:71` covers building-block interaction states and three selected positions; Motion documentation `98:3` records Keen's current 140/220 ms tokens and reduced-motion rules; Status Indicator `104:56` defines five operational-state specimens without asserting service success. A 48→240 px, 220 ms Figma Motion WIDTH track was structurally verified, but no video-export tool was available for frame sampling. Direct Figma screenshots and limitations are under `references/keen/2026-07-19-figma-phase2/`, and `docs/DESIGN_RUNTIME_CONFLICTS.md` now defines how API/SQLite, Zod, Demo seed, code and Figma conflicts are resolved. No responsive/runtime accessibility, React implementation, accepted visual baseline, mismatch percentage or pixel-parity claim was added.
- Completed the Figma-only Phase-1 foundations reconciliation in the existing Keen file (`ugwiIPdF43v2woYsLM3b9R`) without changing the React runtime. The nine local Keen text styles now use the verified native `SF Pro` family instead of an Inter rendering proxy; Button, Badge and Service Banner text instances inherit the updated styles. The five Keen variable collections validate with no duplicate names or broken Light/Dark aliases (54 primitive, 27 semantic color, 8 spacing, 5 radius and 5 dimension variables), and the six effect styles retain the current CSS shadow/focus values with clearer usage descriptions. The Type foundation header node was replaced after visual QA exposed a malformed wrap. Fresh 1440 px-wide Color, Type and Layout renders and exact limitations are stored in `references/keen/2026-07-19-figma-phase1/README.md`. Component geometry/state normalization, screen reconstruction, React implementation, responsive/accessibility checks and accepted visual baselines remain later phases; no pixel-parity claim is made.
- Completed the read-only Phase-0 capture/inventory for the next HyperKnow → Figma reconstruction pass. Twenty-one sanitized, internal-only states at a 1585×851 CSS viewport cover Home, tool menu, Learning Feed Month/Week/quota, Knowledge Base upload/loading/completion/hover/menu/calendar-extraction states, account/settings, Deep Learn path/long reading and a synthetic cited review-plan conversation. Evidence and exact limitations are in `references/hyperknow/2026-07-19-figma-screen-audit/README.md`. One synthetic non-personal text fixture was uploaded; no OAuth connection, destructive delete, share, sign-out, source-code inspection or personal-data retention was performed. The Figma file and React UI were not modified in this phase. The existing Figma/code font and control-geometry conflicts, reusable navigation/content components, responsive variants, motion prototypes and accepted fixed-viewport baselines remain open; no pixel-parity claim is made.
- Reframed the Home default as a learning-first `Today` workspace: the real/sample learning queue and capability disclosure now precede the existing Ask/Study request surface, while the composer uses a flatter, quieter reading rhythm. Shared UI polish raises small-label legibility and control hit areas across Home, Learning Feed, task detail, Conversation, Deep Learn and Settings without changing routes, APIs, persistence, providers or service behavior. The current live Browser Demo Home, Learning Feed and Deep Learn pages were converted into editable raw Figma frames on `07 · Product Screens` in file `ugwiIPdF43v2woYsLM3b9R` (nodes `64:2`, `66:2` and `65:2`) and saved as local capture evidence under `artifacts/figma/page-baselines/`. The Figma frames are page-level baselines, not yet reusable design-system instances. Browser checks at 1280×720 found no horizontal overflow or application error on the three inspected states; the console retained only the existing React Router future warnings. Desktop ESLint, strict typecheck, 26 files / 283 tests, production build, Impeccable detection and `git diff --check` passed. The build retains its existing chunk-size warning, and the visual harness still lacks accepted references, so no pixel-parity claim is made.
- Extended the authorized synthetic-flow audit through source-grounded recall feedback, a recommendation-only three-item review plan and a successful three-page PDF upload. The stable reference captures and authentication limit are documented in `references/hyperknow/2026-07-19-full-synthetic-flow/README.md`: HyperKnow advanced the visible plan to 80% and 90%, exposed compact page-labelled source chips, and accepted `output/pdf/hyperknow-bayesian-learning-fixture.pdf`; Home navigation then redirected to an expired sign-in session, so live Calendar, Drive and Canvas entry states remain authentication-blocked rather than passed. Keen now adapts the useful evidence-summary structure only inside Browser Demo: the Summary unit shows explicitly illustrative generated feedback, unverified demo citation chips, sample mastery and an unscheduled review plan, while Settings reduces its placeholder navigation and adds reversible `Sample connected` previews for Google Calendar and Drive plus an honest Canvas `In development` state. These previews open no OAuth flow, read no account data, make no network request and disappear on reload. New visual-harness routes capture Deep Learn Summary and Connections at 1440×920 and 1100×760 with reduced motion, but all seven configured routes report `missing_reference`; no pixel-parity claim is made. Desktop ESLint, strict typecheck, production build, 26 files / 283 tests and `git diff --check` passed; the existing chunk-size and React Router future warnings remain.
- Completed a second authorized HyperKnow learning-flow audit using one synthetic, non-personal eigenvector fixture and applied one bounded UI-only continuity improvement to Deep Learn. The new rail derives `Current` and `Next` from real session state across opening diagnostic, source-grounded study, active recall, targeted practice, summary, completion and terminal states; Browser Demo uses the same structure and explicitly ends with no scheduled review. It adds no provider reply, citation, mastery result, calendar action or backend contract. The 21-step reference inventory is documented in `references/hyperknow/2026-07-19-complete-flow/README.md`; Keen default, paused and summary captures plus a same-size qualitative comparison are under `artifacts/ui-audit/2026-07-19/`. Browser checks at 1700×777 found no horizontal overflow, duplicate IDs, unnamed focusables or application errors and exercised default, paused/disabled and summary/disabled-review states. The focused Deep Learn suite passed 37/37, the full desktop suite passed 26 files / 282 tests, ESLint, strict typecheck, production build, Impeccable detection and `git diff --check` passed. The build retains its existing chunk-size and React Router future warnings; the visual harness still reports three `missing_reference` states, so no pixel-parity, packaged-app or complete keyboard-accessibility claim is made.
- Polished the bounded Deep Learn editorial workspace without changing its local-session contracts or Demo capability boundary. The header now contains only the real Pause/Resume action; progress is consolidated in the learning-path rail; the lesson measure is narrower; sample/source notes, recall, feedback and pause states use flat reading rhythm instead of side-accent cards or pill-heavy AI grammar. Answer focus now uses `preventScroll`, so opening the textarea at 1280×720 keeps `scrollY` at zero. Fresh default, empty-answer, success, paused, 1024×768 and 900×700 captures plus qualitative before/after and HyperKnow/current comparisons are under `artifacts/ui-audit/2026-07-19/deep-learn-polish/`. Programmatic checks found no horizontal overflow, duplicate IDs, unnamed focusables or browser errors; reduced motion was honored, the visible Tab sequence reached the page controls, and temporary long lesson content remained vertically scrollable without horizontal clipping. ESLint, strict typecheck, 26 files / 282 tests, production build and `git diff --check` passed. The build retains its existing chunk-size warning; the visual harness still reports three `missing_reference` states, so no pixel-parity, packaged-app or complete accessibility claim is made.
- Refined Deep Learn as an editorial study workspace using the authenticated HyperKnow flow audit as qualitative reference. Real or explicitly bundled-sample progress now sits in the learning-path rail; the reading column is left-aligned at a bounded width; source/sample material uses flat text and thin vertical callouts; the recall prompt is a quiet end-of-reading checkpoint; and the bottom navigation remains visible without becoming a floating chat composer. Live API contracts, protected recall/practice phases, persistence and Browser Demo boundaries are unchanged. Browser interaction checks exercised hint, answer, success feedback, disabled submit, Pause and disabled path/navigation states. At 1280×720 the page had no horizontal overflow, duplicate IDs or unnamed focusable controls; a reduced-motion rule was present and the console contained only the existing React Router future warnings. Source/current comparisons and state captures are under `artifacts/ui-audit/2026-07-19/deep-learn-reference-refinement/`. ESLint, strict typecheck, 26 files / 282 tests, production build and `git diff --check` passed; the build retains its existing chunk-size warning. The reference and Keen viewports differ, keyboard Tab injection was not reliable and no packaged-app or pixel-parity claim is made.
- Completed the user-attested HyperKnow authenticated research flow with one upload of the approved synthetic, non-personal fixture `references/audit-fixtures/hyperknow-ui-audit-sample.txt`. The retained internal-only screenshots cover Knowledge Base empty/upload/processing/complete/hover/menu/quota states, Deep Learn entry/trace/result/outline/reading/loading/completion states, cropped generic Settings, and Month/Week Learning Feed states under `references/hyperknow/2026-07-19-flow-audit/`. Account identity, personal memory and existing user-created learning content were not retained; Delete, calendar/integration connection and external sharing were not performed. No third-party implementation code, browser storage or private network traffic was inspected or copied, and the evidence is qualitative rather than a pixel baseline.
- Replaced the last generic Home/Conversation identity placeholders with the transparent Keen mark: the Home request heading now uses the real vector asset, Keen answers use the same asset, and learner messages render as a flat study transcript instead of a colored chat bubble. No runtime path, model response, citation, service status or product capability changed. Browser Demo evidence at 1280×720 and normalized full/focused comparisons against the authorized Home reference are under `artifacts/ui-audit/2026-07-19/brand-identity-refinement/`; the source and current captures have different aspect ratios, so the comparison is qualitative and does not establish pixel parity. Home Ask/Study mode switching and the disabled empty-submit state were exercised; the inspected console had no application errors, while the existing React Router future warnings remain. Desktop ESLint, strict typecheck, 26 files / 282 tests in a single-worker run, production build and `git diff --check` passed; the build retains its existing chunk-size warning.
- Replaced the Sidebar's cropped white-background raster tile and browser PNG favicon with transparent vector-traced Keen brand assets derived from the user's supplied mark and wordmark. Light and reverse SVG variants use stable dark `#161E25`, teal `#11ADA2` and white fills; the Sidebar now renders the real wordmark instead of a separately styled text label. A light/dark inspection sheet and integrated 1280×720 Browser Demo capture are stored under `artifacts/brand/2026-07-19/`. Browser inspection measured the mark at about 22×25 px and the wordmark at 13 px high, confirmed the collapsed mark remains centered, and found no horizontal document overflow. Desktop ESLint, strict typecheck, all 26 files / 282 tests in a single-worker run, production build, SVG XML validation and `git diff --check` passed; the build retains its existing chunk-size warning. These files are traced implementation assets rather than an original vector master, and the low-resolution source was not promoted to the packaged macOS icon set.
- Audited the current Browser Demo Home and Learning Feed visual system without changing runtime UI. Fresh 1440×900, 1180×760 and 900×700 captures are under `artifacts/ui-audit/2026-07-19/final-aesthetic-audit/`. The audit found no actual box overlap or horizontal document overflow, but confirmed excessive nested surfaces, repeated pills, border-plus-shadow cards, micro typography and the centered assistant-composer pattern as the main sources of perceived stacking and AI-template character. At 900×700, 79 of 87 visible leaf text nodes computed below 12 px; tertiary text measured 4.26:1 on white, the effective placeholder color 4.14:1, and calendar event controls about 84×21 px. The ≤930 px Feed layout stacks the full calendar below the task pane and measured 1370 px document height. Semantics remain a strength: no unnamed buttons or duplicate IDs were found, Lucide strokes are consistent, and logs contained no application error. Keyboard focus could not be verified through the audit browser. This is diagnostic evidence, not a visual-parity, full accessibility, packaged-app or implementation-completion claim.
- Used the user-provided Keen icon as the Sidebar brand mark and browser-demo favicon without changing the packaged Tauri icon set. The supplied source is 204×228, so it was not promoted to the macOS `.app`/`.dmg` artwork or represented as packaging-ready. The inspected 1440×920 Browser Demo capture is `artifacts/ui-audit/2026-07-19/keen-home-brand-1440x920.png`. Desktop ESLint, strict typecheck, 26 files / 282 tests, production build and `git diff --check` passed. The existing visual harness captured all three configured pages but returned `missing_reference` for each, so no mismatch percentage or pixel-parity claim is made.
- Extended the native Figma design system in the existing Keen study file (`ugwiIPdF43v2woYsLM3b9R`). Five local collections now contain 99 token-aligned variables after adding Light/Dark `warning-soft` primitives and a semantic alias; the existing nine text and six elevation/focus styles remain. Button set `33:2` keeps its 18 style/state variants and now lets labels stretch inside wider instances. IconButton set `45:30` covers Default, Hover, Focus, Active, Disabled and specification-only Loading states with a swappable icon property; its Plus and Refresh assets are internal Simple Design System library instances, not shipped production assets. Badge set `52:12` mirrors the React `neutral | accent | success | warning | danger` API without invented interactive states. Service Banner set `59:32` documents four real non-healthy runtime groups—Starting, Unavailable, Configuration error and Request error—with current recovery copy and nested Button instances; healthy remains the compact `LearningCoreStatus`, and no shared React `ServiceBanner` export is claimed. Direct Figma renders are under `artifacts/ui-audit/2026-07-19/figma-components/`. The Button before/after snapshot comparison changed 7,185 pixels (0.697%) after the responsive-label repair and was inspected for breakage; it is not an accepted product baseline or pixel-parity result. Runtime Loading support, the Figma 36/12 versus CSS 34/14 Button geometry gap, shared Service Banner extraction, responsive page layouts, motion prototypes and accepted visual baselines remain open.
- Home and Learning Feed return to the authorized HyperKnow-derived light layout without changing Keen's backend or capability boundary. Home now uses one centered learning-request workbench with only the existing Ask and focused-study modes, explicit Browser Demo/local-runtime copy and a lower task queue. The narrower Sidebar and Toolbar, denser task rail and low-elevation month calendar match the selected macOS desktop direction; no branded companion, news feed, generic AI tools, provider success, calendar write or invented learning result was added. Browser evidence at 1462×907, 1180×760 and 900×700 is under `artifacts/ui-audit/2026-07-19/`, with qualitative comparisons and limitations in `design-qa.md`. Frontend lint, strict typecheck, 26 files / 282 tests and production build passed. The visual harness returned three `missing_reference` results, so no pixel-parity or mismatch-percentage claim is made.
- Added an internal Figma design checkpoint for the next UI decision. Authorized HyperKnow Home and Learning Feed references were reconstructed in local HTML and converted to editable raw frames (`ugwiIPdF43v2woYsLM3b9R`, nodes `7:2` and `8:2`). Product-first Keen alternatives at nodes `9:2` and `10:2` move the default Home away from a generic assistant prompt toward an explicit current task, next step, progress and availability rail, while Learning Feed uses flat task rows and a low-decoration month grid. The studies and qualitative comparisons are stored under `artifacts/figma/`; they are not shipped runtime UI, component-library instances, accepted visual baselines or pixel-parity evidence. No third-party implementation code or dependency was incorporated.
- Audited the Figma checkpoint without changing runtime UI. Fresh 1728×907 renders and side-by-side comparisons are under `artifacts/ui-audit/2026-07-19/figma-check/`. The product-first proposal materially reduces the generic assistant feel, but the current raw frames contain no components or instances and do not yet cover focus, disabled, loading, error, success, responsive desktop, dark-mode or motion states. Small tertiary/shortcut text also remains below the intended 4.5:1 contrast target. The audit is evidence for the next design-system pass, not a visual-parity, accessibility or implementation-completion claim.
- The uncommitted enterprise-dashboard visual experiment was discarded after review. Keen retains the established light macOS desktop language shown in the user-supplied two-screen reference: pale blue-gray selection, thin dividers, low elevation, compact icon Toolbar, narrow Sidebar and generous white reading space. The current task-first Home and inline task-detail behavior remain unchanged. Browser captures at 1180×740 are under `artifacts/ui-audit/2026-07-18/style-return/`; the combined qualitative comparison is recorded in `design-qa.md` with `final result: passed`. Strict typecheck, ESLint, 26 files / 282 tests, production build and `git diff --check` passed. The visual harness still reports three `missing_reference` states, so no mismatch percentage or pixel-parity claim is made.

### Added

- Learning Feed now has an inline, task-first detail workspace without adding a route or task API. Selecting a Demo, authenticated read-only or persisted autonomous task replaces the calendar with its available goal, due time, estimate, current state, expected workflow, evidence and one truthful next-action area. Demo actions are explicitly browser-session-only; authenticated fallback tasks remain read-only; persisted tasks keep the existing confirmed Start/Resume and recovery boundary. Task cards no longer mix rationale, progress and mutation controls: they select the detail workspace, while completion, snooze and session start are consolidated in the detail footer. Focus enters the task heading and returns to the originating card when the panel closes; completed Demo feedback is announced as status. Visual checks covered 1000x720, 1180x740 and 1440x920, with screenshots under `artifacts/ui-audit/2026-07-18/keen-task-detail/`; the compact-height pass corrected a footer that initially fell below the viewport. Browser inspection found no unnamed buttons, unlabeled form fields or duplicate task-detail IDs in the inspected state. Strict typecheck and ESLint passed, all 282 desktop tests passed, production build and `git diff --check` passed, and the existing Rollup chunk-size warning remains. The visual harness returned three `missing_reference` states, so no pixel-parity claim is made; Browser Tab injection did not provide a complete keyboard audit, and packaged-app validation was not run.
- A third UI-only refinement deliberately moves Keen away from the generic AI-chat pattern. Home now opens as a task-first `Today` workspace with the learning queue and a plain study-tool list; no textbox, assistant mascot, floating composer, disabled attachment/voice affordance or idle Agent response panel is shown until the learner selects an action. The compact on-demand workbench preserves the existing Ask/Teach/Study/Review/Plan runtime, Demo request-free behavior and provider-missing boundary. Learning Feed is presented as a `Learning schedule` with `Tasks`, `Find next task` and factual task-rationale copy instead of sparkle/Agent/recommendation language. Current 1180x740 captures are under `artifacts/ui-audit/2026-07-18/keen-ui-workspace/`. Browser checks on the inspected Home and Feed states found no horizontal document overflow, duplicate IDs, unnamed focusables or console errors; the default Home contained zero textboxes, three visible sample task rows and five study tools. Strict typecheck and ESLint passed, all 282 desktop tests passed, production build and `git diff --check` passed, and the existing Rollup chunk-size warning remains. The visual harness produced three `missing_reference` results, so no mismatch percentage or pixel-parity claim is made; packaged-app validation and a complete accessibility audit were not run.
- The second Keen UI polish pass makes the existing learning workspace feel denser and more coherent without adding product capabilities. The Home toolbar now uses the product label `Home`, the 1180×740 Demo layout keeps all three bundled sample task rows in the first viewport, and Learning Feed calendar events are keyboard-focusable controls that reveal, filter to and visibly select their existing matching task; they do not create or modify calendar records. New semantic canvas, reading, overlay, progress and soft-status tokens remove the targeted pages' hard-coded light surfaces and make an unset/system theme honor macOS dark mode. Tested dark text and semantic token combinations are at least 5.42:1 on the three primary dark surfaces. Browser checks at 1180×740 found no horizontal document overflow, duplicate IDs, unnamed focusable controls or console errors on the inspected Home/Feed states, and reduced motion reported `0.00001s` transitions. Current light/dark audit screenshots are under `artifacts/ui-audit/2026-07-18/keen-ui-refresh-2/`. Strict typecheck and ESLint passed, the complete desktop suite passed 282/282, production build and `git diff --check` passed, and the existing Rollup chunk-size warning remains. The visual harness again produced three `missing_reference` results, so no mismatch percentage or pixel-parity claim is made; this was not a complete accessibility or packaged-app run.
- The authorized HyperKnow UI study now has a Keen-branded implementation pass for the three captured states: Home composer, Learning Feed/calendar and Deep Learn composer entry. The Sidebar now exposes only the primary local workflow and one study-session entry instead of presenting preview modules as equal product areas; Browser Demo recents and identity-like profile copy were removed. Home uses a centered task composer, truthful mode tools, a compact persisted/sample task list and a real learning-core status pill without seeded statistics in live mode. Learning Feed is now a responsive task rail plus month calendar backed only by the existing Demo tasks or validated local snapshot; live task creation/start/recovery behavior is unchanged. Conversation and Deep Learn receive the same restrained typography, reading width, state and responsive treatment. Shared color tokens now meet at least 4.55:1 for the tested text/semantic colors on the app background, and Progress exposes native progressbar values. Current 1440×920 captures and 1180×740 audit screenshots are stored under `artifacts/visual-diff/current/` and `artifacts/ui-audit/2026-07-18/keen-ui-refresh/`. DOM/keyboard/reduced-motion checks found no horizontal page overflow, duplicate IDs or unnamed focusable controls across Home, Feed, Study entry, Conversation and Demo Deep Learn at 1180×740; this is a focused programmatic check, not a complete accessibility audit. Strict typecheck and ESLint passed, the complete desktop suite passed 281/281, production build and `git diff --check` passed, and the existing Rollup chunk-size warning remains. The visual harness reports three `missing_reference` results because no dimension-aligned Keen baselines exist; no pixel-parity claim is made.
- The first authorization-backed HyperKnow benchmark now inventories three internal-only references: Home composer, Learning Feed/calendar, and the Deep Learn composer-entry state. Keen adopts the two validated structural improvements without copying branded assets: the Inspector now defaults closed so Home and Planner retain their primary workspace width, and the Sidebar exposes `New study session`, which selects a mode-specific Study composer without creating a session or making a service request. Browser captures before and after the change, plus a qualitative side-by-side comparison, are stored under `artifacts/ui-audit/2026-07-18/hyperknow-benchmark/`; personal/user-created sidebar content was excluded from the references. Focused validation passed 16/16 with strict typecheck and ESLint; the complete desktop suite passed 281/281, production build and `git diff --check` passed, and the existing Rollup chunk-size warning remains. No accessibility run, fixed-seed pixel diff, packaged-app validation, dependency or third-party source was added, so no HyperKnow parity claim is made.
- A focused desktop UI audit corrected the primary Home → Learning Feed → Deep Learn path. Home Agent modes and segmented filters now expose their selected state through `aria-pressed`, unit navigation exposes `aria-current="step"`, Learning Feed labels use readable title case and rationale actions no longer wrap mid-label. Deep Learn now resets transient answers when changing units, makes Pause actually suspend demo learning controls, renders an explicitly illustrative and non-persisted Summary instead of another recall prompt, and uses shrink-safe responsive header/content grids at the minimum desktop content width. Before/after Browser Demo captures are stored under `artifacts/ui-audit/2026-07-18/`; they verify the visible sample UI only and are not HyperKnow references, visual-diff evidence, live-session evidence or packaged-app acceptance. Focused interaction tests passed 42/42; the complete desktop suite passed 280/280, and strict typecheck, ESLint, production build and `git diff --check` passed. The build retained the existing Rollup chunk-size warning. No dependency or third-party source was added.
- A first runnable study-summary framework now closes the implemented one-unit Deep Learn path with a deterministic, learner-safe recap and one real local FSRS review schedule. Forward migration 026 records an immutable practice-to-review handoff; the authenticated GET/POST boundary is course-scoped, body-bounded, idempotent and transactional with current-unit/session completion. The strict desktop client restores the summary before rendering source-derived detail, reconciles unknown writes, and exposes an explicit `Finish and schedule local review` action. Focused Python validation passed 32/32 and the complete Python suite passed 1017/1017; the complete desktop suite passed 278/278 with strict typecheck, ESLint and production build passing. Ruff lint/format and `git diff --check` passed. A 1280×760 Browser Demo shell screenshot was captured for UI discussion, but it is sample-only and is not evidence of the live persisted summary, accessibility, packaged-app behavior, visual parity or a full multi-unit loop. Direct task completion Undo and multi-unit progression remain open. No dependency or third-party source was added.
- Targeted practice now runs in live Deep Learn through strict TypeScript/Zod GET/begin/answer contracts. The UI restores before enabling actions, protects lesson/header/navigation/concept/Inspector clues while a prompt is pending or unresolved, preserves exact unknown-write retries, and truthfully separates paused/cancelled states with or without an actual run. Answered copy records one practice mastery observation without claiming a Review/FSRS schedule change. Final Sol acceptance found P0/P1/P2 all zero; the focused suite passed 11/11 and the full desktop suite passed 270/270 with typecheck, ESLint and production build passing. Browser Demo remains request-free. No screenshot, accessibility, packaged-E2E or visual-parity claim is made.
- Targeted practice now has a deterministic source-grounded backend and authenticated GET/begin/answer API. It selects a distinct answer from the cited current chunk while excluding the active-recall answer; migration 025 seals the unit/chunk content and answer-key fingerprints, then an answer creates one atomic practice BKT observation with no Review/FSRS write. Strict Pydantic/authentication/64 KiB request handling, course scope, begin-before-terminal behavior and late exact replay are covered. Focused targeted-practice/active-recall/migration validation passed 108/108; the full Python suite passed 1013/1013 with one existing Starlette/httpx deprecation warning; Ruff lint/format and `git diff --check` passed; final Sol acceptance found P0/P1/P2 all zero. The TypeScript/UI slice is recorded above; summary and Review scheduling/FSRS handoff remain open. No dependency or third-party source was added.
- A strict TypeScript/Zod client and live Deep Learn Active Recall view now restore, begin and answer the persisted source-grounded cloze. While a prompt is pending or its state is unresolved, lesson text, unit labels, session title/goal, concepts, source controls and the global Inspector are protected before paint; request scope also includes the sidecar generation. Writes are single-flight and cancellable, unknown results retain the exact key/revision/run/response for read-before-retry reconciliation, and known conflicts cannot reopen stale controls when reconciliation fails. Paused sessions remain read-only, answered copy explicitly does not claim a Review/FSRS schedule change, and Browser Demo remains request-free. Final independent Sol acceptance found P0/P1/P2 all zero. Focused validation passed 32/32, the full desktop suite passed 259/259, and strict typecheck, ESLint and production build passed. No screenshot, accessibility audit, packaged-app E2E or visual-parity result is claimed.
- Authenticated active-recall GET/begin/answer routes expose the deterministic domain core through strict snake_case models. Authentication and the streamed 64 KiB cap precede validation; course-scope 404s, known 409 conflicts, read 503s and potentially durable unknown-write 503s remain distinct. A second learner-safe projection verifies all returned relationships and omits source/concept/assessment/item identifiers, hidden answers, raw responses and idempotency evidence. Begin returns 201/200 for applied/replay; answer returns 200 for both. Final Sol HTTP acceptance found P0/P1/P2 all zero; its subset passed 35/35 and the full Python suite passed 982/982. The TypeScript client and desktop interaction were completed in the subsequent frontend slice.
- The first active-recall Python domain slice generates one deterministic fill-blank item from a cited source excerpt, stores its answer only in an immutable published assessment, grades one objective response, records positive-weight active-recall evidence, and applies concept-specific weighted BKT exactly once without changing FSRS. Migration 024 closes course/session/unit/source/scoring relationships, concurrent replay and duplicate-attempt paths; cancellation/failure records a cancelled run and skipped checkpoint without evidence. Learner-safe results omit source/goal/concept text that could reveal the answer, and all graph timestamps use the request time. Final Sol acceptance found P0/P1/P2 all zero; focused validation passed 58/58 and the full Python suite passed 978/978. HTTP and desktop integration were delivered by the following slices; no visual claim is made.
- Live Deep Learn now restores and completes the persisted opening diagnostic through strict client contracts. It hides unit content and concept metadata until the reflection is answered, keeps paused phases read-only, labels the response as unscored and non-mastery-changing, and handles single-flight, cancellation, 409 and unknown write outcomes without changing an idempotency key's payload. Final independent review reported P0/P1/P2 all zero; desktop validation passed 246/246 plus strict typecheck, ESLint and production build. Browser Demo remains request-free. No screenshot, accessibility or visual-parity claim is made.
- Forward migration 023 and authenticated Study Session diagnostic begin/read/answer routes add a deterministic, restart-safe opening reflection. Begin and answer have separate durable idempotency, paused phases restore correctly, and concurrent or unknown-result retries converge without duplicate evidence. The answer records immutable zero-weight `user_report` evidence only; it is not scored and does not mutate mastery, Review/FSRS, task completion or unit completion. A true 22-to-23 migration fixture, concurrency and injected rollback tests passed; final independent review reported P0/P1/P2 all zero, the full Python suite passed 935/935, and Ruff lint/format plus `git diff --check` passed. Desktop diagnostic controls remain open and no visual claim is made.
- Repository authorization now permits reading the live user-authorized HyperKnow page, ordinary click/scroll/navigation, screenshot capture and internal implementation use of covered protected visual content. The permission is recorded as a user attestation, not independently verified; fabrication remains prohibited and no new reference capture or visual-diff evidence is claimed.
- Forward-only learning-loop migrations 009–016 and typed persistence repositories for Conversation, Agent run/tool/mutation audit, Study Sessions and versioned plans, Assessment attempts/hints/evaluations, mastery evidence, misconceptions, Review/FSRS state, and explainable Study Tasks. Gate 1 preserves legacy learning rows and has repository/migration evidence only; no live learning-loop UI or Agent execution is claimed yet.
- Forward migration 017 and a pinned py-fsrs 6.3.1 adapter add stable positive review-card identities, upgrade pristine 015 schedules without deleting data, enforce UTC/version/state boundaries, and make repository-created schedules restart-round-trippable. Focused scheduler/repository/migration/vector tests passed 43/43, and the stable full Python suite passed 500/500 with one existing Starlette warning. This is deterministic scheduler/repository evidence only; the Flashcards UI and full assessment-to-review workflow remain open.
- Gate 3 foundation adds typed Agent tools, Level 1/2/3 fail-closed permission checks, cancellable single-step execution, typed reversible mutations, bounded untrusted document data, and redacted audit summaries. The reviewed foundation passed 35/35 focused tests. An initial real-data tool subset for Feed reads, due Review reads and transactional Study Task completion passed 6/6.
- A provider-neutral single-Agent service now persists ordered redacted events, durable tool-step states and bounded provider output, and supports explicit completion, cancellation/failure states plus Last-Event-ID SSE replay. Its 17/17 focused tests do not cover process-restart provider continuation, FastAPI routes or frontend consumption; those remain open.
- Forward migration 018, a real SQLite audit/idempotency adapter and allowlisted Study Task Undo/redo now commit Level 2 domain writes, typed mutations and success audit atomically. Uncertain commits reconcile without false completion or repeated writes; a restricted authorizer-backed tool session blocks transaction escape and cross-table access. The final full Python suite passed 548/548 and independent adversarial review found no remaining P0/P1. FastAPI Agent routes, visible Undo, process-restart provider continuation, real provider selection and remaining product tools stay open.
- Authenticated Agent run create/get/cancel and durable SSE event routes now execute through an app-scoped background runtime and database connection. Startup recovery idempotently persists interrupted/error terminal events, normal Level 2 events expose their real `mutationId`, run creation uses the 64 KiB JSON guard, and an unconfigured production runtime returns `provider_missing` rather than substituting automation. Independent verification passed Python 566/566 and the related subset 105/105; the execution window also passed an expanded 113/113 focused subset, with Ruff lint/format and `git diff --check` passing. A real provider, Undo HTTP, frontend Agent activity/API client, and kill-restart/socket E2E remain open.
- A strict TypeScript Agent client now validates create/get/cancel responses and durable SSE events, preserves event IDs for Last-Event-ID/cursor reconnect, rejects hidden reasoning and unknown response fields, and safely maps provider/busy failures. Desktop lint, strict typecheck and all 124 Vitest tests passed; the focused Agent/API-client subset passed 61/61. No visible Agent Activity UI is claimed yet.
- Authenticated Study Task Undo/Redo HTTP resources now execute only recorded allowlisted inverses through the atomic Level 2 audit boundary. They enforce terminal-run ownership, strict idempotency semantics, 64 KiB declared/chunked request limits, durable step/invocation/mutation events, and restart recovery across the step-only, running-invocation and committed-before-event crash windows. Run-scoped idempotency hashes preserve semantic key ownership without storing the original key in step data. The full Python suite passed 577/577, Ruff lint/format passed 117 files, and final independent review found no remaining P0/P1. Visible Undo and additional Level 2 entity handlers remain open.
- A visible Home Agent runtime now uses the authenticated local run API for Ask/Teach/Study/Review/Plan, explicit cancel, durable activity, Last-Event-ID/token-rotation recovery and terminal-only Study Task Undo/Redo. Strict mutation-action SSE variants keep post-terminal audit events without exposing raw tool results or hidden reasoning; provider/offline/partial/interrupted/cancelled/failed states are explicit and waiting approval is read-only. Browser Demo remains request-free and seeded Feed/statistics remain labeled Sample. Focused API-client/reducer/panel/runtime/Home tests passed 26/26, 8/8, 14/14, 9/9 and 5/5; desktop ESLint, strict typecheck, production build and all 170/170 Vitest tests passed. A 1440×920 current Home capture completed as `missing_reference`; a tool-capable multi-turn provider, authorized visual baseline and kill-restart/socket E2E remain open.
- Configured loopback Ollama/OpenAI-compatible chat models now automatically back a real text-only Agent provider while an installation without local-chat configuration still returns `provider_missing`. The adapter keeps supporting JSON in an explicitly untrusted user-message section, exposes no tools, requires the guarded chat transport's real completion marker and non-empty user-facing output, preserves the configured model revision—or a deterministic SHA-256 fingerprint for long revisions—in durable run metadata, bounds deltas, closes/cancels the transport, and fails without `done` on transport errors or common split hidden-reasoning tags. Invalid provider output uses a stable safe error code/recovery message without publishing blank content. Explicit test Agent factories still take precedence. The full Python suite passed 588/588 with one existing Starlette warning; Ruff lint/format passed all 119 learning-core Python files. This is not a tool-capable multi-turn Agent.
- A Unix-only real-socket Agent recovery E2E now starts a test-only blocking provider behind the production FastAPI runtime and real Uvicorn listener on `127.0.0.1:0`, creates a run, reads a durable partial SSE cursor, SIGKILLs the isolated process group, restarts against the same SQLite database with a rotated token, and verifies old-token rejection, `interrupted/process_restarted`, cursor-only recovery replay and second-restart idempotency. The helper is confined to tests and adds no production automation provider. The full Python suite passed 589/589 with one existing Starlette warning; Ruff lint/format passed all 121 learning-core Python files. This proves the Python socket/auth/database crash boundary, not Tauri supervisor relaunch or packaged-app GUI lifecycle.
- A provider-private multi-turn tool-feedback foundation now lets a tool-aware provider emit a `ToolCall`, receive the durably completed result, and use that result before generating its next action. Every registered tool must declare a recursively closed, strict Pydantic `result_model`; the executor forces strict normalization and revalidation before feedback. Full results are marked `trust=untrusted_tool_data`/`fidelity=full`, while recovered invocations receive only explicitly lower-fidelity `audit_summary` data. Public SSE and SQLite events retain redacted summaries, never raw tool text or mutation before/after/undo. Providers without feedback capability fail before tool execution, cancellation cleanup is bounded, and cumulative 16-round/256 KiB feedback limits fail closed. Deterministic tool-event IDs plus startup reconciliation now recover the domain/audit-committed-before-publication crash window before the run is marked interrupted, without duplicate mutation events. A repeated provider `call_id` with identical arguments executes once, returns private full→audit-summary feedback, and publishes one public result; changed arguments fail without re-execution. The full Python suite passed 621/621 with one existing Starlette warning; Ruff lint/format passed all 122 learning-core Python files. LocalChat remains text-only and no production model is yet allowed to select tools.
- Production Agent runtime tool execution now defaults to an empty allowlist. Only the in-process `FixedAutomationProvider` test fixture receives the registered tool set; an injected provider that merely implements tool feedback is rejected before `tool_start`, audit reservation or side effects. The loopback OpenAI-compatible transport now requires one normal `finish_reason=stop` before `[DONE]`, and Ollama requires `done_reason=stop`; missing, duplicate, `length`, content-filtered or otherwise non-normal termination fails instead of producing false completion. Focused provider/API tests passed 30/30, and the full Python suite passed 623/623 with one existing Starlette warning; Ruff lint/format passed all 122 learning-core Python files. A production read-tool catalog and persisted run-derived course scope remain open.
- Forward migration 019 now persists each Agent run's trusted `course_scope_id`, resolving study-session scope before conversation scope and never trusting user input, provider output or prompt text. Historical runs are backfilled without deleting data; the scope remains immutable audit evidence after an associated conversation/session is deleted through its existing `ON DELETE SET NULL`, while deleting the referenced course remains restricted. Missing or mismatched contexts fail with stable `ValueError`, and idempotent run replay compares the resolved scope. Independent acceptance found and then verified the fix for the original context-deletion trigger regression. The full Python suite passed 628/628 with one existing Starlette warning; Ruff lint/format passed all 123 learning-core Python files. LocalChat remains text-only and production tool execution remains deny-by-default; scoped catalog, host-injected fixed run time/course arguments and the read-only executor are not yet connected.
- Configured loopback Ollama/OpenAI-compatible Agent providers now support bounded structured multi-round tool calls. A registry-derived runtime capability keeps catalog, allowlist and executor together; a production model receives only course-scoped Study Feed and due-Review reads, and their schemas expose only `limit` because the host binds persisted course scope and fixed run creation time. Strict protocol/transport validation rejects unknown or parallel calls, malformed terminal states, duplicate keys, non-finite JSON, hidden reasoning and oversized responses/history. Real API coverage proves a private SQLite review result reaches the second provider round but not public SSE, while Level 2/3 tools remain denied before audit or mutation. Tool rounds currently use bounded non-streaming JSON; final public Agent activity remains durable SSE. Initial Sol review found four P1 issues: cancellation cleanup could mask cancellation, fixture subclasses could inherit trust, catalog/allowlist/executor could be mispaired or mutated, and due-review SQL did not bound rows. All four were repaired with adversarial tests, and final Sol re-audit found no open P0/P1. The final full Python suite passed 768/768 with one existing Starlette warning; Ruff lint/format passed all 129 files and `git diff --check` passed. ContextDelta `2e206888…`, Spark Agent `f21158df…` and Vera `5611699e…` were reviewed as exact-revision candidates only; no third-party code or dependency was incorporated. Approval execution, packaged GUI/provider E2E and visual parity remain open.
- A third production Level 1 tool, `search_course_knowledge`, reuses Keen's existing course-filtered Latin/CJK FTS path to retrieve only indexed current-course chunks. Provider input is limited to a normalized searchable query and limit 1–5; course scope remains host-bound. Outputs explicitly identify lexical-only mode and untrusted course data, retain bounded chunk/document/page/section provenance, mark truncation/`has_more`, omit course-link metadata and storage internals, and cap source text at 1,200 characters per result and 6,000 total. A query-only connection, off-loop FTS, SQLite progress interruption and cancellation cleanup preserve the read/cancellation boundary. Concrete Ollama API coverage proves current-course private feedback drives the second round while cross-course text, storage paths and injection-shaped source instructions stay out of public SSE and persisted audit summaries; no mutation occurs. Final Sol review found no open P0/P1; focused tests passed 91/91 and the full Python suite passed 781/781 with one existing warning. Ruff lint/format and `git diff --check` passed. This slice is lexical-only and adds no new rendered citation UI or visual evidence.
- Safe Level 1 read failures can now return a closed, redacted provider-private `tool_error`, allowing the configured OpenAI-compatible or Ollama Agent to correct arguments or retry a temporary SQLite BUSY/LOCKED read in the same run. Failure step state and the public failed-tool lifecycle event are atomic and durable before feedback; cancellation, permissions, Level 2/3, contract/protocol failures and uncertain audits still terminate. Error feedback is bounded by round/byte, four-per-run and identical-failure limits. A real Ollama API test covers invalid scoped knowledge arguments → corrected call → successful retrieval → final answer with no public source/path/SQL/exception leakage or mutation. Agent Activity marks the recoverable failure and settles unfinished attempts on terminal run states. Full validation passed Python 797/797, desktop 178/178 plus lint/typecheck/build, Ruff lint/format, compileall and `git diff --check`; final Sol re-review found no remaining P0/P1/P2. No dependency or third-party source was added.
- Forward migration 020 and the Agent runtime now implement one closed Level 2 proposal/approval path for `complete_study_task`. The model can propose only task ID and expected revision; the host injects persisted course scope and confirmation-time UTC time, stores immutable private canonical arguments, and exposes only a bounded task/course/effect summary. Confirm executes the local task update, audit, reversible mutation and terminal events atomically; Reject, cancel, stale revision and restart apply no task mutation. Same-key retries replay, conflicting resolutions fail closed with refresh guidance, and the existing Undo action reverses a confirmed completion. Invalid provider proposals are durably redacted before correction feedback. Home Agent Activity now shows offline-aware single-flight Confirm/Reject controls and preserves an unknown-outcome idempotency key. Full validation passed Python 811/811 and desktop 191/191 plus Ruff lint/format, compileall, ESLint, strict typecheck, production build and `git diff --check`; final independent Sol review found no remaining P0/P1/P2. The 1440×920 visual harness still reports `missing_reference`, so no visual-parity claim is made. No dependency or third-party source was added.
- Forward migration 021 and authenticated `POST /v1/courses` add the first real user-created learning-loop entry point. New writes normalize NFKC/whitespace/case for title uniqueness, serialize concurrent creators under one SQLite write transaction, replay an identical idempotency key, and reject title/key conflicts without rewriting legacy duplicate courses or exposing SQLite details. The strict Zod client and Knowledge Base form keep Browser Demo request-free, single-flight creation, abort on unmount, preserve an unknown-outcome key, update the authoritative course cache, and select a confirmed course for the next import. Knowledge document actions were split from the page without changing their tested behavior. Local validation passed Python 820/820 and desktop 205/205 plus Rust 38/38, Ruff lint/format, compileall, migration/sidecar preflight, ESLint, strict typecheck, production build and `git diff --check`. A GitHub Actions definition now mirrors frontend/Python/Rust checks and keeps macOS packaging manual-only; it is not claimed as a remote run or release attestation. No dependency or third-party source was added.
- The autonomous-learning foundation now reuses the existing course, document, mastery, misconception, Review, Study and Feed repositories to build one bounded, course-scoped Learning Snapshot. Its deterministic action tiers are due Review, incomplete Session, very weak concept, repeated misconception and weak concept; the existing feed-priority algorithm supplies the explainable score within those product tiers. A separate indexed-document bootstrap creates at most one deterministic concept plus a zero-attempt BKT prior without fabricating learner evidence or mastery events. Existing or incomplete mastery state is preserved: missing state with history fails explicitly, initialization provenance is reported only for the call that performed it, and Snapshot exposes rather than hides state gaps. Full Python validation passed 838/838 with one existing Starlette warning; Ruff lint/format, compileall and `git diff --check` passed, and independent Sol review found no remaining P0/P1/P2. This batch has no API or UI yet and makes no autonomous-loop or visual claim. No dependency or third-party source was added.
- A deterministic autonomous-recommendation coordinator now turns the Learning Snapshot's first fixed-tier candidate into at most one active, ordinary Study Task. One SQLite write transaction covers optional indexed-document bootstrap, observation, real source/concept coverage and task creation; same-day and cross-midnight concurrent callers converge on one task, while an existing manual or legacy task reports `covered_by_active_task` rather than a false replay. Persisted recommendation evidence retains the complete versioned Feed-priority components, explanation and unclamped score, and titles/reasons use bounded real learning labels. Full Python validation passed 852/852 with one existing Starlette warning; the focused suite passed 50/50, Ruff lint/format, compileall and `git diff --check` passed, and independent Sol acceptance found P0/P1/P2 all zero. This slice has no API or UI yet and does not execute the recommended study action. No dependency or third-party source was added.
- Authenticated `GET /v1/learning-snapshot` and `POST /v1/autonomous-recommendations` now expose the deterministic observation and one-task recommendation through strict snake_case Pydantic responses. The sidecar owns UTC time, the POST shares the existing 64 KiB streaming request guard, a created task is present in the returned post-write Snapshot, and 200/201 outcomes remain distinguishable. The boundary preserves finite historical priority scores above one, same-day completed replay, and date-only/naive legacy task timestamps by explicitly normalizing Keen's stored UTC convention; malformed stored state fails closed through redacted read/write 503 responses. Focused Python validation passed 56/56, Ruff lint/format and `git diff --check` passed, and final independent Sol acceptance found P0/P1/P2 all zero. Desktop rendering and recommended-action execution remain separate open work; no dependency or third-party source was added.
- Forward migration 022 and a source-grounded session coordinator now turn an active autonomous Study Task into exactly one recoverable Study Session. A nullable task foreign key, partial unique index and bidirectional course triggers preserve restart/concurrency identity and prevent task/session course drift or deletion. One write transaction validates the autonomous provenance and course, resumes existing work, or creates a two-unit plan from only the current version of indexed, course-linked chunks; one real chunk is split into two non-empty original-text parts rather than duplicated or synthesized. Missing/short sources, manual tasks, cross-course requests and terminal sessions produce typed blocked outcomes with no partial write or foreign payload. The coordinator creates no answers, completion, mastery or learner evidence. The combined focused migration/session/state suite passed 34/34; Ruff lint/format and `git diff --check` passed; final independent Sol acceptance found P0/P1/P2 all zero. No start-session API or desktop Start control is claimed yet, and no dependency or third-party source was added.
- Authenticated `POST /v1/autonomous-study-sessions` now exposes the source-grounded Start/Resume boundary. Strict snake_case input uses server-owned UTC and the existing 64 KiB authenticated streaming guard; `201` means a session and cited two-unit plan were created, while `200` truthfully represents resume or one of eight typed blocked outcomes. Responses omit storage paths and internal JSON, and pre-serialization invariants bind the requested course/task to the returned task, session and plan. Service and API checks both fail closed on damaged cross-course relationships; unknown SQLite write results use a redacted 503 with durable-outcome guidance, legacy naive/date timestamps normalize to UTC, and malformed timestamps/internal JSON do not leak. Candidate responses now also carry their nullable concept linkage so the client can verify misconception-to-task attribution. Independent final Sol acceptance found P0/P1/P2 all zero; focused API/service/recommendation/migration combinations passed 18/18, 40/40 and 33/33 with Ruff lint/format and `git diff --check` passing. The desktop Start control is still open in this backend slice. No dependency or third-party source was added.
- The live Learning Feed now consumes strict Snapshot/recommendation client contracts: it selects course and study-time budget, displays candidates and persisted task outcomes, and can Plan one next local action. Loading, empty, error, offline, cancelled and unknown-outcome reconciliation are explicit, with course/budget/generation scope, cache and late-response isolation. Home live mode no longer presents seeded Feed/statistics as learner facts; Browser Demo stays request-free and labels its sample data. Focused desktop tests passed 64/64, all desktop tests passed 216/216, strict typecheck, ESLint and production build passed, and focused Python Snapshot/recommendation tests passed 30/30. Independent Sol acceptance found P0/P1/P2 all zero. No screenshot, visual-diff or accessibility evidence is claimed; desktop Start/Resume is not implemented, Deep Learn remains a hard-coded demo, and real session reading is in progress.

- An authenticated persisted Study Session reader and live Feed Start/Resume now connect an active task to the real Deep Learn route. The reader returns only the stored plan and bounded source citations, while strict desktop source-ID validation guards the rendered data. Reader-focused tests passed 42/42 and Sol acceptance found P0/P1/P2 all zero; source-ID contract tests passed 62/62. Focused desktop tests passed 69/69, the full desktop suite passed 226/226, and strict typecheck, ESLint and production build passed; final desktop Sol acceptance found P0/P1/P2 all zero. The final Python suite passed 905/905, Ruff lint/format passed, and Rust formatting, Clippy with warnings denied, and 38/38 tests passed. Deep Learn is deliberately read-only: it writes no progress, answers, mastery, or completion. Browser Demo remains explicitly sample-only and makes zero requests. No screenshot, visual-diff or accessibility evidence was produced, so this is not a visual-parity or HyperKnow-parity claim. Assessment, mastery updates and FSRS progression remain open.

- Repository-wide engineering, security, permission, provenance, and verification rules in `AGENTS.md`.
- Initial empty-repository and missing-reference audit.
- Evidence-gated milestone implementation plan.
- Visual-reference intake and regression backlog that explicitly blocks pixel-parity claims until authorized assets arrive.
- Initial open-source candidate matrix and license-scan history, followed by evidence-backed upstream review records.
- Read-only upstream audit with exact review-only SHAs, verified parent licenses, copyright/NOTICE/header findings, and module-level reuse decisions; no upstream source was added to Keen.
- Empty initial upstream patch ledger and third-party notices baseline.
- Tauri 2 macOS shell with native menu/shortcuts, persisted window state, minimal capabilities, an initial provisional application icon later superseded by the 2026-07-23 canonical mark, and 256-bit ephemeral token generation.
- React/Vite strict-TypeScript desktop UI with Sidebar, Toolbar, Inspector, command palette, Home, Learning Feed, Knowledge Base, Conversation, Deep Learn, Quiz, Flashcards, Planner, Memory, Visualize, and Settings demo routes.
- Truthful offline/demo states that disable provider persistence, model-generated conversation answers/export, calendar writes, and unimplemented source viewing/highlighting.
- FastAPI loopback learning-core slice with Bearer authentication, structured logs, SQLite migration/idempotent seed, course/task/mastery APIs, deterministic BKT, and explicit offline/no-citation SSE.
- Versioned document schema and bounded PDF/Markdown/TXT ingestion with streamed size limits, extension/MIME/content validation, SHA-256 content-addressed storage and deduplication, recoverable status history, pypdf 6.14.2 extraction, and stored one-based page/chunk locations.
- SQLite FTS5 lexical search plus deterministic extractive `/v1/query` responses whose citations carry document/chunk IDs, section paths, one-based page numbers, and source excerpts; empty retrieval returns no inferred answer.
- Reusable strict-Zod learning-core client and Tauri runtime provider. Learning Feed now reads live task/mastery state, while Knowledge Base can list, upload, and search live local sources with explicit starting, unavailable, error, and browser-Demo states.
- Tauri learning-core supervision with a Python-child-owned random loopback bind, exact trusted-pipe port announcement, a 256-bit token delivered once through stdin instead of argv/environment, authenticated startup and continuing liveness probes, one bounded restart, child-exit detection, dual supervisor/bootloader watchdogs, an isolated process group with whole-tree termination, bounded shutdown, per-generation private extraction directories, stale-generation reclamation, and frontend endpoint rediscovery/cache isolation. No shell capability is granted to the frontend.
- Pre-body ASGI authentication and request caps, request-scoped thread-safe SQLite connections, a canonical-identity cross-process database owner lock, serialized/atomic same-hash imports, symlink-confined document storage, 0600 database/file and 0700 data-directory modes, interrupted-import recovery, and stale temporary-upload cleanup.
- PDF parsing in a spawned worker with wall-clock, CPU, memory, page, extracted-character, and chunk budgets, including frozen-PyInstaller spawn handling and macOS worker-tree RSS monitoring, so compressed/operator-heavy files cannot exhaust the long-lived sidecar process.
- PyInstaller 6.21.0 sidecar build script, fixed external-binary configuration, packaged migration/seed data, target-triple validation, dependency preflight, frozen-binary authentication/PDF/resource-limit smoke tests, and a packaging gate that validates the one newly built DMG and repeats the smoke from its mounted `.app`.
- Separate ad-hoc local and Developer ID release signing paths. The local path disables hardened runtime; the release path passes one `APPLE_SIGNING_IDENTITY` through PyInstaller and Tauri before enabling hardened runtime.
- Artifact-level license evidence for pypdf 6.14.2 and python-multipart 0.0.32, plus installed-license review of PyInstaller 6.21.0 and PyInstaller community hooks 2026.6. Distribution notice reconciliation remains incomplete.
- ESLint, Vitest, pytest, Rust fmt/clippy/tests, visual capture/diff harness, and dependency license/security reports.
- Verified arm64 development `Keen.app` and `Keen_0.1.0_aarch64.dmg` bundles; DMG checksum validation passed.
- Verified a later arm64 development `.app` and `.dmg` containing the frozen learning-core sidecar. Manual application lifecycle smoke covered launch, authenticated loopback-only health, bounded single restart, restart-budget enforcement, and cleanup after application quit. The automated packaging gate covered valid PDF import, compressed-PDF resource rejection, process-set cleanup, `hdiutil verify`, and strict deep code-signature verification against both the build-tree app and the mounted final DMG.
- Final local arm64 artifact: `Keen_0.1.0_aarch64.dmg`, 21,761,944 bytes, SHA-256 `da60e4b08bd3b3c8b77e90bbc2c7a7c625009f810d44400a806081242dfc5e87`; it is ad-hoc signed and unnotarized, not a distributable release.
- Development/production identity isolation, explicit-only demo seeding, a strict sidecar PORT/phase/READY/health protocol, generation-safe manual restart with token rotation, shutdown-intent/reopen lifecycle handling, bounded child-output frames, and frontend recovery states whose Retry action invokes the supervisor instead of merely refetching unavailable state.
- Durable single-worker document index jobs with `202` import, persisted stage/progress, real PDF/process cancellation, retry/delete/restart recovery, conservative storage quarantine/reconciliation, missing-source repair, configurable parser budgets, streaming request-size enforcement, and Knowledge Base job actions backed by the authenticated local API.
- Many-to-many course/document links with legacy backfill, same-hash cross-course file/index reuse, stable `courseIds`, explicit link/unlink APIs, and Knowledge Base course selection, filtering, and real multi-course labels.
- Dual lexical retrieval using the existing Latin `unicode61` index plus a feature-tested CJK `trigram` index, with script routing, bounded CJK shingles, mixed-query reciprocal-rank fusion, course filtering, and chunk deduplication.
- Provider-neutral local embedding/vector interfaces, exact-pinned sqlite-vec and HTTPX dependencies with provenance evidence, loopback-only Ollama/OpenAI-compatible embedding calls, immutable model/version/dimension state, ready-only vector search, bounded hybrid RRF, and explicit lexical-only fallbacks.
- Durable embedding-only reindex jobs with isolated staging and atomic promotion. Model changes expose `needs-reindex`; cancellation, provider failure, unexpected failure and restart interruption preserve existing chunks, FTS search and prior-model vectors.
- Strict API/client and Knowledge Base states for provider missing/failure, lexical-only, hybrid, embedding progress and reindex recovery, including a real model-pinned reindex action and no provider calls in Browser Demo.
- Loopback-only OpenAI-compatible and Ollama chat adapters with bounded streaming protocols, redirect/timeout/size guards, explicit terminal-marker validation, and cancellation that closes the provider response on client disconnect.
- A strict generated-answer SSE contract with retrieval events, incremental deltas, database-owned structural citation mapping, invalid-marker rejection, explicit warnings/errors, and an honest `structural_only` validation declaration.
- Live Tauri Conversation streaming with stop, retry, edit-and-resend, draft persistence, course scope, restart/network/provider/lexical-only states, and mapped citation Inspector. Browser Demo remains an explicit fixed transcript and its tests make no network or Tauri request.
- Migration 008 PDF line geometry, authenticated canonical PDF content/range serving, and a page-on-demand local PDF.js viewer with exact-page highlights for supported pages. Rotated/CropBox/custom-user-unit/translated-box pages fail closed to an explicit page-only citation state; the viewer caps authenticated bytes and canvas pixels, cleans up close/load/render races, traps/restores focus, and never fabricates a rectangle.
- A CPython 3.11/macOS arm64 PEP 751 runtime lock with hashed wheels, enforced clean-environment installation, locked-version/import/native-extension checks, and isolated PyInstaller freezing.
- Final hardening artifact: `Keen_0.1.0_aarch64.dmg`, 33,495,605 bytes, SHA-256 `5246ab0c4955498f2891561789d9fb6a27de9fddb13790282606ddf51fb0f16f`. Both build-tree and mounted-DMG sidecars passed READY, async import/poll/cancel, CJK, same-hash multi-course, sqlite-vec, PDF geometry/content, token secrecy, recovery, and process-cleanup smoke; both app executables are arm64 and the mounted app passed strict deep signature validation.

### Known limitations

- At the initial audit, no HyperKnow reference directory or authorized-scope file existed, so visual parity was not measurable and brand-asset reuse was not authorized by repository evidence.
- At the initial audit, there were no dependency manifests, lockfiles, or upstream checkouts, so license scanning could not run and candidate licenses remained unverified.
- HyperKnow pixel parity remains blocked because no authorized reference capture or visual-diff evidence has been accepted; the canonical authorization scope is now present.
- The initial development bundles were ad-hoc/linker-signed, not notarized, and omitted the Python sidecar. The current arm64 app/DMG include the sidecar and use valid ordinary ad-hoc seals for local verification; hardened runtime is deliberately disabled on this no-Team-ID path so the PyInstaller one-file dylibs can load. Gatekeeper rejects the artifact. Intel/Universal builds and the same-Team Developer ID hardened-runtime/notarization path are unverified.
- OCR, reranking, and citation entailment validation are not implemented. PDF highlights are supported only where validated zero-origin, unrotated, unit-scale MediaBox/CropBox geometry is available; other pages explicitly fall back to page navigation without a highlight. The frozen local embedding/hybrid, loopback chat, PDF geometry/content, and vector stacks were verified inside the final mounted DMG.
- A real loopback structured Agent provider, provider-private multi-turn result and safe read-error feedback, persisted trusted run course scope, three scoped Level 1 reads including lexical course-knowledge retrieval, frontend Agent activity, visible Study Task Undo/Redo, and one confirmed `complete_study_task` proposal path are implemented. Hybrid Agent retrieval, other Level 2 domains, all Level 3 actions, Keychain-backed remote providers, the FSRS-backed Flashcards/assessment workflow, calendar/notifications, complete persistence, automated GUI lifecycle E2E, accessibility/performance, and complete shipped notices remain open.
- HyperKnow reference captures remain absent despite the canonical authorization scope now being present, so pixel parity is still blocked and no mismatch threshold is claimed.

## 2026-07-26 Deep Learn recall continuity

- Browser Demo Deep Learn now presents one continuous read → recall → feedback flow. The prompt explains that plain-language answers and unformatted formulas are accepted, the answer editor has an explicit label and cancel path, unit navigation is unavailable while editing, and correct, hint, and revision feedback render as restrained inline document results.
- The duplicate page-level Demo banner was removed because the App shell already owns the global disclosure. The lesson header keeps a compact sample/not-saved cue for standalone truthfulness.
- Focused Deep Learn validation passed 27/27 tests, strict TypeScript, and scoped ESLint. Browser inspection at 1280×768 and 900×768 found no horizontal document overflow, one `main`, and no unnamed buttons. This is current-browser evidence, not pixel parity or packaged-WebView acceptance.

## 2026-07-26 Supporting empty-state continuity

- History and Review Browser Demo states now retain the visible structure of a real record collection and due queue. A compact header owns the collection label and truthful zero count; one bounded empty body owns the explanation and primary action.
- The earlier three-column instructional strip was removed after competitor review. HyperKnow informed the list-first structure, while StudyFetch and RemNote informed the object/queue-first hierarchy; no third-party mark, copy, source or feature breadth was incorporated.
- History, Review and App workflow validation passed 62/62 tests, strict TypeScript, scoped ESLint, the Impeccable detector and the production frontend build. Browser inspection at 1280×768 and 900×768 found no horizontal document overflow, one `main`, and no unnamed buttons.

## 2026-07-27 DeepTutor-structure packaged polish

- Applied a bounded structural polish from the reviewed DeepTutor v1.5.1 screenshots without copying its runtime, source, branding or feature breadth. Home starts higher; Knowledge Base defers import/course-management detail; History uses one contextual empty state; Settings uses learner-facing Status, Capabilities, Privacy & data and Open source sections.
- Neutralized unavailable primary actions, aligned shared select geometry and added an explicit local chevron asset. The single global Browser Demo disclosure remains available without duplicating visible labels.
- Inspected a fresh production-named packaged app across all six release-navigation pages plus Settings Capabilities; captures are under `artifacts/ui-audit/2026-07-27/deeptutor-polish/`. The full desktop suite passed 47 files / 543 tests; strict TypeScript, ESLint, production build, Impeccable detection and `git diff --check` passed. The unchanged bundled sidecar passed the complete DMG and restart smoke before the final CSS-only rebuild. Pixel parity, packaged Dark/reduced-motion, Developer ID signing and notarization remain unclaimed.

## 2026-07-27 Round 03 UI convergence acceptance

- Simplified the remaining release-page actions and empty states: Home uses a
  labeled submit action, Knowledge Base exposes one first-source import,
  History preserves real mastery while cached sessions refetch, Review uses
  learner-facing queue language, and Settings is limited to Status, Model,
  Privacy and About while retaining the real provider form.
- ChatGPT Pro reviewed the fixed Round 03 evidence bundle across UI/design,
  interaction, learning-Agent and architecture boundaries and returned
  `ACCEPT` with no must-fix items. The decision is recorded in
  `artifacts/orchestrator/round-03/orchestrator-review.md`.
- Targeted desktop validation passed 8 files / 158 tests and the final Review
  copy check passed 1 file / 21 tests. Strict TypeScript, ESLint and the
  production frontend build passed. The existing visual-regression command
  still exits 1 because references are missing or stale; no baseline was
  silently replaced and no pixel-parity claim is made.
- A fresh packaged `Keen.app` reached `Learning core ready`. Home, real
  zero-data Knowledge Base, Feed, History, Review, Settings Status and Settings
  Model were inspected at 1203×768. Strict deep signature validation and the
  bundled-sidecar smoke passed. Developer ID signing, notarization, packaged
  Dark/reduced-motion and fixed-reference parity remain open.

## 2026-07-27 Learning Feed task-detail refinement

- Replaced the selected task's dashboard-like facts grid and generated-looking
  timeline with compact metadata, one learning brief and a short next-step
  list.
- Reflowed the task tabs and course filter so labels remain intact in the
  narrow rail.
- Rewrote Browser Demo actions as normal learning actions while preserving a
  clear no-save boundary.
- Captured the corrected 1203×768 state and a qualitative before/after
  comparison. Strict TypeScript and 44 focused desktop tests passed.

## 2026-07-27 Core learning pages and adaptive-decision visibility

- Reworked Deep Learn, Review and History into one quiet, list/reader-first
  product language bounded by the authorized DeepTutor structure.
- Added native MathML presentation and plain-language recall guidance.
- Exposed the already-persisted adaptive action reason in source review and
  targeted practice, and added a truthful “Why this review” explanation based
  on the real Review item and FSRS state.
- Replaced History's oversized first-use card with a compact next-action row.
- ChatGPT Pro returned `ACCEPT` after the second review, with no blocker for
  the DeepSeek AGI core-business management-trainee portfolio framing.
- The final production `.app` reached `Learning core ready`; packaged-Light
  History and Review empty states were captured, and a normal `⌘Q` cleanly
  stopped the bundled sidecar. Strict deep-signature verification and the
  bundled-sidecar smoke passed.

## 2026-07-27 Adaptive prerequisite loop native acceptance

- Closed the packaged native acceptance gate for the bounded Adaptive
  Prerequisite Intervention Loop without adding another page, Agent framework
  or proposal branch. An isolated `Keen Provider Acceptance.app` restored a
  real persisted Diagnostic/Recall/Intervention chain, automatically produced
  one source-grounded `deepseek-v4-flash` plan proposal, and exposed the
  learner-owned reason, evidence, citation, Accept and Keep boundary.
- Native Accept appended plan version 2; native Undo appended recovery version
  3. After a complete app/sidecar quit and cold launch, Home returned to the
  exact Deep Learn Session with the same `Adjustment undone` receipt and plan
  version 3. Authoritative counts remained one proposal, two Session Agent
  runs and twelve durable events, so recovery did not create another proposal
  or provider call.
- The fresh app passed strict deep signature verification. Four 1203×768
  captures, runtime hashes, identities, SQL counts, exact test boundaries and
  limitations are recorded in
  `artifacts/orchestrator/round-05-agent-loop/native-acceptance.md`. The Keep
  path remains component/service tested rather than fabricated by resetting
  this native evidence chain. No learning-efficacy, pixel-parity,
  Dark/reduced-motion, Developer ID or notarization claim is added.
