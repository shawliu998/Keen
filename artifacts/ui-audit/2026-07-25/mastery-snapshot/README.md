# History current-mastery snapshot

- `history-current-mastery.jpeg` is a 1220×768 native capture from the rebuilt ad-hoc Debug `Keen Dev.app`.
- `history-legacy-concept-label.jpeg` verifies the bounded one-line fallback for an older concept whose stored name is a full learning request.
- The selected `Calculus I` course contains three persisted mastery rows with recorded attempts. The UI shows the weakest concepts first and uses the exact current probabilities as bar lengths.
- This is a current snapshot, not a time series or a learning-outcome claim. Zero-attempt priors are intentionally hidden.
- New Focused Study requests now derive a concise deterministic concept label from the source-retrieval topic. Existing persisted names are not migrated or rewritten.
- The capture is qualitative implementation evidence only. It is not an accepted visual baseline, pixel-parity result, complete accessibility audit, Developer ID build, or notarized release.
