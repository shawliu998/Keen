# Deep Learn practice refinement

The persisted Calculus session
`focused-session-151b83d4-fed0-5f35-88ab-2c359f0979b2` was restored from
History in the rebuilt Debug app.

- `learning-path-and-cloze.jpeg` verifies the neutral compact learning rail.
- `cloze-input.jpeg` verifies the separated source statement, normalized
  formula, single-line `Missing term` field and `Check answer` action.

The accepted answer remains server-side and the existing deterministic
practice write/retry contract is unchanged. These captures are current-state
evidence, not accepted visual baselines or a pixel-parity claim.
