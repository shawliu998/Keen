# Visual Reference and Regression TODO

2026-07-28 provider-setup note: the existing Settings → Model surface now
starts with provider choice, keeps model and Keychain input visible, places
the locked or editable API base under one Advanced disclosure, and presents
save → learning-service restart → model-access verification as a compact
three-step status. Compatibility presets are labelled as presets or previews,
not as vendor-certified integrations. Focused component behavior and strict
TypeScript passed, but this change has no new packaged screenshot,
accepted-reference diff, Dark/reduced-motion observation or pixel-parity
claim.

2026-07-28 provider package note: the rebuilt arm64 app and mounted DMG passed
the frozen-sidecar Provider/fresh-profile learning/recovery gate, but this
gate operates through authenticated packaged APIs and adds no native Settings
screenshot. The provider form's visual state therefore remains covered by
component behavior rather than a new packaged visual claim. The ad-hoc app is
not notarized, and `spctl` rejection is recorded as a distribution boundary,
not a visual defect.

2026-07-27 adaptive-proposal note: Deep Learn now has one additional truthful
state inside the existing Plan Proposal block: `Agent suggested · not applied`,
followed by a bounded `Why now`, saved-evidence count and the existing
Accept/Keep decision. It adds no page, modal, Agent avatar, dashboard, third
column or navigation item. Provider-backed Explain/source-example actions now
carry one quiet point-of-action disclosure; deterministic local Practice does
not. The UI contract passed 45 focused desktop tests, strict TypeScript and
ESLint. This increment has no new packaged screenshot or accepted reference
diff, so packaged adaptive-provider state, Dark/reduced-motion observation and
pixel parity remain open. The code/contract review is recorded in
[`round-05-agent-loop`](../artifacts/orchestrator/round-05-agent-loop/pro-review.md).

2026-07-27 real-Provider native closeout: the isolated native DeepSeek path
produced and restored one verified source-grounded Agent explanation beside
its exact pending Practice. The first packaged run exposed a real continuity
regression: once Practice became current, the successful artifact disappeared
with the stale failure fallback it was meant to supersede. Deep Learn now keeps
the intervention restore probe mounted, hides obsolete fallback only, and
shows a verified artifact in the same document flow as its Practice. The real
artifact also exposed both display `\[...\]` and inline `\(...\)` notation;
both now reuse bounded accessible native MathML rather than literal
delimiters. Current evidence is
[`02-artifact-practice-mathml.jpeg`](../artifacts/orchestrator/provider-acceptance/02-artifact-practice-mathml.jpeg);
Home cold restore and connected Provider evidence are in the same
[`provider-acceptance`](../artifacts/orchestrator/provider-acceptance/)
directory. Focused desktop evidence passed 4 files / 52 tests with strict
TypeScript and ESLint, and the final ad-hoc arm64 package restored the same
artifact/Practice after cold launch. Pro returned `A) ACCEPT` and `STOP`;
further UI expansion is not an open visual task. No accepted-reference diff,
pixel parity, Developer ID or notarization claim is added.

2026-07-27 packaged vertical-acceptance note: a dedicated native app/profile
completed the real default journey from indexed Eigenvectors source through a
visible two-Unit path, both Recall/Practice cycles, Summary, Review scheduling,
Home and History. Restarts restored the exact in-progress and completed
Session. Native inspection exposed a state-continuity issue rather than a new
visual direction: a stale `provider_missing` result could replace the newer
Practice surface after restore. The authoritative Practice state now wins.
Bounded inline `\(...\)` content also renders as accessible native MathML
instead of raw delimiters; current evidence is
[`13-review-mathml.jpeg`](../artifacts/orchestrator/vertical-acceptance/13-review-mathml.jpeg).
The remaining journey captures and Pro's `A) ACCEPT` are in
[`vertical-acceptance`](../artifacts/orchestrator/vertical-acceptance/).
This closes the current native journey observation without claiming an
accepted-reference diff or pixel parity. Pro explicitly stopped further UI
expansion; the next bounded package is real-provider acceptance using the
existing interface.

2026-07-27 Deep Learn workspace refinement: the current two-column reading
workspace now behaves as one continuous surface at the enforced 1180×740
desktop size. The learning-path rail fills the available height instead of
ending mid-page, while its compact-width layout returns to content height.
Browser Demo no longer repeats `sample · not saved` inside the session header
because the shell already carries the single global Demo disclosure. The
visible Pause control is compact, the step label is shortened to `Read, then
recall`, and the bottom action row groups Previous with the on-demand Demo
source while preserving Next unit as the only primary action. The source
remains after Recall in keyboard order. Browser inspection at 1180×740 and
1000×720 reported zero horizontal overflow and zero unnamed buttons; the
1180×740 rail ended exactly at the viewport boundary, and no console errors
were reported. Strict TypeScript, ESLint and 3 focused files / 50 tests passed.
This is Browser Demo evidence, not a packaged-WebView, Dark/reduced-motion,
accepted-reference-diff or pixel-parity result.

2026-07-26 Learning Feed density note: the task-first split workspace now
continues the current DeepTutor-informed shell hierarchy without importing an
upstream component or changing task behavior. The task rail is narrower and
denser, active section and selected task use the restrained Keen teal state
layer, task titles retain their full row width instead of wrapping around a
status badge, and the detail heading plus sticky next action align to one
reading column. A non-empty section now opens its first task by default instead
of leaving an unused detail canvas; closing detail returns to the compact
Overview state, and changing section opens that section's first task. The
detail toolbar now identifies the course and uses one icon-only close action;
the duplicate contextual Calendar action was removed, while the task-pane
Calendar handoff remains secondary. Demo task detail now shows two connected
steps rather than a single orphaned step. Browser inspection at 1180×740 and
1000×720 reported zero horizontal document overflow; the selected task also
had zero internal horizontal overflow. The final 1180×740 interaction pass
reported one selected row, one matching detail panel, zero unnamed buttons and
no console errors. Strict TypeScript, ESLint and 2 focused files / 44 tests
passed. This is Browser Demo evidence only; a persistent packaged-WebView
capture, accepted-reference diff, Dark/reduced-motion pass and pixel-parity
claim remain open.

2026-07-26 reopened UI-shell note: after direct comparison with the reviewed
DeepTutor v1.5.1 Home screenshot, the earlier top-aligned draft was superseded.
Keen now follows the reference geometry more closely: a 192 px light sidebar,
quiet borderless Home toolbar, centered Keen mark plus serif prompt, 680 px
composer, intent/live source controls inside its bottom toolbar and one compact
send action. DeepTutor branding, broad navigation and unavailable controls were
not incorporated; the existing saved-work composition and runtime contracts
are unchanged. The final pass applies Keen teal only to the creation action,
current navigation/mode, send action and focus feedback; the composer retains a
single outer focus ring, and the sidebar Demo disclosure no longer clips.
Browser inspection at 1180×740 found no wrapping or horizontal overflow; the
accent capture is
[`home-deeptutor-accent-v3-1180x740.png`](../artifacts/ui-audit/2026-07-26/home-deeptutor-accent-v3-1180x740.png).
Strict TypeScript, ESLint and 3 focused files / 54 tests passed. This is Browser
Demo evidence, not a packaged-WebView, accepted-reference diff, Dark-mode
capture or pixel-parity claim.

2026-07-26 guided-learning UI closeout note: the packaged UI keeps an enforced 1180×740 minimum, so 1000px and 760px evidence is correctly recorded as browser responsive verification rather than a native-window claim. Feed at both widths, Dark Deep Learn Summary at 1000×720 and Dark History at 760×700 all reported one main landmark, zero unnamed buttons, reduced motion enabled and zero horizontal document overflow. A synthetic display-only no-space title retained zero document and element overflow; it was not persisted or cited as product data. Native keyboard inspection of the real Upcoming task confirmed focus moves to its normalized task heading and the next Tab reaches the only `Open study` action. Captures are under [`release-closeout`](../artifacts/ui-audit/2026-07-26/release-closeout/). No product code changed. Complete screen-reader coverage, packaged Dark/reduced-motion below the enforced minimum, an accepted reference diff, Developer ID/notarization and pixel parity remain open; further unrelated UI polishing is not the current product priority.

2026-07-26 supporting-continuity note: Feed, Deep Learn and History now use one learner-facing title and handoff vocabulary while retaining the exact persisted task/session data. At 1180×740 the rebuilt native Feed shows `Strengthen …`, a plain-language weak-concept reason, two saved learning steps and one `Open study` action; History shows the normalized session title once, replaces the false summary-stage `0%` indicator with the saved Recall/Practice result, and ends with `Finish and schedule review`. Current evidence is [`feed-task-detail-1180.png`](../artifacts/ui-audit/2026-07-26/supporting-continuity-native/feed-task-detail-1180.png) and [`history-session-1180.png`](../artifacts/ui-audit/2026-07-26/supporting-continuity-native/history-session-1180.png). Neither primary action was invoked during inspection. The complete desktop suite passed 47 files / 538 tests; strict TypeScript, scoped ESLint, production build, ad-hoc Tauri app build and scoped Impeccable detection passed. Dark/reduced-motion, complete keyboard/screen-reader coverage, an accepted reference diff, Developer ID/notarization and pixel parity remain open.

2026-07-26 Deep Learn outcome-continuity note: the useful HyperKnow pattern retained here is document-flow continuity, not its chat composer, side progress ornament or upstream visual assets. Keen's Recall, Learning Agent, Practice and ready-to-finish states now read as one saved learning sequence instead of unrelated cards and implementation logs. Recall and practice outcomes share the same flat state/result/evidence/next-action hierarchy; `Incorrect`, local FSRS and mastery-observation copy no longer lead the interface. The path rail and final action both say `Finish and schedule review`, while generated mode prefixes are omitted only from the displayed Deep Learn title. A fresh packaged app restored the existing ready summary at 1180×740 without scheduling its review; current native evidence is [`summary-ready-1180.png`](../artifacts/ui-audit/2026-07-26/deep-learn-flow-native/summary-ready-1180.png). The complete desktop suite passed 46 files / 531 tests; strict TypeScript, scoped ESLint, production build, ad-hoc Tauri app build and scoped Impeccable detection passed. Native Recall/Agent/Practice captures, Dark/reduced-motion, complete keyboard/screen-reader coverage, an accepted reference diff, Developer ID/notarization and pixel parity remain open.

2026-07-26 DeepTutor selected-source workspace note: the reviewed reference's useful file-rail/content-stage hierarchy is now a Keen-owned, course-scoped workspace instead of an inline table expansion. The 1280×720 Browser Demo capture uses a 248 px source rail and 742 px preview stage; the 900×700 capture uses 220 px and 542 px respectively. Both retain one clear selected file, the All libraries return action, zero horizontal overflow and zero unnamed buttons. Demo rows remain metadata-only; live PDFs alone cross the authenticated content boundary and render through PDF.js, while unsupported formats fail closed. Captures are under [`deeptutor-knowledge`](../artifacts/ui-audit/2026-07-26/deeptutor-knowledge/). The packaged closeout is under [`knowledge-reader-native`](../artifacts/ui-audit/2026-07-26/knowledge-reader-native/): a fresh sidecar-bundled app at 1180×740 rendered the real three-page PDF, exposed one focused-study handoff, and kept course links plus deletion behind collapsed Source settings. Native accessibility inspection covered the PDF image, pagination, CTA, and collapsed/expanded settings without executing a learning or destructive action. The complete desktop suite passed 46 files / 531 tests; strict TypeScript, ESLint, production build, ad-hoc Tauri app build and scoped Impeccable detection passed. The Debug PDF's roughly 25-second first render was not benchmarked. Dark/reduced-motion, complete keyboard/screen-reader coverage, an accepted visual diff, Developer ID/notarization and pixel parity remain open.

2026-07-26 DeepTutor Knowledge Base overview note: Keen now maps the reviewed reference's knowledge-base-first hierarchy onto real course scopes rather than copying its retrieval-engine administration. Four Browser Demo course libraries render as two restrained rows at 1280×800; selecting one updates the native source filter and the source list from five rows to the two linked rows, and selecting it again restores the full list. The 992×226 px library grid leaves the Sources heading, tools and first rows visible in the initial viewport. At 900×700, it retains two 377 px columns and still exposes the Sources heading and first row without horizontal overflow. Captures are under [`deeptutor-knowledge`](../artifacts/ui-audit/2026-07-26/deeptutor-knowledge/); both widths had zero horizontal overflow and zero unnamed buttons, and the inspected desktop run had no console errors. Strict TypeScript, ESLint, Impeccable detection and 2 focused files / 68 tests passed. The selected-source note above supersedes this checkpoint's preview gap; packaged WebView, Dark/reduced-motion, accepted visual diff and pixel parity remain open.

2026-07-26 DeepTutor Home adaptation note: the existing Keen `New learning` state now uses the reviewed reference's useful centered hierarchy without adding a route, sidebar item, upstream asset or parallel component system. The Keen mark and copy lead into one 720 px composer; Ask/Focused study, the truthful live source selector and one send control share its toolbar. Browser Demo at 1280×800 measured the composer at 720×153 px with zero horizontal overflow, zero unnamed buttons and no console errors. Current evidence is [`home-new-learning-browser-demo.png`](../artifacts/ui-audit/2026-07-26/deeptutor-home/home-new-learning-browser-demo.png); strict TypeScript, ESLint and 3 focused files / 45 tests passed. This is not an accepted upstream visual diff, packaged-WebView capture, Dark/reduced-motion result or pixel-parity claim; the editable Figma reference replica remains In Review.

2026-07-25 History current-mastery note: History now places one compact, flat ranked-bar snapshot before saved Questions and Study sessions when the selected course has real recorded mastery attempts. The 1220×768 rebuilt native Debug-app captures under `artifacts/ui-audit/2026-07-25/mastery-snapshot/` show three persisted Calculus I rows, weakest first, with explicit percentage and attempt labels, plus the one-line ellipsis fallback for an older full-request concept name. Its full name remains available to assistive technology and as a title; new Focused Study records receive concise deterministic topic labels at creation. The bars reuse Keen tokens and semantic meters; no gradient, shadow, chart dependency, Lieflat Charts code, or third-party asset was introduced. This is a current snapshot only because no time-series contract exists. Dark mode, compact-width reflow, reduced motion, a complete keyboard/screen-reader audit, an accepted reference, visual diff and pixel parity remain unverified.

2026-07-22 lightweight-continuity note: Home's existing approved saved-work composition now resolves one real task across all courses and, when that task owns a persisted Study Session, shows its saved goal, progress, and phase-specific next action before resuming exact Deep Learn context. Non-Session work retains the scoped Feed handoff. The same quiet row gains ellipsis protection for long course/progress text. This is not a new Journey page, navigation item, component direction, Figma frame, screenshot baseline, or pixel-parity result. Focused behavioral evidence is 5 files / 140 tests; a fresh packaged visual/restart capture was not produced for this delta.

Current priority: visual work supports the end-to-end guided learning slice defined in `docs/PRODUCT_POSITIONING.md`. Do not expand standalone baseline, audit, or packaging infrastructure before that slice is coherent; existing missing references remain recorded but are not the active product objective.

## Current app rebuild and Reduce Motion blocker — 2026-07-25

The current Feed/Deep Learn anti-template changes were rebuilt into the
production-named ad-hoc arm64 `Keen.app`. The production frontend and release
Rust binary built successfully; both the desktop executable and bundled
learning-core are arm64, and `codesign --verify --deep --strict` passed.

A second native attempt reached System Settings → Accessibility, but the control
channel closed again when opening Display. Reconnecting the control runtime
did not restore access. No system preference was changed. Consequently this is
build/signature evidence only: packaged reduced-motion-on and native visual
inspection of the latest two UI changes remain unverified.

## Whole-product anti-template pass — 2026-07-25

Home, Learning Feed's unselected and selected detail states, Deep Learn,
Knowledge Base, History, Review, and Settings were inspected together in
Browser Demo at 1180×760. The shared shell, restrained surfaces, one creation
entry, page typography, button vocabulary, and empty-state structure were
consistent. The pass found two structural defects rather than a need for a new
visual direction:

- Demo Deep Learn omitted the existing `session-header-live` variant, putting
  Pause on a second row and expanding the header from 74 px to 120 px. It now
  reuses that shared two-column header.
- Feed's unselected detail showed three empty pseudo-fields beneath its
  instruction, implying missing data. The placeholders and their orphaned CSS
  were removed; the task list and real selected detail are unchanged.

Both repaired pages showed zero horizontal document overflow. Focused
validation passed 2 files / 50 tests, strict TypeScript and ESLint; the scoped
Impeccable detector returned no findings. This is Browser Demo evidence, not a
packaged-WebView result, accepted-reference comparison, complete accessibility
audit, or pixel-parity claim. Packaged reduced-motion-on remains open.

## Deep Learn keyboard and long-content closeout — 2026-07-25

At 1180×760, Browser Demo now has a component regression for the ordered
keyboard path from `Pause demo` through the path-collapse control and seven
unit choices to `Answer now`. A temporary browser-only stress case also replaced
the lesson lead with one long unbroken source token. Before the repair, the
paragraph had 1,794 px of inaccessible internal overflow while the document
itself exposed no horizontal scroll. The reading column now inherits
`overflow-wrap: anywhere`; the same case reports zero element and document
overflow. Reload restored the bundled content after inspection.

The focused Deep Learn file passed 27/27 tests, strict TypeScript and ESLint.
This verifies the Browser Demo path and reading-column boundary, not every
keyboard route or arbitrary persisted document rendering. Native
reduced-motion-on remains open: the macOS UI-control channel closed before the
Display preference opened, and no system preference was changed. Accepted
reference diff, Developer ID/notarization and pixel parity also remain open.

## Shell/Task Detail/History/Review truthfulness delta — 2026-07-25

Four small presentation corrections, no new visual direction: the Toolbar no longer carries a duplicate accent `New learning` icon button, Task Detail Expected steps start at 1 without a pre-completed fake item, History's both-empty case merges into one quiet empty composition with one `New learning` action, and Review reuses the Study cloze presentation instead of exposing raw source prose and formula words in one heading. Focused tests passed 6 files / 100 tests with strict TypeScript and scoped ESLint. A rebuilt Debug app was inspected natively for Shell, Feed and the real due Review; `supporting-pages-refinement/review-cloze.jpeg` records the Review result. History's both-empty visual state, Dark, reduced-motion-on and accepted-reference comparison remain open; no pixel-parity result is claimed.

## Deep Learn V3 interaction preview — 2026-07-25

The design-only preview at `docs/design-previews/agent-composer/deep-learn.html` tests the approved two-column Deep Learn direction without adding a runtime route, component system or dependency. It preserves the canonical Keen shell and bounded reading column, removes the duplicated path-level Quick Recall step, and consolidates Ask, closed-book Recall, deterministic feedback, misconception-targeted practice and next-unit handoff into one non-overlay Learning Dock. Sources remain an on-demand drawer instead of a third persistent column.

Browser inspection at 1510×1107, 1280×900 and 1180×800 found no horizontal document overflow and exposed one visible input in each state. Recall hides the answer-bearing lesson, focuses the textarea and temporarily compacts the path; its Dock begins 36px after the locked next-step copy rather than leaving an empty viewport. Hint use, incomplete/source-review/retry, misconception/targeted-practice, correct completion, path advancement and the next-unit Ask reset were exercised without console or page errors. The screenshots in `docs/design-previews/agent-composer/` are preview evidence only: they are not accepted baselines, Figma parity, a complete keyboard/accessibility audit, packaged-Tauri evidence, live Provider output or persisted product state. Only the bounded Recall focus behavior described below has entered the runtime; broader Dock/hint/handoff mapping remains pending product and contract review.

## Deep Learn Recall focus runtime delta — 2026-07-25

The first bounded V3 runtime delta reuses the existing two-column Deep Learn page and persisted Active Recall contract. A confirmed pending prompt is labelled `Closed-book recall`, removes the otherwise empty phase separator/offset, hides source-derived lesson material through the existing protection boundary and focuses the sole response textarea. Generated `Study: <goal>` titles no longer repeat the identical goal below the heading; distinct title/goal pairs remain unchanged. Answer completion removes the focus-state class and returns focus to the real result heading; paused and recovery states do not receive the pending-prompt focus behavior. Focused component tests passed 40/40 with strict TypeScript, scoped ESLint and `git diff --check`.

A rebuilt ad-hoc Debug `Keen Dev.app` restored one real pending Recall after a full app/sidecar restart. The expanded and collapsed 1220×768 captures under `artifacts/ui-audit/2026-07-25/recall-focus/` verify the deduplicated header, closed-book label, focused response, absent lesson/Inspector and stable two-column/collapsed-rail compositions. The disabled submit is skipped by keyboard traversal. Read-only SQLite confirmed the Recall remained unanswered and created no mastery event or Review handoff. The bundle's arm64 desktop and sidecar passed strict deep signature verification.

Visual verification remains open at the exact 1180×740 native minimum, plus long prompt/answer content, Dark appearance and reduced-motion-on. These captures are not an accepted reference, complete accessibility audit or pixel-parity result. The preview's Learning Dock, hint treatment and next-unit choreography remain design-only and must not be treated as shipped behavior.

## Bounded Agent intervention visual boundary — 2026-07-25

The selected-artifact lineage increment adds no new visual surface or design
direction. The existing `Try another approach…` interaction now binds its
request to the visible ready run/artifact and preserves that exact pair for an
uncertain retry; stale identity is restored as the existing conflict state.
Focused Deep Learn component coverage passed 14/14. The rebuilt ad-hoc
`Keen Dev.app` then rendered a real `deepseek-v4-flash` source-example
successor and restored it from History after a full app/sidecar restart while
retaining the same visible Practice. This was accessibility-tree and SQLite
acceptance rather than a stored fixed-viewport capture, so it adds no
screenshot, accepted-reference diff, reduced-motion result or pixel-parity
claim.

A real Debug-app inspection exercised the bounded post-Recall intervention through loopback LiteLLM/`deepseek-v4-flash`: Deep Learn showed the Agent explanation, source and pending independently scored Practice inline, and Toolbar/Activity showed the completed run. Full app/sidecar restart restored the same artifact and pending Practice through History. This is real runtime visual inspection, but no new fixed-viewport screenshot, accepted reference, visual baseline, mismatch measurement or pixel-parity result was created. Packaged Dark/reduced-motion-on/long-content, complete accessibility, Developer ID, hardened runtime and notarization limits remain open.

The follow-up Practice-primary steering pass replaced the completed artifact's three always-visible equal-weight actions with one restrained inline `Try another approach…` disclosure. The existing typed choices appear only after expansion; the pending Practice remains the document's primary work. Native Debug-app inspection caught that the first CSS version visually/accessibility-exposed children despite the HTML `hidden` attribute, added an explicit hidden-display rule, and reverified that collapsed alternatives disappear from the macOS accessibility tree and expanded alternatives return. The check clicked only disclosure expand/collapse, created no Agent run or Practice, and a final read-only database comparison reported no changes. Focused tests passed 27/27, full desktop tests 508/508, strict TypeScript and ESLint passed, and the current ad-hoc arm64 package passed lifecycle/restart smokes. No fixed screenshot artifact, accepted visual reference, packaged Dark/reduced-motion-on capture, mismatch percentage or pixel-parity result is added.

The Activity follow-up closes the raw-completion-JSON polish item without changing the learning document or adding another action. Only a strict valid `completion_published` artifact produces a flat `Learning record` with summary, source document/page/section and next-step label; explanation body, quote, metadata, hashes, identifiers and private Practice prompt stay out of this projection. The real persisted DeepSeek record was restored from History in a freshly bundled Debug app. Native inspection showed `Learning record`, `Chain rule study note.md · p. 1 · Chain rule` and the independent Practice next step, with no raw JSON in the visible or accessibility presentation and the real pending Practice still primary in Deep Learn. The inspection made no Agent choice or Practice submission; its before/after database baseline was unchanged. Focused Activity tests passed 35/35, the complete desktop suite passed 512/512, strict TypeScript and ESLint passed, and independent review found no P0–P2 issue. No stored screenshot, accepted reference, mismatch percentage or pixel-parity claim is added.

## Contextual Provider recovery visual boundary — 2026-07-25

The freshly packaged arm64 `Keen.app` was opened read-only in the host's dark appearance after its bundled-sidecar and restart smokes passed. Settings → Capabilities rendered the existing two-rail shell and one flat Provider form without clipping, horizontal overflow, a modal, a third workspace column, repeated security marketing, or a parallel navigation entry. Provider, Endpoint and Model retained the shared field geometry; keyboard inspection confirmed Endpoint → Model order and skipped the disabled `Save provider` action. No credential was entered and no setting or user learning record was changed.

Component coverage verifies the contextual Deep Learn return note, save/restart/test state machine, terminal retry, safe exact return target, stale-generation lock and Browser Demo boundary. The native app had no indexed course, persisted intervention or installed Ollama model, so the contextual post-`provider_missing` return state and a positive Agent artifact were not manufactured for a screenshot. This pass stores no new screenshot artifact, accepted reference, mismatch measurement, reduced-motion-on result, complete keyboard/a11y audit, or pixel-parity claim. Developer ID, hardened runtime and notarization remain unverified.

## Source-tree stability boundary — 2026-07-22

The final source validation only synchronized legacy migration-test expectations through the current staged `029` and `030` migrations. It made no visual, token, component, Figma, route, motion, screenshot, or packaging change. Existing packaged-current-source, Dark, reduced-motion-on, long-content, accepted-reference, and pixel-parity limits remain unchanged.

## Packaged current-source visual boundary — 2026-07-22

The current source was rebuilt into a local ad-hoc `Keen UI Audit.app` with a freshly frozen sidecar and migrations through `030`; isolated lifecycle, Review-restart, and deep signature checks passed. This run did not open or capture a product page, alter tokens/components/Figma, or establish a new visual reference. Packaged Dark, reduced-motion-on, long-content, complete keyboard traversal, accepted-reference, and pixel-parity limits remain unchanged; it is neither a Developer ID release nor a notarized `.dmg`.

## Alpha 0.1.0 RC visual boundary — 2026-07-22

The final RC closeout opened the repository-local and temporary DMG-installed app only to verify ready, Retry recovery, quit and restart lifecycle. It changed no product UI and produced no new fixed-viewport screenshot or visual baseline. Existing Alpha/DeepSeek captures remain prior real-state evidence; packaged Dark, reduced-motion-on, long-content, complete keyboard/accessibility, accepted-reference and pixel-parity limits remain open.

## Goal-owned Focused Study visual boundary — 2026-07-22

Focused Study changes submission ownership and recovery only. It reuses the existing Home New-learning composition—intent, selected indexed source, goal prompt, and one `Start focused study` action—and the existing Deep Learn route, path rail, reading column, Feed, History, and Review surfaces. A no-match or identity conflict remains inline at the prompt; a confirmed session alone navigates to Deep Learn. No page, navigation item, Figma frame, card, badge, typography, token, icon, radius, shadow, or motion pattern was added.

The focused work has component/API coverage for source selection, goal normalization, blocked and retry states, but no new safe packaged focused-session capture or accepted same-state reference was produced. It therefore adds no visual-diff, screenshot, accessibility-complete, reduced-motion-on packaged, long-content packaged, or pixel-parity claim. Existing visual limits remain unchanged.

## Exact Review task handoff visual boundary — 2026-07-22

This correction reuses the existing Feed task row, Task Detail `Review` action, flat Review document, button styles, focus treatment, and Feed return behavior. It adds no page, primary navigation item, Figma frame, card, token, typography, icon, shadow, radius, animation, or Demo review result. The only new visible states are existing Review boundary compositions: incomplete task link; task no longer available; and the same task-context boundary for an unretryable 409. They keep one `Return to Learning Feed` action and do not expose another due card or fake completed state.

Focused component coverage verifies the 404 and 409 boundary copy, scoring controls leaving the accessibility tree, and truthful Feed recovery routes. No populated packaged Review capture, accepted same-state reference, visual-diff result, reduced-motion-on capture, or pixel-parity claim was produced; existing visual limits remain unchanged.

## Adaptive remediation visual boundary — 2026-07-22

The first persisted adaptive Study Session branch uses the existing Deep Learn composition only. Incorrect Recall replaces the next protected step with an inline source review in the bounded reading column; the path rail states `Review source` then `Targeted practice`, and the review has one primary `Continue to practice` action. Correct Recall continues directly to the existing Practice surface. No new navigation item, sidebar group, card stack, Inspector, third column, badge family, font, token, shadow, radius, or motion pattern was added.

Focused component coverage verifies focus handoff, source identity/content, paused disabling, 409 reconciliation, inline loading/error Retry, strict recovery after an unknown adaptive read, and the reduced-motion-safe no-new-animation path. The current check is behavioral rather than a new visual baseline: no safe packaged live adaptive session or accepted same-state reference was captured, so no screenshot comparison, accessibility-complete result, visual-regression percentage, or pixel-parity claim is made. The existing Deep Learn missing-reference, packaged Dark/reduced-motion-on, and long-content limits remain open.

## Binding visual closeout backlog — 2026-07-22

The approved visual direction, tokens, real page contracts, and existing Figma file are frozen. Historical notes below remain evidence, but they may not reopen completed pages or expand the current scope. Work only in this order:

1. **P0 — Shell/Sidebar:** one `New learning` action; remove the duplicate New study session entry; preserve Home, Knowledge Base, Learning Feed, History, Review, and Settings; reduce the normal footer to compact status plus Settings; verify expanded/collapsed, hover, focus, active, disabled, loading, error, and compact states.
2. **P0 — Feed/Task Detail:** task list is primary; Today/Upcoming/Completed organize the default view; calendar is secondary and does not fill an empty page; selected detail shows goal, expected steps, current progress, next action, and one primary Start/Continue/Review control. A real `Later…` action may be secondary; a four-button task-management strip is prohibited.
3. **P0 — Deep Learn:** no more than two persistent columns at 1180–1280 px; path rail may collapse; the learning document stays 65–75ch; source/citation detail opens on demand; completion is an inline result with Review as the primary handoff, not a centered success card.
4. **P1 — Supporting consistency:** recheck Home without redesign, then Conversation, History, Knowledge Base, Review, and Settings for the same control geometry, 12 px auxiliary-text floor, one-primary-action rule, state vocabulary, reading width, focus treatment, and truthful operational copy.
5. **P1 — Packaged verification:** after implementation, capture only the changed/required states in the current unsigned Debug app at 1440×900, 1220×768, 1000×720, and the supported narrow reflow. Verify keyboard order, focus return, long content, Light/Dark, and reduced motion. Reuse the current harness; record `missing_reference` where applicable and do not create a new baseline system.

Visual bans for this closeout:

- No centered assistant identity, generic capability-chip cloud, chat bubbles, floating composer, nested cards, ornamental metric dashboard, empty full-month calendar, permanent Agent activity trace, or repeated local/security/provider marketing.
- No new color direction, font pairing, radius scale, shadow system, icon family, motion library, Figma file, or component library. Reuse SF Pro/system typography, the existing restrained blue accent, Lucide, current semantic tokens, 8–14 px radii, and 150–240 ms state motion.
- No page is added to the release surface because an old demo route exists. Quiz/flash cards/visual explanation remain Study or Review content types; Planner, Memory, Visualize, Drive, Canvas, Calendar writes, and DeepTutor-style Agent/platform surfaces remain deferred.

Exit this backlog when the five implementation-plan rows are verified. Do not continue polishing unrelated pages afterward.

2026-07-22 runtime UI closeout note: visual backlog items 1–4 are implemented and verified without reopening the visual direction. The Sidebar has one `New learning` action and no duplicate Study entry; Feed is task-first with an explicit Calendar view and true compact list→detail return; Deep Learn keeps two desktop columns, adds a labelled collapsible path rail, reflows to one column at 680 px, and no longer stacks prompt actions as a second sticky footer above lesson navigation. Supporting pages retain the previously converged shared controls and reading measures. Browser checks covered 1220×768, 1000×720, 760×700 and a 680×740 stress view with no horizontal document overflow; compact Task Detail restored focus to the originating task. Current screenshots are `artifacts/visual-diff/current/home.png`, `learning-feed.png`, `task-detail-figma-compact.png`, `deep-learn-figma-default.png`, `deep-learn-summary.png`, and `deep-learn-summary-compact.png`. The refreshed 26-entry report contains 16 `missing_reference`, eight comparison-only Figma results, and two expected failures against the superseded zero-tolerance Keen Home/Feed self-baselines (2.222% and 2.612%). Therefore item 5 remains open: no accepted replacement baseline, packaged WebView rerun, pixel-parity result, forced packaged reduced motion, or complete accessibility audit is claimed.

2026-07-22 packaged UI closeout note: backlog item 5 is partially verified in the current ad-hoc `Keen UI Audit.app`. Exact WKWebView measurements and captures cover 1440×900, 1220×768 and the enforced 1180×740 native minimum; the prior 1000×720 result remains an intentional browser stress check rather than a native-window claim. Current packaged Home, empty Feed, secondary Calendar and empty History have no clipping or default three-column regression, the native Tab path is continuous, Calendar keyboard close restores its opener, and supervised sidecar replacement returns the visible service state to ready. The native pass fixed a permanent loading/no-course collision, connected macOS color-scheme preference to the existing dark tokens, removed the unconditional Figma capture script, made Demo completion a clearly no-write state preview, and repaired two focus/control selectors. Captures and exact limitations are under `artifacts/ui-audit/2026-07-22/packaged-ui-closeout/`. The host remained Light with reduced motion off, so Dark and reduced-motion-on are not packaged captures; long persisted Deep Learn/Conversation content was unavailable in the isolated audit identity and was not invented. The refreshed report still has 16 missing references, eight provisional comparisons, and two failed superseded zero-tolerance self-baselines; no pixel-parity or accepted replacement-baseline claim is made.

2026-07-22 Study Session pause/resume note: Deep Learn now uses one compact shared Pause/Resume button in the existing sticky session header and resolves paused content from the persisted exact resume phase. This removes the former summary-probe guess and prevents a paused reading/recall/practice session from being painted as an opening diagnostic. Loading, confirmed, conflict, known-error and unknown-write retry states remain inline; the same idempotency key is reused after an unconfirmed result. The header keeps its flat two-column structure and changes to a vertically bounded action/status group at the compact breakpoint; shared focus, disabled/loading and reduced-motion behavior remains in force. No Figma frame, packaged WebView capture, fixed-viewport screenshot, accepted visual reference, visual diff or pixel-parity result was produced. The interaction is behaviorally verified but remains visually unverified in the packaged app.

2026-07-22 core UI polish note: the latest ad-hoc Debug `Keen Dev.app` was rebuilt and inspected at 1220×768 after a bounded React/CSS pass. Home New learning now has a flat Source row, custom single-chevron native select, unboxed document-like prompt and compact saved-task action group; ready state omits redundant form help, while truthful loading/missing-source states remain. History removes completed-row repetition and exposes saved counts instead of API window language. Saved Conversation routes show their fixed course as context rather than a disabled selector, and the completion scroll only moves when the transcript actually overflows. Task Detail uses readable stored dates and a two-line visible title bound while preserving the full accessible heading. Final native screenshots are `artifacts/ui-audit/2026-07-22/ui-polish/home-new-learning.png`, `home-source-focus.png`, `task-detail.png`, `history.png`, and `conversation.png`. Native Tab traversal verified learning intent → source → prompt and the custom focus ring. Long stored task/question/answer content is visible in the captured states; reduced-motion behavior remains covered by focused code/component checks, not a packaged preference-on capture. The 1180×740 Tauri minimum was not re-captured in this pass. No reference overlay, accepted baseline, mismatch result or pixel-parity claim is added.

2026-07-22 real DeepSeek Conversation note: the rebuilt ad-hoc Debug `Keen Dev.app` completed both a page-level Retry and a fresh Home Ask → Conversation handoff with `deepseek-v4-flash`, the indexed Calculus I source, a truthful lexical-only warning and one persisted source citation. A full app/sidecar restart restored the completed record through History. Native captures are `artifacts/ui-audit/2026-07-22/deepseek-provider/conversation-completed.png` and `home-to-conversation-completed.png`. This is real runtime evidence for the existing Conversation/Home visual states, not a new design direction, accepted baseline, fixed-viewport visual diff or pixel-parity result. Vector retrieval remains unavailable, the build is ad-hoc/unsigned and unnotarized, and packaged reduced-motion-on was not forced.

2026-07-22 continuity closeout note: the global New learning → Home Ask, indexed Knowledge Base → Home handoff, and Deep Learn → Feed/History `course_id` continuity slices are product/route repairs only. They add no visual redesign, Figma update, packaged Tauri WebView capture, accepted baseline, comparison result or pixel-parity evidence. Existing Provider/citation, Apple Developer ID and packaged reduced-motion limitations remain unchanged.

2026-07-22 Home two-state note: the canonical Figma Home page now includes `Home / Saved work / 1180×740` (`196:328`) and `Home / New learning / 1180×740` (`197:351`). Current React captures are `artifacts/ui-audit/home-saved-work-2026-07-22.png`, `home-new-learning-2026-07-22.png`, and `home-new-learning-compact-2026-07-22.png`; the compact capture uses a 760×700 viewport and all three emulate reduced motion. Saved work and the full composer are no longer shown simultaneously, long task copy has a bounded title/meta hierarchy, and the new-learning form follows intent → source → prompt → specific action. The request-free Demo omits the live source selector because no indexed service source exists in that state. A rebuilt unsigned Debug app with the healthy bundled core was then inspected at 1220×768: saved work, exact persisted-task continuation, New learning with its live source selector, and native Tab traversal through intent → source → prompt are captured under `artifacts/ui-audit/2026-07-22/home-packaged/`. Tauri enforces a 1180×740 minimum, so the 760×700 check remains browser-only; packaged reduced-motion-on was not forced. Figma's Sidebar is older than runtime and its frames are Home-content contracts rather than whole-shell references. None of these captures is an accepted visual baseline or pixel-parity evidence.

2026-07-21 History persisted Conversations note: this slice reuses the accepted History shell, flat list rows, focus tokens and motion contract; it adds a separate Questions section beside Study sessions without changing Figma, navigation or the visual system. Questions and Sessions keep independent local loading/error/empty states, one row action and learner-facing status/scope labels. The rebuilt Debug app screenshot `artifacts/ui-audit/2026-07-21/history-conversations/01-history-unavailable-real-dev-app.png` was opened and visually checked against the current flat foundation, but the bundled learning core reached only the truthful unavailable state. Healthy native Questions rows, authoritative open/reload and restored citations remain component/API evidence rather than live-app visual evidence; no new baseline, Figma approval, pixel comparison, provider success or citation claim was added.

2026-07-21 Home Ask durable handoff note: this slice reuses the approved Home workbench and accepted Conversation reading surface; it changes submission ownership and persistence handoff only, with no Figma, token or layout change. The latest Debug `Keen Dev.app` build rendered Home but reached only the truthful learning-core-unavailable state, so no new screenshot or healthy native visual claim was added. Component tests cover the explicit course/all-indexed scope and one-shot handoff; normal native create/stream/reload remains visually unverified.

2026-07-21 durable Conversation note: this slice changes persistence, authenticated transport and restore behavior without changing the accepted Conversation layout, tokens, Figma state or visual reference. The latest Debug `Keen Dev.app` bundle built successfully but reached only the truthful learning-core-unavailable state, so no new native normal-state screenshot or visual acceptance claim was added. Restored/completed/cancelled/failed/interrupted rendering is covered by the existing document-style component surface; native healthy persistence and restart remain visually unverified.

2026-07-21 core-loop handoff note: current Browser captures at `artifacts/ui-audit/2026-07-21/core-loop-handoff/01-conversation-demo-1180x740.png`, `02-conversation-demo-900x700.png`, and visually checked `06-conversation-demo-900x700-collapsed-disclosure-fixed.png` confirm the frozen shell's quiet reading axis, one visible compact global Demo disclosure even after the persisted Sidebar collapse, one main landmark, zero unnamed buttons and no document horizontal overflow in the inspected Conversation states. The authorized HyperKnow citation reference and Keen sample-answer capture are different states, so the comparison remains qualitative and no pixel result is claimed. The fresh Debug capture `03-home-unavailable-real-dev-app.png` confirms current frontend launch/render only; its healthy normal sidecar state was not reproduced. Do not treat this as a new baseline or an accepted real-provider state.

2026-07-21 History + Review foundation note: History now uses a flat, single-action Study Session list and Review a bounded 65–75ch prompt/reveal/rating column using the existing Shell, field, focus and motion tokens. Browser evidence under `artifacts/ui-audit/2026-07-21/history-review-foundation/` covers request-free empty states at 1180×740, Review reflow at 900×700 and a 590×700 compact stress check with no document overflow or unnamed buttons. The same directory contains latest Debug-app History/Review unavailable captures plus qualitative reference/current composites. Because those composites compare different states, and the Dev.app did not reach populated History or due Review, visual acceptance for the implemented normal states remains blocked. Figma was not changed; no baseline, mismatch threshold, complete keyboard/a11y audit or pixel-parity claim was added.

2026-07-21 Conversation foundation note: Conversation now follows the authorized long-answer structure as a Keen-owned flat document rather than a generic chat surface. The learner question, answer, warnings, citations and continuation share one bounded reading axis; only a real scoped citation opens the existing PDF viewer. Browser evidence under `artifacts/ui-audit/2026-07-21/conversation-foundation/` covers empty, request-free sample long content, 900×700 reflow and a qualitative HyperKnow/current side-by-side. The same directory contains a latest Debug-app startup/recovery capture; a healthy provider run was not reproduced visually. The browser document had no horizontal overflow or unnamed buttons, and shared focus/reduced-motion contracts remain active. Figma was not changed because the existing canonical direction already covers this structure; Conversation remains unapproved and has no accepted mismatch threshold or pixel-parity claim.

2026-07-21 Learning Feed + Task Detail note: the Feed retains the Approved/HyperKnow master-detail organization while removing the old explanatory intro, repeated Demo disclosures, rationale cards and badge-heavy task rows. At 1180×740 the task rail measured 332 px and the calendar remained primary; at 900×700 it reflowed below the queue without horizontal overflow. Task Detail preserves the rail, focuses its heading on entry, returns focus to the original row and exposes one existing next action. Browser captures and qualitative side-by-sides are under `artifacts/ui-audit/2026-07-21/feed-task-detail/`. The latest real Debug app capture in that directory shows the truthful unavailable state; healthy normal Feed and Task Detail were not reproduced in the real app. Reduced-motion CSS remains in force but packaged reduced motion was not forced, and no approved baseline, mismatch threshold or pixel-parity claim was added.

2026-07-21 Home task-first note: Home now puts a real non-empty learning queue before the request workbench and promotes the workbench only for an empty queue or explicit Ask/Study/Review entry, with matching DOM/heading order. Real Debug-app restore and unavailable captures are `artifacts/ui-audit/2026-07-21/home-task-first/01-restore-real-dev-app.png` and `02-error-real-dev-app.png`; both were opened and checked against the flat Approved Home organization. The app did not reach a healthy normal queue in this turn. Normal-task and empty ordering are therefore component/integration evidence only, not real Dev.app visual verification. No new Figma frame, baseline, compact-window result, reduced-motion capture, or pixel-parity claim was added.

2026-07-21 UI Foundation + Deep Learn pilot note: the existing Shell/token/shared-control layers and the existing real Study Session flow were reused. Deep Learn now holds a stable 204 px path rail, centered 65–75ch reading column, flat source/checkpoint separators, geometry-preserving restore skeleton and one quiet bottom/current action vocabulary across diagnostic, reading, recall, practice and completion. The old illustrative mastery aside and synthetic protected-state Inspectors were removed; a real selected document still opens the global Inspector, while pending recall clears it. A follow-up pass removed repeated phase titles and hidden/protected rail copy, restored the session goal as the page-header context, kept normal ready status only in the Sidebar, and fixed the completed Review time to an English no-seconds format. The two new real-session captures are under `artifacts/ui-audit/2026-07-21/deep-learn-polish/`. The canonical Figma file was not changed by this follow-up: frame `65:2`, `Shell + Deep Learn · In Review / Provisional · 1180×740`, remains Provisional. Real Debug `Keen Dev.app` captures are under `artifacts/ui-audit/2026-07-21/ui-foundation-deep-learn/`: cold restore, persisted reading, pending active recall, flat completion and real Inspector-present. The minimum-window captures are 1192×768 image pixels and the fullscreen Inspector capture is 1228×768; they are not asserted as exact CSS viewport measurements. Because Tauri blocks 820×700, only current React/CSS was checked there: no horizontal overflow, 60 px collapsed Sidebar, 184 px rail and 576 px content region. HyperKnow comparison remains qualitative, the reading fixture is short rather than a deterministic long article, packaged reduced-motion-on was not forced, and no Deep Learn baseline or pixel-parity result is accepted.

2026-07-21 practice/Review handoff note: the rebuilt local `Keen Dev.app` was operated through the same persisted session's deterministic practice, completed Summary, due Review reveal/rating, Learning Feed return and full app/sidecar restart. The live completion view exposed an unexplained `100% session` / `50% units` pairing plus no next CTA. It now states `1 of 2 planned units completed · focused session closed`, keeps the finished session lifecycle at 100%, describes the original Review handoff without claiming its frozen due snapshot is the current FSRS schedule, offers shared-button actions to Review and Learning Feed, and removes the protected Summary Inspector when the route changes. Review then truthfully showed the post-rating empty queue and Feed showed no currently eligible action. The native Dev.app UI was inspected directly and Impeccable detection found no changed-file issue, but no fixed CSS viewport, stored screenshot artifact, accepted Figma/HyperKnow reference, visual diff, packaged reduced-motion measurement or complete keyboard/accessibility audit was added. Deep Learn completion/Summary and Review still lack accepted visual baselines; no pixel-parity claim is made.

2026-07-22 multi-unit progression note: this slice reuses the existing Deep Learn rail, editorial column, checkpoint card, buttons, focus treatment and motion rules; it does not select a new visual direction or add a Figma frame. Unit status is now truthful in that rail: the persisted current unit owns the active step, completed/skipped units are read-only, and ready/locked units are disabled and cannot display source content. After an intermediate practice answer, the existing checkpoint surface shows `Unit complete` and one `Continue to next unit` action before the authoritative Unit 2 reload; only the final unit enters the existing Summary/Review surface. Component tests cover keyboard exclusion for locked native buttons, completed-unit read-only inspection, fail-closed missing/forged pointers, Unit 2 reload and cross-unit late-response isolation. No packaged WebView capture, fixed viewport, reduced-motion rerun, accepted visual reference, visual diff or pixel-parity result was produced, so the existing Deep Learn visual TODOs remain unchanged apart from removing multi-unit progression as a missing functional state.

2026-07-21 live learning-loop note: no visual direction or accepted baseline changed. An unsigned local `Keen Dev.app` with the real sidecar and isolated dev SQLite completed Home focused-study entry, diagnostic, source reading, one active-recall answer, History return and full-application restart/resume. The restored session retained its `practicing` state and recorded recall. The only product repair was backend task admission for an explicit learner-goal request when the recommendation is already covered by an actionable local task; autonomous task-only starts remain provenance-gated. Existing 1180×740 and 820×700 History browser evidence remains applicable because no React/CSS changed. Packaged reduced motion, long-content capture, a signed build, accepted History/Deep Learn baselines, complete keyboard/accessibility coverage and pixel parity remain open.

2026-07-21 History slice note: the new persisted-session return page was inspected in Browser Demo at 1180×740 and 820×700. Both states had one `main`, no document overflow and no unnamed buttons; the compact Sidebar selected History correctly, and the empty-state action opened the existing focused-study request. Browser Demo intentionally contains no fabricated History rows. The populated course-scoped row, progress, status and Continue behavior are component-tested against the new read contract, but no accepted History visual reference, mismatch percentage, live packaged capture or pixel-parity claim exists.

2026-07-21 guided-study entry note: Home now offers a live course-source selector only for the existing Start focused study path, then reuses the persisted recommendation/session and Deep Learn surfaces. This implementation added no screenshot, Figma export, visual-regression result, baseline approval or pixel-parity claim. Its focused evidence is behavioral (source-scope integration 8/8 and autonomous-session API 20/20); compact layout, visual comparison and packaged behavior for this new live state remain unverified.

## Current gate

At the initial 2026-07-15 audit, `references/` did not exist. The canonical authorization scope is now recorded at `references/authorized-scope.md`. Three authorized internal-research HyperKnow screenshots were captured on 2026-07-18 and are inventoried in `references/hyperknow/2026-07-18/README.md`: Home composer, Learning Feed/calendar, and the Deep Learn entry selection. They cover only those visible states and are not approved for redistribution. No page can currently be described as pixel-matched, and no `<3%` mismatch result has been computed.

The product may proceed with the brief's provisional tokens and non-branded placeholders. Those values must remain identifiable as provisional until measured from authorized references.

2026-07-23 Agent-native research note: `docs/AGENT_NATIVE_USER_RESEARCH.md` adds no new visual direction or accepted reference. It narrows the future Deep Learn intervention to one flat inline state in the existing reading column: a compact `why now / source scope / then` explanation, one policy-selected learner-activated action, capability-bounded source disclosure with partial/unsupported limits visible, one ready-state primary action, and a quiet closed steering menu. Agent avatars, floating chat, a third persistent column, modal warnings, equal-weight action chips, bulk-generation screens and Agent navigation remain rejected. These are planned page contracts only; no React state, Figma frame, screenshot, accessibility result, visual diff or packaged evidence was created.

2026-07-24 Agent-native shell implementation note: the runtime shell now uses a compact canonical-brand Sidebar, a restrained toolbar Agent-status control and one 320 px on-demand context drawer with Activity, Sources and Outline tabs. Deep Learn retains two persistent columns; its Agent entry is secondary, contextual and hidden whenever protected learning phases make it unsafe or misleading. Route changes render a restoring state instead of exposing actions from the previous workspace. The visual structure is a Keen-owned adaptation of the reviewed DeepTutor and authorized HyperKnow patterns; no third-party code, CSS, marks, screenshots or copy was shipped. Strict TypeScript, targeted ESLint and whitespace checks pass, but the current source could not be rendered through Vite or the in-app browser because this host's native Node/Rollup/browser modules hang or fail code-signature loading. There is therefore no current screenshot, keyboard/browser pass, visual diff, packaged WebView result, accepted baseline or pixel-parity claim for this implementation yet.

2026-07-21 UI-framework closeout note: `docs/DESIGN_DIRECTION.md` freezes the current task/source/progress workspace and makes Figma pages explicitly `Approved`, `Provisional` or `Reference`, preventing a new competitor screenshot from silently replacing product structure. Home and Learning Feed were compared qualitatively against Approved Figma nodes `113:3` and `162:97`; Knowledge Base was checked against the authorized HyperKnow source-list reference while retaining Keen's real indexing/recovery fields. Current Browser Demo captures at 1440×920, 1180×740 and 900×700 are under `artifacts/ui-audit/2026-07-21/ui-framework-closeout/`. The pass fixed the missing expand action after a manual Sidebar collapse and added a regression. Inspected routes had no document-level horizontal overflow; Feed hides the calendar at 900 px, Knowledge Base becomes a list, Conversation keeps a bounded reading measure and independent transcript scrolling, and reduced-motion emulation measured 0.00001 s durations with automatic scrolling. The visible focus ring and DOM focus order were inspected, but direct browser Tab injection remained unreliable, so this is not a complete keyboard or accessibility audit. Approved Figma copy was updated to the truthful Demo wording, although no fixed-viewport accepted baseline or mismatch threshold was established. A fresh local ad-hoc packaged `Keen.app` was subsequently inspected: Home, Feed, Knowledge Base, empty Conversation and the no-course Study entry rendered with the service ready; native focus reached Review due; and the Sidebar recovered after collapse. A deliberate termination of its current sidecar child was followed by a supervisor-created replacement and a ready UI; the transient unavailable UI was not captured. Its 1203×768 capture images did not themselves record CSS dimensions. An isolated build-time audit bundle later measured the packaged WKWebView directly at 1440×920 CSS px and at the enforced 1180×740 native minimum, with `inner`, root-client and `visualViewport` dimensions equal and DPR 2. A below-minimum resize attempt remained at 1180×740. The current packaged query was `reduced=false`; packaged `reduced=true`, long-learning-content and transient unavailable UI remain unverified because the System Settings control channel closed before the preference could be read or changed and no corresponding packaged state was captured. Because a separate installed old app can share the normal `Keen` name/identity, audit starts now use the fixed-path, Info.plist-validating `scripts/open-ui-audit-app.sh`; the audit's `Keen UI Audit` menu text is config-derived and its identity is locked apart from unchanged production `Keen`. The packaged captures and candidate-baseline matrix are under `artifacts/ui-audit/2026-07-21/packaged-webview/`. All visual candidates remain rejected because their fresh-data states lack an aligned deterministic fixture/reference and packaged reduced-motion evidence; the probe does not retroactively turn the earlier route captures into accepted baselines. The deterministic browser harness wrote `artifacts/visual-diff/report.json` with 16 `missing_reference` items and eight `provisional` Figma comparisons. No pixel-parity claim is made.

2026-07-21 Keen-owned baseline note: only Browser Demo Home and Learning Feed are now accepted as regression inputs, not design parity: each is a 1180×740 CSS px / DPR 1 PNG captured with `visualTest=true` and reduced motion, and each has a strict 0% current-to-self check. The Browser Demo fixture has fixed July 2026 content and emitted no application fetch/XHR, Tauri, learning-core or provider request. Fresh natural-size exports of Approved Figma Home `155:187` and Feed `164:179` match the current PNG dimensions; their measured comparisons are still provisional (Home 2.0290884104443423%, Feed 1.5566880439761797%), with intentional product adaptations documented rather than thresholded away. `references/visual-baselines/README.md` records fixture, viewport, scale, motion, hash and Figma provenance. The 26-entry report has 2 accepted baseline passes, 16 `missing_reference` and 8 `provisional` comparisons. Knowledge Base, Conversation, Deep Learn and every other page/state remain missing or provisional. No pixel-parity claim is made.

2026-07-21 Knowledge Base + Source Scope note: the current Browser Demo captures under `artifacts/ui-audit/2026-07-21/knowledge-source-scope/` cover flat default/selected source lists, geometry-matched loading, unavailable and 900×700 reflow. A qualitative same-size comparison against authorized HyperKnow `07-knowledge-base-list.png` confirms the shared quiet workspace and source-first hierarchy but also the intentional difference: Keen uses a dense status/course list instead of document thumbnails because its real local handlers expose operational source state. The rebuilt Debug app captured only source unavailable; healthy live indexing/progress/failed actions remain component/integration evidence, not accepted visual evidence. Knowledge Base still has no accepted baseline or pixel-parity claim.

2026-07-21 icon-density and boundary note: the user-reported Learner Memory crop was treated as negative evidence for repeated decorative brain glyphs, not as a design to reproduce. The current route is a flat capability boundary with no seeded profile, mastery/misconception rows, search/filter controls, edit/enable/delete icons or disabled settings action. The new-request route now starts empty, the global Inspector has no synthetic fallback content, and Settings consolidates provider/integrations into Capabilities with plain status rows. Current 1280×800 and 900×700 screenshots are under `artifacts/ui-audit/2026-07-21/icon-reduction/`. Browser checks found one `main`, no horizontal overflow and no unnamed buttons on Memory, new Conversation and Settings; direct Tab injection remained unreliable, so component keyboard tests—not a complete browser keyboard audit—are the current evidence. The visual harness completed with 16 missing references and eight provisional comparisons. No accepted pixel match or packaged-app result is claimed.

2026-07-20 Study Planner note: the direct UI-demo route no longer renders a fixed July schedule, invented weekly totals, in-memory completion, disabled import/add/navigation/rationale controls or a calendar confirmation preview. It now presents an explicit not-implemented boundary, one working Learning Feed alternative, and non-interactive prerequisites for future course inputs, durable plan state and Level 3 calendar handoff. Screenshots at 1280×800 and 760×700 are under `artifacts/ui-audit/2026-07-20/planner/`; both inspected sizes had one `main`, no horizontal overflow, no disabled controls and no dialog. The complete desktop suite passed 299/299. The visual harness captures `planner-boundary` but reports `missing_reference`, so no accepted match, packaged-app result or complete accessibility audit is claimed.

2026-07-20 Visualize note: the direct UI-demo route is no longer a fake generation builder. It now contains one fixed, accessible eigenvector SVG plus concise availability boundaries; prompt editing, output formats, fake progress/success, disabled source/export/motion controls and the future renderer card were removed. Current 1280×800 and 820×720 screenshots are under `artifacts/ui-audit/2026-07-20/visualize/`. Browser inspection found one `main`, zero horizontal overflow, zero unnamed buttons and zero disabled controls; the compact layout reflows its notes below the figure and scrolls only inside the page viewport. The complete desktop suite passed 298/298. The visual harness now captures `visualize-demo` at 1280×800 but reports `missing_reference`, so no accepted visual match, packaged-app result or complete accessibility audit is claimed.

2026-07-20 command-surface note: the command palette keeps implemented primary navigation and request/session entry actions. Review is now included as a real local FSRS workflow; UI-only Quiz, Planner, Learner Memory and Visualize remain absent from discovery and visibly bounded when opened directly. Earlier command-palette screenshots predate this Review entry and must be refreshed. No accepted command-palette reference exists, so no pixel-parity claim is made.

2026-07-20 Review note: the bundled Flashcards deck and sample intervals were removed. `/review` now renders the authenticated SQLite/FSRS queue in the desktop runtime and a request-free capability boundary in Browser Demo. Browser captures at 1280×800 and 760×700 found one `main`, zero horizontal overflow and zero unnamed buttons. A 760×700 local-startup fixture was checked with `prefers-reduced-motion: reduce`; both skeleton animations resolved to `none`. Current captures are under `artifacts/ui-audit/2026-07-20/review/` and `artifacts/visual-diff/current/`. Both configured Review references are missing, so these are current-product screenshots only and establish neither accepted parity nor complete accessibility coverage.

2026-07-20 shared-component note: runtime Button, IconButton, Badge, Progress and field styles now have one canonical implementation instead of base-plus-refresh duplication. Button covers compatible secondary/primary/danger/ghost, 30/34 px sizing and accessible loading; IconButton defaults to `type="button"`; Badge uses semantic Light/Dark surfaces; and `--warning-soft` is no longer Figma-only. `docs/figma/COMPONENT_RUNTIME_SPEC.md` records the exact runtime contract. Cross-page inspection at 1280×720 found consistent visible Badge and Button geometry, one `main`, no horizontal overflow and no unnamed buttons on Home, Feed, Knowledge Base, Conversation and Settings. New component tests and the complete 295-test desktop suite passed. Current Figma comparisons remain provisional and several page references remain missing, so this does not establish accepted visual parity.

2026-07-19 brand-asset note: the user-provided 204×228 mark and 386×178 wordmark references were background-separated and vector-traced into transparent light/reverse SVG variants under `apps/desktop/public/brand/`. A light/dark inspection sheet is stored at `artifacts/brand/2026-07-19/brand-assets-proof.png`, and the integrated 1280×720 Browser Demo check is `artifacts/brand/2026-07-19/sidebar-brand-1280x720.png`. The Sidebar and browser favicon now use these vectors, eliminating the cropped white raster tile. Browser inspection measured the rendered mark at about 22×25 px and the wordmark at 13 px high, confirmed the collapsed mark remains centered, and found no horizontal document overflow. The trace preserves the supplied silhouettes and normalizes compression texture to stable dark/teal fills; it is not an original vector master, a pixel-parity result, or sufficient packaging-icon evidence. The Tauri `.app`/`.dmg` icon set remains unchanged.

2026-07-23 packaged-icon source note: the user selected the flat mark over the glossy rendering. The selected silhouette was first redrawn as a clean three-path SVG with dark `#161E25` and teal `#11ADA2` fills; `apps/desktop/src-tauri/icons/icon.svg` places it on a quiet rounded-square field, and Tauri regenerated the checked-in platform icon outputs from that source. Direct inspection covered the 512 px and 32 px PNG outputs; a fresh packaged `.app`/`.dmg`, Dock capture and signed/notarized result were not produced, so packaged appearance remains unverified.

2026-07-18 focused UI-audit note: current Browser Demo captures for Home, Learning Feed, Deep Learn and its illustrative Summary are stored in `artifacts/ui-audit/2026-07-18/`. The audit found and corrected minimum-content-width Deep Learn header/grid overflow, the misleading Demo Summary recall card, non-semantic selected states, ineffective Demo Pause controls, stale cross-unit answer state and a wrapping Learning Feed rationale label. These are current-product before/after captures, not accepted HyperKnow references. The in-app audit viewport was narrower than Keen's supported macOS window minimum, so it is useful for overflow diagnosis but does not replace the still-missing fixed 1180×740/1440×920 app captures, accessibility scan or reference-backed visual diff. No parity claim is made.

2026-07-18 HyperKnow benchmark note: the accepted internal references showed a full-width primary workspace and a Deep Learn entry implemented as a composer tool selection rather than an immediately created session. At that date Keen defaulted the Inspector closed, exposed `New study session` in the Sidebar, and opened Home with the Study intent, mode-specific heading and prompt without creating a session. The duplicate Sidebar entry is now superseded by the binding 2026-07-22 one-entry contract above. Current Keen before/after captures and a side-by-side structural comparison are stored under `artifacts/ui-audit/2026-07-18/hyperknow-benchmark/`. The comparison is qualitative only: viewports/crops differ, no fixed seed or pixel diff was run, and no parity claim is made.

2026-07-18 authorized implementation note: Keen now applies the three captured HyperKnow structures to its own branded UI. Home uses a centered composer and lower task list; Learning Feed uses a task rail plus month calendar; the Deep Learn entry is a selected composer mode. Current 1440×920 captures are under `artifacts/visual-diff/current/`, and 1180×740 Home/Feed/Conversation checks are under `artifacts/ui-audit/2026-07-18/keen-ui-refresh/`. Focused DOM/keyboard/reduced-motion checks found no horizontal page overflow, duplicate IDs or unnamed focusable controls on five primary states at 1180×740. This is not a complete accessibility audit. The harness reports three `missing_reference` results because the authorized images and current Keen views do not form dimension-aligned accepted baselines; no mismatch percentage or pixel-parity claim is made.

2026-07-18 second-polish note: targeted Home and Learning Feed surfaces now use semantic light/dark canvas tokens, and the unset/system theme follows macOS dark appearance. At 1180×740 the Home first viewport contains all three learning rows, while Feed calendar events can reveal and select their matching existing task without creating or changing a calendar record. Light/dark screenshots are under `artifacts/ui-audit/2026-07-18/keen-ui-refresh-2/`. Focused Browser inspection found no horizontal document overflow, duplicate IDs, unnamed focusables or console errors on those states; reduced motion measured `0.00001s`. Tested dark text/semantic tokens are at least 5.42:1 against the primary dark surfaces. Browser Tab-key injection was unavailable in this control session, so existing Playwright keyboard evidence remains the current keyboard result and this is still not a complete accessibility audit. The fixed 1440×920 harness still reports three `missing_reference` results, not a visual pass.

2026-07-18 task-first refinement note: user review found that the centered composer still read as a generic AI assistant. Home now defaults to a `Today` workspace with the learning queue and five plain study tools; the input workbench appears only after an action is chosen, and idle Agent response, mascot, prompt cards and nonfunctional media controls no longer occupy the default state. Learning Feed uses schedule/task terminology and no sparkle icon. Current 1180×740 screenshots are under `artifacts/ui-audit/2026-07-18/keen-ui-workspace/`. Focused Browser inspection found zero default Home textboxes, all three sample tasks and five study tools visible, and no horizontal overflow, duplicate IDs, unnamed focusables or console errors on the inspected Home/Feed states. The existing behaviors and capability boundaries are unchanged. This intentionally departs from the authorized HyperKnow centered-composer reference after user feedback; the visual harness still reports three `missing_reference` states, and no parity, packaged-app or complete accessibility claim is made.

2026-07-18 task-detail refinement note: Learning Feed now uses the calendar area as an inline task-detail workspace after selection, keeping task inspection and the next real action in one bounded surface. It shows only existing Demo/authenticated/autonomous fields; unit progress is not invented before a session plan exists. Demo mutations remain browser-session-only, authenticated fallback records are read-only, and persisted Start/Resume retains its existing confirmation and recovery states. Screenshots at 1000×720, 1180×740 and 1440×920 are under `artifacts/ui-audit/2026-07-18/keen-task-detail/`. The compact-window capture exposed and then verified the fix for a footer that fell below the viewport and compressed its explanatory copy. Focused Browser inspection found no unnamed buttons, unlabeled fields or duplicate detail-title IDs. Entry/return focus behavior is component-tested; Browser Tab injection did not move focus reliably, so this is not a complete keyboard or accessibility audit. The harness still reports three `missing_reference` results, and no parity or packaged-app claim is made.

2026-07-18 style-return note: a user-supplied 1224×392 composite selected the earlier light Keen desktop language over the later enterprise-dashboard experiment. The experiment was uncommitted and removed; current product behavior was not rolled back. Home and Feed were recaptured at 1180×740 under `artifacts/ui-audit/2026-07-18/style-return/`, and a full qualitative comparison plus focused Feed crop is recorded in `design-qa.md` with `final result: passed`. The source is downscaled and not viewport-aligned, so this is style-direction evidence only, not a visual baseline or pixel-parity result. The fixed harness still returns three `missing_reference` states.

2026-07-19 HyperKnow-layout return note: the user selected the centered request workbench and rail-plus-calendar composition again. Keen now applies those structures using only the existing Ask/Study modes, sample/local evidence labels and validated task data; HyperKnow branding, companion copy, news, generic tools and service-success claims were not copied. Home and Feed captures at 1462×907, 1180×760 and 900×700, plus normalized qualitative comparisons, are under `artifacts/ui-audit/2026-07-19/`. Focused checks found a visible keyboard focus ring, no horizontal clipping in the inspected breakpoints and no browser console errors. `design-qa.md` records the review with `final result: passed`. The fixed 1440×920 harness still reports three `missing_reference` results, so this is not a pixel-parity or `<3%` visual-regression pass.

2026-07-19 Figma study note: the authorized Home and Learning Feed references were reconstructed as local HTML for internal comparison and converted into editable raw Figma frames in `Keen Desktop UI — HyperKnow Layout Study` (`ugwiIPdF43v2woYsLM3b9R`, nodes `7:2` and `8:2`). A separate product-first Keen proposal removes the default centered assistant prompt, flattens task/calendar surfaces and makes availability boundaries part of the workspace; its editable raw frames are nodes `9:2` and `10:2`, with source HTML/CSS under `artifacts/figma/hyperknow-html-reference/`. Side-by-side PNG comparisons remain under `artifacts/figma/comparisons/`. These frames are layout studies, not component-library instances, shipped assets, accepted baselines or pixel-parity evidence. The current Figma file still needs native token collections, component variants, interaction states, motion prototypes and fixed-viewport visual comparison before implementation selection.

2026-07-19 Figma checkpoint audit note: nodes `7:2`, `8:2`, `9:2` and `10:2` were re-rendered at 1728×907 and inspected side by side under `artifacts/ui-audit/2026-07-19/figma-check/`. The task-first proposal materially reduces the generic assistant feel, but it is not implementation-ready: metadata contains zero components and zero instances, the HTML study has no explicit focus-visible state, several small-text colors remain below 4.5:1, the screens are fixed-width, dark mode is absent, and the requested state/motion matrix is not represented. The detailed audit scores this bounded study 11/20 and records its evidence limits. No pixel parity, runtime accessibility, responsive, motion or packaged-app claim is made.

2026-07-19 Figma foundations note: the same file now has a separated design-system page structure, 99 Keen variables across five collections, Light/Dark semantic aliases, nine text styles, six effect styles, and visual Color/Type/Layout documentation. The three new variables are Light/Dark warning-soft primitives plus their semantic alias. The user-provided app icon and wordmark remain on the Cover; the nine Figma text styles now use the verified native SF Pro family while production keeps the macOS system font stack. Verified captures are under `artifacts/ui-audit/2026-07-19/figma-foundations/`. Component pages now cover Button `33:2`, IconButton `45:30`, Badge `52:12` and Service Banner `59:32`; their direct renders and evidence notes are under `artifacts/ui-audit/2026-07-19/figma-components/`. Badge is intentionally informational only. Service Banner includes only the real non-healthy startup/failure groups and documents that healthy remains compact and the shared React export does not yet exist. A same-size Button snapshot comparison after responsive-label repair changed 7,185 pixels (0.697%); it is local Figma regression evidence only, not an accepted product reference. Component geometry/state normalization, interaction prototypes, Sidebar/Nav Row, full page-state coverage and accepted visual-regression baselines remain open. These captures are not runtime accessibility, responsive, motion, packaging or pixel-parity evidence.

2026-07-19 HyperKnow → Figma Phase-0 note: the user's authenticated 1585×851 Chrome state was inspected through Home, tool selection, Month/Week Learning Feed, quota, Knowledge Base upload/processing/completion/hover/menu/calendar-extraction, account/settings, Deep Learn long reading and a synthetic cited review-plan conversation. Twenty-one sanitized captures and measured visible style samples are inventoried in `references/hyperknow/2026-07-19-figma-screen-audit/README.md`. One full Settings capture containing account identity and one blurred-Sidebar overlay capture were removed from the retained set. The Figma file and React UI were not changed in this phase. Before page reconstruction, Phase 1 must reconcile the Figma Inter proxy with the production macOS system stack, resolve the 36/12 versus 34/14 Button geometry gap, create Sidebar/Nav Row, segmented controls, menus/popovers, task/path rows, progress, file tiles, citation panel and bottom request bar as local components, and cover Default/Hover/Focus/Active/Disabled/Loading/Error/Success plus reduced-motion behavior. No screen is accepted as a fixed baseline and no pixel-parity claim is made.

2026-07-19 HyperKnow → Figma Phase-1 foundations note: the five existing Keen collections were retained and validated rather than duplicated: 54 primitive, 27 semantic color, 8 spacing, 5 radius and 5 dimension variables. All semantic Light/Dark aliases resolve, numeric values match `packages/design-tokens/src/tokens.css`, and the existing `warning-soft` variable remains Figma-only because no matching CSS token exists. Nine text styles now use verified native SF Pro Regular/Medium/Semibold styles; Button, Badge and Service Banner instances inherit them. Six effect styles retain the current CSS shadow/focus values with clarified usage descriptions. Visual QA found and repaired one malformed Typography eyebrow by replacing it with the verified one-line Foundation header node (`76:2`). Fresh Color `16:4`, Type `16:5` and Layout `16:6` renders are inventoried under `references/keen/2026-07-19-figma-phase1/`. No React component, runtime behavior, responsive behavior or accessibility result changed in this phase. Component construction and page reconstruction remain open, and these renders are not accepted references or pixel-parity evidence.

2026-07-19 HyperKnow → Figma Phase-2 component/motion note: the same file now includes token-bound Nav Row `80:56`, Progress `89:46`, Segmented Control building blocks `91:54` and composite set `92:71`, Motion documentation `98:3`, and Status Indicator `104:56`. Nav Row hover/click reactions and one 220 ms Progress width track were read back through the Plugin API; final direct renders are under `references/keen/2026-07-19-figma-phase2/`. The available Figma tools did not expose video export, so no frame-sampled motion claim exists. The Segmented Control composite uses local segment layers because automated nested text overrides were blocked by Figma font availability. Screen reconstruction, runtime implementation, supported-window/dark-mode coverage, keyboard/accessibility validation and accepted visual baselines remain open. Data/code/Figma precedence and known conflicts are now explicit in `docs/DESIGN_RUNTIME_CONFLICTS.md`; component status labels remain design vocabulary rather than service-success evidence.

2026-07-20 HyperKnow → Figma Phase-3 Home note: Home Screen page `113:2` now contains a componentized default Browser Demo `113:3`, learning-core offline `130:52`, and provider-not-configured `131:195` at 1585×907. Queue Row set `121:26` adds Default, Hover and Focus variants; the screens reuse the existing Nav Row, Badge, Button and icon components. The exact data and capability mapping is recorded in `docs/figma/HOME_DATA_MAPPING.md`. Final renders and a detailed evidence ledger are under `references/keen/2026-07-20-figma-phase3/`. Plugin API QA found only SF Pro visible text, no bad text geometry or temporary lorem/shimmer artifacts, and no completion/provider-success claims. Visual QA removed redundant operational badges after the Figma SF Pro overlay path produced stale or wrapped glyphs. React implementation, collapsed Sidebar, compact 1180×740 layout, dark mode, runtime keyboard/accessibility/reduced-motion testing and accepted same-viewport visual baselines remain open. Code Connect was unavailable in the current Figma seat/tool context; no mismatch percentage or pixel-parity claim is made.

2026-07-20 Phase-3 screenshot-correction note: fresh 1585×907 Figma exports confirmed that the prior Home screenshots contained measurable defects: every `⌘N` shortcut exceeded its clipped control by 1 px, the default footer text retained a 9 px stale-glyph overflow, operational copy floated away from the state it described, and offline/provider explanations were duplicated inside unnecessarily tall panels. Frames `113:3`, `130:52`, and `131:195` were corrected in Figma without changing React. The final structural pass found zero visible text nodes outside clipping ancestors and zero non-positive visible text geometry. Before/after captures are under `references/keen/2026-07-20-figma-phase3-screenshot-audit/`, and the canonical Phase-3 PNGs were refreshed. Compact/dark/collapsed/runtime checks and accepted visual baselines remain open; no pixel-parity claim is made.

2026-07-19 aesthetic audit note: fresh current-run Browser Demo captures at 1440×900, 1180×760 and 900×700 are stored under `artifacts/ui-audit/2026-07-19/final-aesthetic-audit/`. No actual element overlap or horizontal document overflow was found. The dominant visual problems are structural: the Home centered assistant composer, nested Feed surfaces, repeated pills, border-plus-shadow cards and a microtype-heavy hierarchy. At 900×700, 79/87 visible leaf text nodes were below 12 px; tertiary-on-white contrast was 4.26:1, effective placeholder contrast 4.14:1 and calendar events measured about 84×21 px. At ≤930 px, the complete calendar stacks below the task pane and produced 1370 px document height. Lucide icon strokes, landmarks, button naming and ID uniqueness are positive. Keyboard focus behavior was not verified. This is audit evidence only; no code fix, accepted visual baseline, WCAG pass or pixel-parity claim is made.

2026-07-19 brand asset note: the supplied 204×228 Keen icon now replaces the placeholder `K` in the Sidebar and is used as the Browser Demo favicon. It is not used for the Tauri/macOS package icon because the source is not a packaging-resolution master. The inspected 1440×920 capture is `artifacts/ui-audit/2026-07-19/keen-home-brand-1440x920.png`; the fixed visual harness still reports three `missing_reference` states, so this is qualitative evidence only.

## Authorization intake

Resolve these before copying any protected visual asset:

- [x] Supply one canonical authorization document. `references/authorized-scope.md` is the canonical path; the notes-path variant is noncanonical.
- [ ] List every allowed screenshot, recording, Figma export, icon, illustration, logo, and Orbie asset.
- [ ] State allowed use: inspect only, derive measurements, modify, redistribute in source, and/or redistribute in built artifacts.
- [ ] Record prohibited pages/assets and any attribution or expiration conditions.
- [x] Record the user's attested internal-research permission for protected HyperKnow content. Ordinary interactive inspection is allowed; authentication bypass, bulk extraction, private data retention and redistribution remain prohibited.

2026-07-16 authorization note: the canonical scope permits reading the currently visible user-authorized page, screenshot saving, ordinary navigation and implementation use of covered protected visual content for internal research. The permission is a user attestation, not independently verified. The first attempt did not produce a usable capture. A later authenticated Chrome inspection on 2026-07-18 produced the three inventory-backed references above without retaining personal or user-generated learning content. They were used for qualitative structural comparison only, not a parity claim.

## Missing page references

| Priority | Page/state | Required evidence | Status |
| --- | --- | --- | --- |
| P0 | Shared 1440×920 shell | Expanded Sidebar, Toolbar, main canvas, Inspector, traffic-light/titlebar relationship | Missing |
| P0 | Home | Saved work, explicit New learning, source focus, unavailable and long-task states | Partial — both runtime compositions and packaged normal states exist; accepted same-state reference and packaged reduced-motion-on remain missing |
| P0 | Conversation | Streaming, long answer, citations, provider/error recovery and source preview/highlight | Partial — authorized long-conversation/citation references and current default/compact/long-content captures exist; aligned live streaming/provider-error/PDF-viewer references remain missing |
| P0 | Knowledge Base | Flat source list, import/drop, each real index status, failed/partial, selected-document Inspector/preview | Partial — authorized empty/processing/complete/hover/menu references and current default/compact/narrow/import screenshots exist; live failure/partial and Inspector coverage remain missing |
| P0 | Deep Learn | Goal, diagnostic, map, unit, checkpoint, recall, practice, summary, paused/resumed | Partial — entry/tool-selection state captured 2026-07-18 |
| Deferred | Quiz / Practice | Cover real item types only inside Deep Learn or Review; no independent release page baseline | Standalone page removed from the active visual scope |
| P0 | Learning Feed | Today/upcoming/completed, course filters, task selection/detail, secondary calendar, empty/error and long-task states | Partial — Today/calendar/confirmed/pending state captured 2026-07-18; task-list-first delta remains open |
| P1 | Settings | All sections, provider states, secret input, notices | Partial — current Status and Connections default/compact captures exist; live startup/error/recovery, secure provider setup and accepted reference coverage remain missing |
| P1 | Review | Prompt, reveal, rating controls, due/empty/source/error/uncertain-write/success states | Partial — real Review UI exists; aligned packaged populated and reduced-motion evidence remains open |
| Deferred | Study Planner | No release-surface evidence required until a real independent planning contract is approved | Existing boundary screen is historical only |
| Deferred | Memory / Persona | No release-surface evidence required until a real learner-approved memory job is approved | Existing boundary screen is historical only |
| Deferred | Visualize | Cover future visuals as Study/Conversation content blocks; no independent page baseline | Existing bundled example is historical only |
| P2 | Responsive shell | 1180×740 minimum, resized Sidebar, collapsed icon rail, Inspector hidden/collapsed | Partial — current Home/Feed/Conversation captures and overflow checks exist; collapsed rail and reference comparison remain open |

For each active P0/P1 surface above, collect only the empty, loading, partial, error, offline, permission-denied, cancelled, sidecar-unavailable, and applicable provider/index/migration failure states that can occur in its real current action. Deferred rows do not justify fixtures, frames, routes, or implementation work.

2026-07-16 hardening note: the implemented shell now distinguishes binding, migrating, recovering, starting-server, health-checking, restarting, unavailable, and configuration-error service states. No authorized reference or new screenshot/diff evidence exists for these states, so their visual acceptance remains missing.

2026-07-16 Gate 3 note: Knowledge Base now has real course selection, multi-course labels, relation actions, course filters, and partial course-state errors. These states passed component/integration behavior tests, but no authorized visual reference or screenshot/diff evidence exists, so visual acceptance remains missing.

2026-07-16 Gate 4 note: Knowledge Base now renders provider-missing/failure, indexed-lexical, indexed-hybrid, embedding, cancelled/interrupted, and needs-reindex states with real warnings and actions. Component/integration behavior tests passed, but no authorized reference or screenshot/diff exists; visual acceptance and any parity claim remain blocked.

2026-07-16 Gate 5 note: Conversation now renders real Tauri streaming, locally stopped, provider-missing, disconnected, lexical-only, structural-citation, and empty live states; Browser Demo is explicitly labeled and performs no request. Behavioral tests passed, but no authorized reference or visual-diff evidence exists, so visual acceptance remains missing.

2026-07-16 Gate 6 note: Conversation now opens an authenticated page-on-demand PDF citation dialog with supported-geometry highlights, original excerpts, truthful loading/offline/error/page-only states, bounded canvas allocation, keyboard focus trapping/restoration, and a bundled local PDF.js worker. Behavioral tests passed, but no authorized reference screenshot or visual-diff evidence exists; no pixel-parity claim is made.

## Learning-loop live-state gap

Historical status on 2026-07-16: **in progress** at the milestone level. The table below records the gap at that date; later dated evidence supersedes its implementation status, and the binding 2026-07-22 backlog at the top of this file supersedes its requested UI scope.

| Page | Current rendered behavior | Required real-state visual coverage |
| --- | --- | --- |
| Conversation | Live RAG streaming exists, but sent messages/history are not durable and Study mode does not create a Session | persisted history, Ask/Teach/Study/Review/Plan, creation confirmation, restart recovery, Agent activity and interrupted run |
| Deep Learn | Live Start/Resume renders a persisted multi-unit plan, opening diagnostic, per-unit Active Recall and targeted practice, authoritative successor recovery, and a final deterministic summary-to-FSRS handoff. Pending/unresolved assessment or summary states hide lesson/session/source clues before paint; answers record deterministic mastery evidence once, intermediate units do not complete the task or create Review, and final summary schedules one local review without another mastery write. Browser Demo remains explicitly sample-only and makes zero requests. | editable plan, teaching, live multi-unit/summary/practice screenshot and accessibility coverage, additional checkpoint/item types, direct task-completion Undo, broader persisted pause/resume/recovery and all service/provider failures |
| Quiz | Three bundled single-choice items with in-memory score/hints | six real item types, confidence, four hint levels, submit/feedback, mastery evidence/delta, misconception and scheduled review |
| Review / Flashcards | Authenticated SQLite due queue, front/answer reveal, optional local recall response, source context, empty/loading/offline/error/uncertain-write/success states, and persisted Again/Hard/Good/Easy FSRS ratings; Browser Demo substitutes no cards | browser screenshots and accepted baselines; keyboard/reduced-motion/a11y verification; generated-card edit/delete policy; course filtering; restart and packaged-app E2E |
| Learning Feed | Live course-scoped Snapshot/recommendation Feed renders candidates, persisted tasks, Plan and Start/Resume actions with explicit loading/empty/error/offline/cancelled/unknown-reconciliation states; Start/Resume opens the real read-only persisted Deep Learn view. | Historical gap only: task-list-first organization, one truthful Start/Continue/Review action, real `Later…` deferral when supported, mutation recovery, persisted progress and Session create/resume coverage. The former four-action Complete/Snooze/Reschedule treatment is superseded. |

No screenshot or current UI fixture for these Demo/read-only surfaces is evidence that the learning workflow is connected. The three authorized HyperKnow references cover only Home, Learning Feed/calendar and the Deep Learn entry state; they do not cover the real workflow states in this table. Continue with provisional non-branded design values for uncovered states and do not claim parity until a fixed reference/seed/viewport visual diff exists.

2026-07-16 learning-loop Gate 3 note: Home now visibly consumes authenticated
Agent create/get/cancel/events, renders durable activity and recovery states,
and exposes terminal-only Study Task Undo/Redo backed by recorded inverse
actions. Browser Demo remains request-free; waiting approval is explicitly
read-only; the still-seeded Feed and statistics are labeled Sample. Component
and integration behavior tests passed. A new 1440×920 current capture was
written to `artifacts/visual-diff/current/home.png`; the report remains
`missing_reference`, so no diff percentage exists. The Home / Agent visual row
therefore remains Missing rather than passed, and no pixel-parity claim is made.

2026-07-16 Agent provider-hardening note: the production-default empty tool
allowlist and strict local-chat stop-reason validation are backend-only changes;
they add no rendered state or new visual evidence. The Home / Agent reference,
failure-state captures and parity comparison therefore remain Missing.

2026-07-17 Agent course-scope note: migration 019 and trusted run-scope
resolution are persistence-only changes. They do not add a rendered tool state,
reference capture or visual-diff result; the Home / Agent visual row and scoped
tool activity states therefore remain Missing.

2026-07-17 structured read-tool note: the loopback provider can now execute two
host-scoped Level 1 reads through a strict multi-round protocol, but this batch
changes only the Python runtime and backend tests. It adds no rendered state,
authorized reference capture, visual-diff artifact or parity measurement. The
Home / Agent tool trace, loading/error/permission states and HyperKnow visual
comparison therefore remain Missing.

2026-07-17 course-knowledge tool note: the Agent can now perform bounded,
course-scoped lexical retrieval as a third Level 1 tool, with private source
feedback and backend E2E evidence. This batch adds no rendered citation state,
source preview, authorized reference capture or visual-diff artifact. The Home /
Agent retrieval trace, source interaction states and HyperKnow comparison remain
Missing; no visual parity claim is made.

2026-07-17 course-setup note: Knowledge Base now contains a functional local
course-creation form with component/integration evidence, including Demo and
service-unavailable behavior. No authorized HyperKnow reference, fixed-viewport
capture, accessibility scan or visual-diff artifact was produced for this
state. Its layout and error states therefore remain implemented but visually
unverified, and no parity claim is made.

2026-07-17 autonomous-foundation note: indexed-document concept bootstrap and
the deterministic Learning Snapshot are backend-only building blocks. They add
no rendered Agent status, Feed recommendation, Deep Learn state, screenshot or
visual-diff evidence. All autonomous-learning visual rows therefore remain
Missing; no product-flow or parity claim is made from backend tests.

2026-07-17 autonomous-recommendation note: the backend can now persist one
real, explainable Study Task from the deterministic Snapshot, including a
truthful replay/covered/empty outcome. It is not exposed through an API or
rendered by the desktop in this slice, and no Start action is connected. The
Home/Feed recommendation, state matrix, screenshot and visual-diff rows remain
Missing.

2026-07-17 autonomous-recommendation API note: authenticated strict resources
now expose the real Snapshot and persisted recommendation outcome, but this
backend slice adds no rendered state, screenshot, accessibility result or
visual-diff artifact. Home/Feed visuals and the Start action remain Missing.

2026-07-17 autonomous-session note: migration 022 and the backend coordinator
can create or recover a source-cited Study Session from a persisted autonomous
task, but no HTTP Start resource or desktop control is included in this slice.
Deep Learn/session states, screenshots and visual diffs therefore remain
Missing.

2026-07-17 autonomous-session API note: the authenticated sidecar now exposes
the real Start/Resume boundary and bounded cited plan data, but no desktop
control or rendered study state is included in this backend slice. Deep Learn,
blocked/created/resumed visuals, screenshots and visual diffs remain Missing.

2026-07-17 autonomous-learning desktop note: the live Learning Feed now renders
the authenticated Snapshot and recommendation outcome with course and study-time
selection, candidates, persisted task state and an explicit Plan action. Its
loading, empty, error, offline, cancelled and unknown-outcome reconciliation
states have behavior-test evidence; scope/cache/late-response isolation is also
tested. Home live mode no longer displays seeded Feed/statistics as learner data,
while Browser Demo remains visibly sample/request-free. No screenshot,
accessibility run, authorized reference capture or visual-diff was produced for
this slice. Therefore Learning Feed and Home/Agent visual rows remain Missing,
and no visual or pixel-parity claim is made. Desktop Start/Resume is still
unimplemented; Deep Learn remains its hard-coded demo while a real session
reader is in progress.

2026-07-17 autonomous Deep Learn note: the live Learning Feed now starts or
resumes a persisted Study Session and routes to a read-only Deep Learn view
that renders the stored plan and bounded source citations. Focused desktop and
source-ID contract tests passed, and independent acceptance found no open
P0/P1/P2; this is behavioral evidence only. The view does not write learner
progress, answers, mastery, task completion, or FSRS scheduling. Browser Demo
remains visibly sample-only and request-free. No screenshot, authorized visual
reference, visual-diff, or accessibility run was produced, so the Deep Learn
and Learning Feed visual rows remain Missing and no HyperKnow or pixel-parity
claim is made.

## Reference manifest fields

Every accepted reference should have a manifest entry with:

- stable `reference_id` and source file path;
- page/route and state name;
- capture viewport in CSS pixels and display scale;
- app/version/capture date if known;
- required deterministic seed fixture;
- authorization-scope entry;
- higher-resolution/newer precedence information;
- ignored dynamic regions and why;
- mismatch threshold (default target below 3% for primary pages);
- notes for expected macOS-native variance.

## Regression harness status

- [x] Create `tools/visual-regression/` without fabricated baseline images.
- [x] Add a machine-readable page manifest (schema validation remains open).
- [x] Start from deterministic frontend seed data and a direct route.
- [x] Disable motion/transitions/caret and request reduced motion (fixed clock/locale/timezone remain open).
- [x] Capture the Home application viewport at 1440×920 and display scale 1.
- [x] Capture Learning Feed and Deep Learn entry at 1440×920 and Home/Feed/Conversation at 1180×740.
- [x] Produce `current` and a machine-readable report under `artifacts/visual-diff/`; diff and mismatch are emitted only when a real reference exists.
- [ ] Exclude only documented truly dynamic regions.
- [ ] Retain failed artifacts in CI.
- [ ] Iterate in order: layout, panel widths, typography, spacing, color/border, radius, shadow, icons, micro-motion.
- [ ] Record measured colors and dimensions in `packages/design-tokens`; do not silently overwrite provenance.

## Provisional design values

The values supplied in the brief—such as `#F6F8FA` app background, 248 px Sidebar, 336 px Inspector, and 52 px Toolbar—are starting hypotheses, not reference measurements. Implementation and tests should name them accordingly until a manifest-backed measurement replaces them.

## Visual acceptance record

A current-image run has completed, but comparison cannot run without an authorized reference. `missing_reference` is a blocker state, not a pass.

The 2026-07-17 recoverable-read slice adds a red recoverable-failure dot, grey cancelled/stopped dots, and explicit lifecycle copy. Its event/schema/reducer/panel behavior is tested, but no new screenshot or authorized visual diff was captured; spacing, color and copy remain provisional visual work rather than parity evidence.

The 2026-07-17 Level 2 slice adds a Home Agent confirmation card with a safe task/course/effect summary, Confirm/Reject controls, pending feedback, offline blocking and explicit unknown-outcome copy. Component/runtime tests cover those states. The visual harness was rerun at 1440×920 against the deterministic Home route and produced `missing_reference`; that route does not fixture a live waiting-approval run, and no authorized HyperKnow reference is present. Approval-card spacing, typography, button treatment and responsive behavior therefore remain `implemented / unverified` visually, with no mismatch percentage or parity claim.

The 2026-07-17 diagnostic-progression backend slice initially added no rendered UI. The subsequent live Deep Learn client now renders the opening reflection, three self-assessment controls, pending/error/unknown/cancelled recovery, paused read-only state and first-active-unit handoff. Component and contract tests cover behavior, but no screenshot, accessibility audit or authorized visual diff was produced. Spacing, responsive treatment and parity therefore remain `implemented / unverified` visual work.

The 2026-07-18 Active Recall frontend slice renders not-started, restoring, pending, answered, cancelled, paused, unavailable, conflict and unknown-write recovery states. Contract/component tests cover the behavior and the source-hiding lock, but no screenshot, accessibility audit or authorized visual diff was produced. The interaction is therefore `implemented / unverified` visually; no HyperKnow-parity claim is made.

The 2026-07-18 targeted-practice frontend slice renders restoring, not-started, pending, answered, cancelled, paused, unavailable, conflict and unknown-write recovery states, with a source-hiding Inspector lock. Contract/component tests cover behavior, but no screenshot, accessibility audit or authorized visual diff was produced. The interaction is `implemented / unverified` visually; no HyperKnow-parity claim is made.

The 2026-07-18 summary framework renders restoring, ready, completed, cancelled, paused, unavailable, conflict and unknown-write recovery states and locks source, concept and review-lineage detail. Contract/component tests cover the live behavior. `references/keen/ui-review-basic-framework-2026-07-18.png` is a 1280×760 capture of the request-free Browser Demo shell on its sample Summary unit; it is useful for UI discussion only and does not show or verify the live persisted summary card. No accessibility audit, authorized reference comparison or HyperKnow-parity result exists.

The 2026-07-19 identity refinement removes the generic Home book glyph, black `K`
assistant avatar and colored learner bubble from the primary request/transcript
surfaces. Home and Keen answers now use the transparent traced Keen mark, while
learner copy is set as a flat reading row. Browser Demo captures and normalized
full/focused comparisons are under
`artifacts/ui-audit/2026-07-19/brand-identity-refinement/`. The authorized source
and current captures are not identical aspect ratios or product states, so this
is qualitative design-QA evidence only. Hover/focus/active/loading/error/success
coverage across the whole application, packaged Tauri rendering and an accepted
dimension-aligned baseline remain open.

The 2026-07-19 HyperKnow flow audit was completed in the user's authenticated
Chrome session with one upload of the approved synthetic, non-personal fixture.
The internal-only inventory under
`references/hyperknow/2026-07-19-flow-audit/` now covers Knowledge Base
empty/upload/processing/complete/hover/menu/quota, Deep Learn
entry/trace/result/outline/reading/loading/completion, cropped generic Settings,
and Month/Week Learning Feed states. Account identity, saved personal memory and
existing user-created learning content were not retained. Delete,
calendar/integration connection and external sharing were not performed. The
captures vary in crop/viewport and are qualitative references, not accepted
pixel baselines.

The first bounded implementation from this audit refines Deep Learn only. Its
1280×720 Browser Demo and source/current before/after comparisons are under
`artifacts/ui-audit/2026-07-19/deep-learn-reference-refinement/`. Progress is now
part of the learning-path rail; the lesson uses a bounded editorial column,
thin-accent callouts and a flat end-of-reading checkpoint. Browser checks found
no horizontal overflow, duplicate IDs, unnamed focusables or application errors
and exercised success and disabled states. The reduced-motion rule remains
present, but Tab-key injection and additional viewport captures were not
reliably available in this browser session. No pixel parity, packaged-app or
complete accessibility claim is made.

The follow-up Deep Learn polish is recorded under
`artifacts/ui-audit/2026-07-19/deep-learn-polish/`. It removes the remaining
vertical accent strips and nonfunctional header controls, narrows the lesson
measure, keeps answer focus from scrolling the document, and replaces the
paused/success card treatments with compact inline status. Current-run captures
cover default, empty-answer, success, paused, 1024×768 and 900×700 states; the
before/after and authorized-reference/current composites are qualitative because
their source viewports and states differ. Browser checks found no horizontal
overflow, duplicate IDs, unnamed focusables or application errors and exercised
reduced motion, a visible keyboard-focus sequence, and temporary extended lesson
copy. The fixed visual harness still reports `missing_reference` for Home,
Learning Feed and Deep Learn entry, so no mismatch percentage or pixel-parity
claim is made.

The complete-flow follow-up is inventoried under
`references/hyperknow/2026-07-19-complete-flow/`. It extends the qualitative
reference through unit completion, task/feed/calendar modes, synthetic file
upload/search/actions, history, planning, diagnostic, source-grounded concept
explanation, recall and next-step copy. The bounded Keen implementation adds
truthful Current/Next continuity to the existing Deep Learn rail without copying
the floating composer, quota overlay, generic AI Home or unfinished integration
affordances. Default, paused and summary captures plus a same-size comparison are
under `artifacts/ui-audit/2026-07-19/`. The configured visual harness still has no
accepted baselines and returned three `missing_reference` results.

The fuller synthetic-state follow-up is inventoried under
`references/hyperknow/2026-07-19-full-synthetic-flow/`. Stable captures cover
source-labelled recall feedback, a recommendation-only review plan and a
successful three-page PDF upload; transient session-refresh captures are retained
but rejected as stable visual references. Calendar, Drive and Canvas live entry
states remain authentication-blocked after the authorized session redirected to
`/signin`. Keen Browser Demo now covers the corresponding summary/evidence and
connection-preview states without changing live capabilities. Current captures
are under `artifacts/ui-audit/2026-07-19-synthetic-states/` and
`artifacts/visual-diff/current/`. The harness now covers seven states at 1440×920
and 1100×760, all with `missing_reference`; no mismatch percentage exists.

The 2026-07-20 Figma Phase-4 slice adds a true 1180×740 Home reflow, reusable
Feed Task Card and Calendar Event sets, and default/compact componentized
Learning Feed screens. Direct exports and Plugin API QA are under
`references/keen/2026-07-20-figma-phase4/`; data and capability precedence is
in `docs/figma/LEARNING_FEED_DATA_MAPPING.md`. The compact Feed retains seven
calendar columns and has no horizontal root overflow; its task list intentionally
scrolls below the viewport. This is editable design evidence only. Selected
task detail, loading/error/offline variants, collapsed Sidebar, dark mode,
runtime keyboard/reduced-motion/accessibility checks, React implementation and
accepted same-viewport baselines remain open.

The same Phase-4 file now also includes selected-task default/compact screens
and compact loading, no-courses, service-unavailable and validation-error
screens. The selected detail uses the current Browser Demo task and marks its
mastery as illustrative; operational states use current React recovery copy
and do not substitute Demo tasks. Figma font/geometry/horizontal-overflow QA is
recorded under `references/keen/2026-07-20-figma-phase4/`. Actual runtime state
capture, keyboard traversal, reduced-motion and accessibility testing remain
open, as does React implementation of the approved Figma direction.

The first bounded React convergence pass is now implemented for Home, Learning
Feed and selected task detail. Runtime captures at 1180×740 and 1585×941 are
under `artifacts/ui-audit/2026-07-20/react-figma-convergence/`. At the compact
size, Home uses the Figma 720 px measure, the document has no horizontal
overflow, and selecting the second task scrolls only the task rail while the
detail toolbar stays visible. Captures requested reduced motion. The Figma
Button set's 18 labels were moved to Inter Medium so text overrides work, but
Code Connect remains account-seat blocked. A Demo-only deterministic visual
fixture now freezes July 2026 and selects a requested sample task without
changing Tauri/live behavior. The configured harness compares three 1180×740
provisional Figma exports: Home 1.988%, Feed 1.555%, and selected task detail
3.158% after compact panel/reading/footer convergence. Seven older routes still
report `missing_reference`; these provisional measurements are not accepted
baselines or pixel-parity results. Complete keyboard/a11y coverage, live
operational-state captures and accepted visual baselines remain open.

The 2026-07-20 operational-state convergence adds deterministic development-only
Browser fixtures for local startup, unavailable service, no courses and invalid
live data. They require `visualTest=true` plus `visualCoreState`, issue no Tauri
or fetch calls and do not substitute Demo tasks. At 1180×740 their provisional
Figma comparisons are 1.314%, 1.218%, 1.057% and 1.368% respectively. The
runtime intentionally keeps `Learning core ready` in the shared Sidebar for the
no-course state instead of copying Figma's page-data label into a service-status
component. Chrome found no horizontal overflow, honored reduced motion and
showed keyboard focus on both recovery actions. These are comparison-only
figures against provisional exports, not accepted baselines or parity passes.

The 2026-07-20 Deep Learn convergence keeps the default 1585×950 composition
aligned with editable Figma node `65:2` while improving real reading behavior:
the session header and side rails remain available during long content, unit
changes focus the new lesson heading, and submitted demo feedback receives focus.
The latest same-size visual harness measured 1.602% mismatch and refreshed the existing
1100×760 compact Summary screenshot. The reference is a provisional export of
the current product frame, not an accepted competitor baseline. The follow-up
distillation pass keeps one main landmark, raises light tertiary contrast to
approximately 4.70:1 on white, and changes the 900 px shell to a 60 px labelled
icon rail. Scripted Chrome checks found zero horizontal overflow, honored reduced
motion and focused the next lesson heading. Earlier QA-only long-copy checks also
kept the sticky header and path rail in place. No parity claim is made.

| Date | Reference ID | Route/state | Viewport | Seed | Mismatch | Artifact paths | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-15 | Missing | `/?demo=true&visualTest=true` | 1440×920 @1x | Deterministic frontend seed | Not computed | `artifacts/visual-diff/current/home.png`, `artifacts/visual-diff/report.json` | `missing_reference`; no parity claim |
| 2026-07-18 | Missing | Home, Learning Feed and Deep Learn entry | 1440×920 @1x | Deterministic Browser Demo | Not computed | `artifacts/visual-diff/current/{home,learning-feed,deep-learn-entry}.png`, `artifacts/visual-diff/report.json` | three `missing_reference` results; structural implementation only |
| 2026-07-18 | Missing | Home and Learning Feed light/dark polish | 1180×740 @1x | Deterministic Browser Demo | Not computed | `artifacts/ui-audit/2026-07-18/keen-ui-refresh-2/`, `artifacts/visual-diff/report.json` | focused responsive/theme evidence; references still missing |
| 2026-07-19 | Authorized structural references; no accepted pixel baseline | Home and Learning Feed default | 1462×907, 1180×760, 900×700 | Deterministic Browser Demo | Not computed | `artifacts/ui-audit/2026-07-19/`, `artifacts/visual-diff/report.json` | qualitative design QA passed; harness returned three `missing_reference` results |
| 2026-07-19 | Authorized structural Home reference plus user-supplied Keen mark | Home request identity and Demo Conversation transcript | 1280×720 | Deterministic Browser Demo | Not computed | `artifacts/ui-audit/2026-07-19/brand-identity-refinement/`, `design-qa.md` | scoped qualitative QA passed; aspect ratios/states differ, no parity claim |
| 2026-07-19 | Authorized authenticated Deep Learn flow; no accepted pixel baseline | Demo Deep Learn reading/progress/default and disabled states | Reference 1488×851 crop; Keen 1280×720 | Synthetic reference upload; deterministic Keen Browser Demo | Not computed | `references/hyperknow/2026-07-19-flow-audit/13-session-thinking.png`, `artifacts/ui-audit/2026-07-19/deep-learn-reference-refinement/`, `design-qa.md` | qualitative scoped QA passed; viewports differ, no parity claim |
| 2026-07-19 | Authorized authenticated Deep Learn flow; no accepted pixel baseline | Demo Deep Learn default, answer, success, paused and compact desktop states | Reference 1488×851 crop; Keen 1280×720, 1024×768 and 900×700 | Synthetic reference upload; deterministic Keen Browser Demo | Not computed | `artifacts/ui-audit/2026-07-19/deep-learn-polish/`, `design-qa.md`, `artifacts/visual-diff/report.json` | scoped polish QA passed; harness still `missing_reference`, no parity claim |
| 2026-07-19 | Authorized complete learning flow; no accepted pixel baseline | HyperKnow diagnostic → explanation → recall and Keen Deep Learn Current/Next default, paused and summary states | Reference and Keen screenshots 1700×777 image pixels; source CSS scale not independently retained | Synthetic non-personal fixture; deterministic Keen Browser Demo | Not computed | `references/hyperknow/2026-07-19-complete-flow/`, `artifacts/ui-audit/2026-07-19/hyperknow-keen-learning-continuity-comparison.png`, `artifacts/visual-diff/report.json` | qualitative continuity QA passed; three harness baselines remain missing, no parity claim |
| 2026-07-19 | Authorized synthetic result flow; no accepted pixel baseline | HyperKnow recall/review/PDF upload and Keen Demo Summary/Connections | HyperKnow 1700×777 image pixels; Keen 1440×920 and 1100×760 @1x | Synthetic non-personal fixtures; deterministic Browser Demo | Not computed | `references/hyperknow/2026-07-19-full-synthetic-flow/`, `artifacts/ui-audit/2026-07-19-synthetic-states/`, `artifacts/visual-diff/current/{deep-learn-summary,settings-connections,deep-learn-summary-compact,settings-connections-compact}.png`, `artifacts/visual-diff/report.json` | scoped visual QA passed; seven harness baselines missing, no parity claim |
| 2026-07-19 | Current runnable Keen page baseline; no accepted pixel reference | Home, Learning Feed and Deep Learn Browser Demo | Browser QA 1280×720; Figma HTML captures 1585×907/941/950 | Deterministic Browser Demo | Not computed | `artifacts/ui-audit/2026-07-19-core-ui-hardening/`, `artifacts/figma/page-baselines/`, Figma file `ugwiIPdF43v2woYsLM3b9R` page `67:2` | three editable raw Figma page frames created; no horizontal overflow in inspected browser states; frames are not componentized and are not accepted visual-regression baselines |
| 2026-07-20 | Authorized qualitative Feed structure plus current Keen raw frame; no accepted pixel baseline | Home compact and Learning Feed default/compact Figma designs | Home/Feed compact 1180×740; Feed default 1585×941 | Deterministic Browser Demo | Not computed | `references/keen/2026-07-20-figma-phase4/`, `docs/figma/LEARNING_FEED_DATA_MAPPING.md`, Figma nodes `155:187`, `162:97`, `164:179` | Figma componentization and compact geometry QA passed; runtime and baseline comparison remain pending, no parity claim |
| 2026-07-20 | Approved provisional Figma exports; no accepted pixel baseline | React Home, Learning Feed and selected task detail | 1180×740 and 1585×941 @1x | Deterministic Browser Demo, reduced motion; July 2026 visual fixture | Home 1.988%; Feed 1.555%; detail 3.158% | `artifacts/ui-audit/2026-07-20/react-figma-convergence/`, `artifacts/visual-diff/{report.json,current,diff}` | three provisional same-size comparisons recorded; seven older references remain missing; no parity claim |
| 2026-07-20 | Approved provisional Figma operational exports; no accepted pixel baseline | Feed startup, unavailable, no-course and invalid-data states | 1180×740 @1x | Development-only browser visual fixtures, reduced motion | 1.218%; 1.314%; 1.057%; 1.368% | `artifacts/visual-diff/{report.json,current,diff}/feed-*-figma-compact.png`, `artifacts/ui-audit/2026-07-20/react-figma-convergence/` | no Tauri/network calls; recovery focus checked; intentional healthy/no-course Sidebar difference; no parity claim |
| 2026-07-20 | Current-product Figma node `65:2`; no accepted pixel baseline | Deep Learn Browser Demo default, compact Summary, distilled narrow shell and long-content behavior | 1585×950, 1180×740, 900×700 and 1100×760 @1x | Deterministic Browser Demo, reduced motion; QA-only injected long copy for scrolling | 1.602% for default; compact reference missing | `artifacts/figma/page-baselines/keen-deep-learn-live-capture-1585x950.png`, `artifacts/visual-diff/{report.json,current,diff}/deep-learn-figma-default.png`, `artifacts/visual-diff/current/deep-learn-summary-compact.png`, `artifacts/ui-audit/2026-07-20/react-figma-convergence/deep-learn-{compact,narrow,distilled}*.png` | one main landmark; 60 px labelled icon rail at 900 px; zero horizontal overflow; provisional comparison only; no parity claim |
# Home service-state cleanup evidence — 2026-07-20

The Home unavailable state now uses one reading axis and one recoverable error:
the isolated workspace disclosure and duplicate live-evidence badge are gone,
and the schedule action is absent until the queue is readable. Deterministic
current-product captures at 1440×900 and 980×700 are under
`artifacts/ui-audit/2026-07-20/home-status-cleanup/`. The visual harness measured
2.018% against the provisional Home Figma export. That export is not an accepted
baseline, so no pixel-parity claim is made; the canonical Home reference is still
missing.

The follow-up Home action-state correction does not change the captured default
or unavailable compositions. Non-cancellable `creating`, `recovering`, terminal
`streaming`, and `cancelling` phases now use labelled shared loading states;
only a real active run exposes the square stop icon and clickable `Cancel`
action. The composer owns that one action, while Agent Activity omits its cancel
control on Home and reusable panels render no dead control when no callback is
available. Creating, cancelling and missing-action behavior is component-tested,
but no accepted reference or fixed-viewport capture exists for these transient
runtime states. Their visual acceptance therefore remains open, and the
provisional 2.018% Home comparison is unchanged rather than rerun or represented
as parity.

The 2026-07-20 Agent Activity distillation replaces the elevated rounded card,
filled generic status block, nested mutation surfaces and 9–10 px microcopy with
a flat divided record, transparent state row and 11–13 px text hierarchy. An
explicit development-only Browser Demo fixture renders the empty `Ready` state
and states that no Agent ran or data changed. Its 980×900 current capture is
`artifacts/visual-diff/current/home-agent-activity-empty.png`. Browser inspection
found no horizontal overflow or unnamed buttons and confirmed reduced-motion
coverage, zero panel radius/shadow, transparent status treatment and 13 px
response text. The configured reference is missing, so visual acceptance for
empty, running, streaming, approval, error and mutation states remains open and
no mismatch or parity claim is made.

# Packaged Alpha golden-path evidence — 2026-07-22

The lightweight Home continuity follow-up was also checked in a freshly rebuilt
packaged UI-audit app using existing persisted Alpha Bayesian state. Home showed
the same course, learner goal, progress and `Finish summary` action before and
after restart, and the action opened the exact course-scoped Deep Learn route.
This was a native interaction/state check without a new accepted screenshot
baseline; it does not establish pixel parity.

The current ad-hoc `Keen UI Audit.app` was inspected in its packaged Tauri
WebView at 1180×740 CSS px, DPR 2, Light appearance and `reduced=false`. The
authenticated sidecar remained ready while the real persisted Alpha course was
followed through Completed Feed, read-only completed Task Detail, History,
completed Deep Learn Summary, Review and Knowledge Base. A normal quit and
relaunch restored the same History record and completed Summary; both quits
left no audit app or learning-core process.

Evidence is stored in `artifacts/ui-audit/2026-07-22/alpha-golden-path/`:

- `01-completed-feed-list.jpeg`
- `02-completed-task-detail.jpeg`
- `03-history-completed-summary.jpeg`
- `04-review-after-rating.jpeg`
- `05-indexed-source.jpeg`
- `06-restart-recovery.jpeg`

These are native acceptance screenshots of real local state, not deterministic
visual-regression references. Dark appearance and packaged reduced-motion-on
were not exercised in this pass. Existing missing references remain unresolved,
so no mismatch percentage, accepted-baseline result or pixel-parity claim is
made.

The final independent Alpha closeout audit made no visual or product-code
change. It reconfirmed that this evidence supports the internal learning-loop
slice only; packaged Dark, `reduced=true`, long-content and accepted-reference
coverage remain open.

2026-07-23 continuity freeze note: the final Home/Review/Summary work is a
behavioral state-consistency correction, not a new visual direction. Home now
fails closed while due Review truth is pending or the learning core is
unhealthy, and the completed Summary keeps its Review/Feed navigation disabled
while persisted continuity caches refresh. Focused tests passed 54/54, the
complete desktop suite passed 440/440, and independent Sol-high acceptance found
P0 = 0 and P1 = 0. No new packaged screenshot was captured. Packaged Dark,
`reduced=true`, long-content, accepted references and pixel parity remain open
and are not prerequisites for the bounded non-UI Agent contract Slice 0.
2026-07-24 core UI vertical-slice note: the current Shell → Learning Feed /
Task Detail → Deep Learn closeout reuses the existing tokens, shared controls,
canonical Keen SVGs, runtime page contracts and fixtures. Sidebar collapse now
keeps the canonical mark and keyboard-reachable toggle without overflow. Feed
uses Today / Upcoming / Completed as its only default sections, keeps overdue
work labelled inside Today, and retains one selected-task action. Deep Learn
keeps the collapsible path rail plus a bounded 72ch reader at the 1180–1280px
contract, opens Sources and Activity only on demand, and makes the existing
Explanation → Recall → Intervention → Practice order explicit. No third column,
Agent run button, route, provider behavior, dependency, global token family,
block registry, third-party component, asset, fixture or sample success was
added. Scoped ESLint, strict TypeScript, focused component tests, the production
Vite build and `git diff --check` pass; the build retains its existing
large-chunk warning. Browser Demo Home at 1180×740 and Deep Learn at 1280×720
were inspected with zero application-console errors and two existing React
Router future warnings. Evidence is stored under
`artifacts/ui-audit/2026-07-24/current-slice/`. These captures verify only the
request-free Demo shell. Packaged WebView verification, accepted references,
visual diff, accessibility-complete and pixel-parity evidence remain open.

2026-07-24 bounded learning-intervention note: Deep Learn now uses the existing
two-column path-rail/document composition for one real post-Recall Agent
intervention. Checking, choice, running/cancel, verified explanation,
source-review fallback and Practice handoff stay inline under explicit Recall →
Intervention → Practice labels; source details expand on demand and do not add a
third persistent column. Generated explanation text is rendered inertly rather
than as HTML. The component and Provider UI checks pass 6/6, strict TypeScript
and scoped ESLint pass, and the backend/API checks are recorded in the
implementation plan. No fixed-viewport current screenshot, accepted reference,
visual diff, packaged WebView, Dark/reduced-motion-on capture or pixel-parity
claim was produced for these new transient states.

2026-07-24 saved-path note: the live Deep Learn contract now renders the
validated persisted plan before the opening reflection. It uses the existing
two-column composition and tokens to show one quiet route overview with unit
order, objective, per-unit/total minutes and unique indexed-passage count; the
rail mirrors the order as disabled steps. Source body and chunk identities stay
hidden and no unit can be entered early. The learner then gets one primary
`Start opening reflection` action. Component journeys cover the truthful data,
locking, reload contract, StrictMode lifecycle and source-review/Practice action
hierarchy. No live persisted-session screenshot, packaged WebView capture,
accepted reference, mismatch percentage or pixel-parity result was produced for
this new state, so its visual status is `implemented / unverified`.
## 2026-07-25 feedback-flow convergence

- [x] Replace raw provider Markdown with inert editorial paragraphs, ordered
  lists, emphasis, source quotation and readable inline/display math.
- [x] Replace the oversized full-prompt Practice heading with one short heading,
  one instruction and a bounded question surface.
- [x] Normalize the persisted chain-rule prose formula to
  `y′ = f′(g(x)) · g′(x)` for presentation without changing the hidden answer.
- [x] Remove the false input-like focus ring from programmatically focused
  Recall results, separate the status badge from the result heading and tighten
  Recall → Intervention → Practice spacing.
- [x] Keep Home saved-work composition to one Next-up action.
- [x] Add direct Feed empty-state recovery and correct completed Task Detail
  status semantics.
- [x] Capture the final post-spacing Debug app state after the final rebuild.
- [ ] Run accepted-reference visual diff, Dark and reduced-motion-on coverage;
  none is claimed by this pass.

## 2026-07-25 practice refinement

- [x] Remove the tinted learning-path surface and keep one quiet divider.
- [x] Replace the ambiguous percentage-first rail with unit position and
  completed-unit truth.
- [x] Show Recall, current and next as one compact sequence.
- [x] Replace the essay-sized Practice textarea with a single-line missing-term
  field and `Check answer`.
- [x] Separate the normalized formula from the source sentence and show the
  missing term as a visible blank.
- [x] Support Enter submission and preserve unknown-outcome retry semantics.

## 2026-07-25 guided-learning Figma delta

- [x] Reuse the canonical Keen Figma file, tokens, components and brand assets.
- [x] Define mutually exclusive Home saved-work and New-learning compositions.
- [x] Keep one `New learning` entry and the release navigation only.
- [x] Define a two-column Deep Learn reading state with readable math and an
  inline recall handoff.
- [x] Validate visible typography as SF Pro, with STIX Two Math limited to the
  equation.
- [x] Add and visually inspect a continuous closed-book Recall state.
- [ ] Visually validate the written Agent intervention and targeted Practice
  states after the Figma screenshot service recovers.
- [ ] Correct and validate the current/next learning-path rail labels in the
  final two states.
- [ ] Compare the delta against the packaged runtime at a fixed viewport before
  accepting any visual baseline.

## 2026-07-25 Appica primitive convergence

- [x] Review an immutable Appica UI revision and its exact root MIT license.
- [x] Keep Keen's component API and adapt the useful treatment instead of
  introducing Tailwind/Base UI as a parallel runtime.
- [x] Unify Button, Field, Card, Badge and Progress sizing, surfaces, focus,
  hover and pressed states through existing shared primitives.
- [x] Prevent primary and secondary button labels from wrapping.
- [x] Inspect Home and Learning Feed at 1280×800 and Deep Learn at 1180×760
  after the global 40 px control change; no horizontal overflow or shared-button
  wrapping was found.
- [x] Normalize the canonical Figma Recall actions to two equal 128×40
  instances with a 12 px gap.
- [x] Preserve two readable lines for Feed task titles while keeping the status
  badge visible; verify the selected task-detail state at 1280×800.
- [x] Give each Settings section specific orientation copy, remove future
  integration placeholders, and keep provider security/restart detail
  contextual to Capabilities.
- [x] Inspect browser-demo Settings Status and Capabilities at 1280×800 with no
  horizontal document overflow.
- [x] Converge the History and Review empty states on the shared restrained
  composition, including an explicit icon, 13 px explanation and one primary
  recovery action.
- [x] Remove the repeated Settings header runtime label while preserving the
  single global Demo disclosure and contextual Status truth.
- [x] Reinspect History, Review, Settings Status and Settings Capabilities at
  1280×720 with no horizontal document overflow.
- [x] Make Browser Demo New learning truthful: Ask is the editable draft
  handoff; Focused study explains its indexed-desktop-course requirement and
  cannot redirect an entered study goal into Conversation.
- [x] Remove the Demo instruction to select an absent source control and give
  the unavailable Study path one real `View sample sources` action.
- [x] Reinspect Home Ask, Home Focused study and Knowledge Base at 1280×720
  with no horizontal document overflow.
- [x] Inspect History, Review and Settings in the packaged WebView after the
  global 40 px control change.
- [x] Inspect current Browser Demo Dark and reduced-motion-on states at
  1180×760, plus native packaged Home, Feed and Settings in Dark.
- [ ] Inspect reduced-motion-on in the packaged WebView; browser emulation does
  not replace this native check.
- [ ] Treat the connected Appica Figma source as thumbnail-only until its
  underlying component pages become accessible; do not claim component or
  pixel parity.

## 2026-07-25 packaged supporting-page pass

The production-named ad-hoc `Keen.app` was rebuilt and inspected in its native
WKWebView at the configured 1204×768 window across Home, Knowledge Base,
Learning Feed, History, Review and Settings. The authenticated bundled learning
core reached `ready`; no synthetic course, task, question or review data was
created during inspection.

The pass found one release-page hierarchy defect: the no-course Learning Feed
still rendered the task rail, an empty detail panel and a prominent Calendar
action. It now replaces that workspace with one bounded three-step onboarding
composition and one `Open Knowledge Base` action. Calendar remains secondary
and becomes visible only in the real/Demo task workspace. The rebuilt native
app was visually rechecked after the change.

Focused desktop validation passed 3 files / 90 tests, strict TypeScript and
ESLint. A production frontend build and current ad-hoc `.app` bundle completed;
strict deep code-signature verification passed. Earlier in the same pass, the
full DMG workflow verified checksum, mounted-app signature, bundled-sidecar
health/PDF/CJK/token cleanup and Review restart/idempotency recovery. Because
the final Feed adjustment was frontend-only and only the `.app` was rebuilt
after it, that earlier DMG is not claimed as the current visual artifact.

Still open: packaged reduced-motion-on, long-content and complete keyboard
traversal; accepted-reference visual diff; Developer ID/notarization; and the
full anti-template acceptance gate. No pixel-parity claim is made.

## 2026-07-25 Dark and motion token closeout

The fixed 1180×760 Browser Demo exposed two WCAG AA failures in Dark: white
text on the light accent measured 2.42:1, and gray text on the fixed cream Demo
disclosure measured 2.11:1. Dark accent text now uses the existing ink value
and measures 7.33:1. Demo disclosure foreground/background now use semantic
warning tokens and measure approximately 6.13:1 over the Dark reading surface.

The same audit found four CSS variables referenced but never defined:
`surface-subtle`, `radius-md`, `shadow-xs`, and `motion-inline`. They now live
in the shared token package with Light/Dark surface and shadow values. The
intervention quotation also uses one full subtle border instead of a decorative
side stripe. The scoped Impeccable detector reports zero findings after the
repair.

Browser Deep Learn, Learning Feed and Settings passed Dark inspection at
1180×760 with zero horizontal document overflow. Emulated reduced motion
reported 0.01 ms animation and transition durations for the inspected Deep
Learn controls. A current ad-hoc `Keen.app` was then rebuilt and native Home,
the no-course Feed and Settings were inspected in macOS Dark. The system
appearance was restored from Dark to its original Automatic setting after
inspection.

Focused validation passed 5 desktop files / 84 tests, strict TypeScript,
ESLint, production frontend build and strict deep app signature validation.
Packaged reduced-motion-on, long persisted learning content, complete
keyboard-only traversal, accepted-reference visual diff, Developer
ID/notarization and pixel parity remain open.

## 2026-07-26 Plan Proposal Slice 3A boundary

The source-grounded, read-only Plan Proposal contract is implemented behind
authenticated create/current APIs and a strict client schema. This increment
adds no React surface, Figma frame, screenshot, accepted reference or visual
baseline. In particular, no `Accept adjustment` or `Keep current plan` control
is shown because migration 032, deterministic CAS apply, receipt and safe Undo
do not exist yet. The next visual slice may add one inline current-versus-
proposed sequence inside Deep Learn, with citations and truthful
queued/running/ready/stale/unavailable states, but Apply must remain absent
until its real write contract is complete.

## 2026-07-26 Plan Proposal Slice 3B inline diff

Deep Learn now places one restrained Plan Proposal invitation and diff inside
the existing Intervention document flow. It uses the reading column instead of
a new page, drawer or card grid; Practice remains the only primary action.
Queued/running, provider-missing, conflict, stale and ready states are covered,
and the ready state says `not applied` above a source-linked proposed sequence.
Focused component/integration tests passed 4 files / 61 tests; strict
TypeScript, scoped ESLint, production build, ad-hoc Debug `.app` build and
strict deep signature verification passed. The scoped Impeccable detector
reported no findings.

Native macOS acceptance used the persisted Calculus session and configured
`deepseek-v4-flash` provider. The generated inline diff restored through a full
app/sidecar restart. Read-only SQLite inspection confirmed Session revision 7,
plan version 1 and two saved Units, with no apply-proposal row for that
Session. This is functional packaged-WebView evidence, not an accepted visual
baseline, pixel-parity result, complete keyboard traversal, Developer ID
signature or notarization claim.

## 2026-07-26 DeepTutor code-to-Figma route audit

The reviewed DeepTutor `v1.5.1` commit was checked again at its immutable SHA,
and its complete `web/app` route inventory was grouped into current Keen page
contracts. The resulting accept/merge/defer/reject matrix is in
`docs/DEEPTUTOR_UI_ADAPTATION.md`. Six bounded reference/adaptation pairs are
planned in the existing canonical Figma file: Chat/Home, Knowledge Base,
Mastery/History+Review, Book/Deep Learn, Settings and Notebook/deferred.

A fresh-page Figma pilot was removed after screenshot verification showed that
new SF Pro text existed in node metadata but did not render. Existing Approved
frames still rendered normally. No empty DeepTutor page remains in the file.
Future reference work must duplicate a verified Keen frame first, retain its
text/style/component lineage, and then reshape it; no pixel-parity or completed
DeepTutor-frame claim is made.

2026-07-26 DeepTutor-pair correction: the first mixed Chat/Home reference and
Keen adaptation were visually rejected as sparse, inconsistent and unlike the
upstream UI. Nodes `238:157` and `238:327` are hidden and named Rejected. The
private canonical Figma file now uses official upstream Home screenshot node
`252:202`, sourced from reviewed v1.5.1 path
`assets/figs/web-1.4.6+/home/00-overview.png`, at exact-aspect 1180×660. Its
export was visually inspected without cropping or bars. Editable replica
`257:157` now rebuilds the measured shell, Geist/Lora typography, 720px
composer, vector controls and reference-only upstream logo fills. The 1180×660
comparison returned RMSE `0.0734996` and 16,513 pixels outside 5% fuzz
(`2.12031%`). Because the white canvas dominates that metric, the frame remains
`In Review`, not accepted pixel parity. It is not a Keen page contract, runtime
asset or regression baseline. Visually accept or refine this replica before
designing another Keen adaptation or moving to Knowledge Base.

## Final UI anti-template acceptance gate

Run this once the release-page UI convergence is complete. This is a
verification gate for the existing product slice, not a new visual direction or
component system. Hallmark is a methodological reference only; no Hallmark
source, skill package or design asset is incorporated.

- [ ] Review Shell/Sidebar → Learning Feed/Task Detail → Deep Learn → Home,
  History, Review, Knowledge Base and Settings as one coherent product, not as
  isolated screenshots.
- [ ] Score six qualities before acceptance: product point of view, information
  hierarchy, execution quality, Keen-specific language, restraint and
  appropriate structural variety.
- [ ] Reject repeated dashboard-card templates, excessive pills/badges,
  decorative gradients/glass/glow, generic AI-assistant copy, ornamental
  eyebrow labels, invented metrics, fake progress and future-feature
  affordances.
- [ ] Check for meaningless whitespace, weak density, inconsistent line
  wrapping, competing primary actions, over-rounded containers and surfaces
  that look interactive but are not.
- [ ] Confirm all visible data and success states come from current Demo or
  runtime contracts; do not invent testimonials, outcomes, provider activity,
  integrations or learning efficacy.
- [ ] Confirm typography, colors, spacing, radii and elevation use the existing
  Keen tokens. Do not improvise a second palette or type scale during polish.
- [ ] Exercise hover, focus, active, disabled, loading, partial, empty, error,
  offline, provider-missing/rate-limited, cancelled and completed states where
  they affect the guided-learning slice.
- [ ] Inspect Browser Demo at 1280×800 and 1180×760, plus the supported compact
  desktop width. Verify no horizontal overflow, clipped actions, awkward
  wrapping or hidden recovery path. Do not import Hallmark's phone-width
  requirements blindly into this macOS desktop product.
- [ ] Inspect the packaged WebView in Light, Dark and reduced-motion-on modes,
  including long content and keyboard-only traversal of the primary journey.
- [ ] Run the existing deterministic UI detector and an independent
  design/product review against the same fixed-viewport build. Reconcile
  disagreements with captured evidence before changing the UI.
- [ ] Record screenshots, viewport/seed, state source and unresolved findings.
  Claim visual parity only when an accepted reference and actual visual-diff
  result exist.

### 2026-07-26 Deep Learn checkpoint pass

- [x] Keep the default Demo lesson to two persistent work columns.
- [x] Remove the duplicate page-level Demo banner while retaining the global
  shell disclosure and a compact standalone sample/not-saved cue.
- [x] Join answer instructions, labeled input, cancel/check actions, and
  correct/hint/revision feedback into one inline checkpoint flow.
- [x] Prevent unit changes while an answer is being edited and remove the
  competing sticky unit footer from that transient state.
- [x] Inspect 1280×768 default/input/revision and 900×768 default states.
  Captures: `artifacts/ui-audit/2026-07-26/deep-learn-next/`.
- [ ] Verify the same states in the packaged WebView, Dark and reduced-motion
  modes before closing the whole-product visual gate.

### 2026-07-26 Supporting empty-state pass

- [x] Retain the visible product structure of a saved-record collection in
  History and a due queue in Review.
- [x] Remove the rejected three-column instructional strip. Keep one truthful
  count, one bounded empty body and one primary action.
- [x] Use HyperKnow, StudyFetch and RemNote for structural hierarchy only; do
  not add their trademarks, assets, copy or feature breadth.
- [x] Inspect History and Review at 1280×768 and History at 900×768. Captures:
  `artifacts/ui-audit/2026-07-26/supporting-empty-states-v2/`.
- [x] Verify the supporting-page shell and populated History/Review continuity
  in a fresh sidecar-bundled `Keen UI Audit.app` at the native 1180×740
  minimum. This pass also covers Feed task detail and a persisted Deep Learn
  Summary. Captures:
  `artifacts/ui-audit/2026-07-26/packaged-supporting-v2/`.
- [x] Replace raw bracket-string matrices/vectors discovered in packaged
  Review with native MathML plus accessible labels; recheck the rebuilt app.
- [ ] Reproduce the exact zero-record History/Review states in the packaged
  WebView and include them in the final whole-product keyboard/long-content
  pass. The current isolated native fixture contains saved records.

### 2026-07-27 Supporting-page density pass

- [x] Retain History and Review's shared collection/queue empty-state
  composition; no second redesign was needed.
- [x] Reframe Knowledge Base course libraries as compact filters rather than
  tall dashboard cards, moving the Sources section 60 px earlier at 1180×740.
- [x] Verify library filter and restore behavior against the Browser Demo
  source rows.
- [x] Inspect Knowledge Base at 1180×740 and 1000×720 with zero horizontal
  overflow and zero unnamed buttons. Capture:
  `artifacts/ui-audit/2026-07-27/supporting-consistency/knowledge-overview-1180x740.png`.
- [x] Inspect the compact Knowledge Base plus populated History and Review in a
  fresh ad-hoc `Keen UI Audit.app` at the native 1180×740 minimum in Dark.
  Capture:
  `artifacts/ui-audit/2026-07-27/supporting-consistency/packaged-knowledge-1180x740.png`.
- [ ] Re-run this supporting-page slice with packaged reduced-motion enabled;
  accepted-reference diff and pixel parity remain unclaimed.

### 2026-07-27 DeepTutor-structure packaged polish

- [x] Use the reviewed DeepTutor v1.5.1 screenshots only for bounded structure:
  fixed content starts, compact secondary navigation, progressive disclosure
  and one primary action.
- [x] Move Home's creation workbench upward without reintroducing a centered
  assistant persona or parallel creation entry.
- [x] Keep Knowledge Base import and course management collapsed until the
  learner requests them.
- [x] Replace stacked History empty states with one contextual destination.
- [x] Rewrite Settings headings and descriptions around learning actions,
  while keeping provider and security detail contextual.
- [x] Make disabled primary actions visually neutral and align shared select
  geometry with text inputs.
- [x] Inspect the fresh production-named packaged app across Home, Knowledge
  Base, Learning Feed, History, Review, Settings Status and Settings
  Capabilities. Captures:
  `artifacts/ui-audit/2026-07-27/deeptutor-polish/`.
- [ ] Complete the remaining fixed-reference visual diff, packaged
  Dark/reduced-motion and long persisted-content acceptance. No pixel parity is
  claimed.

### 2026-07-27 Round 03 final Light-mode pass

- [x] Inspect one fixed current-run browser set for Home, populated Knowledge
  Base, Feed/task detail, Deep Learn, History, Review and Settings Model.
- [x] Obtain an independent Pro review against the same evidence bundle.
  Result: `ACCEPT`, no must-fix items.
- [x] Build and relaunch a fresh bundled `Keen.app`; wait for
  `Learning core ready`.
- [x] Capture the exact packaged zero-data Knowledge Base, Feed, History and
  Review states plus Settings Status and Model at 1203×768.
- [x] Verify the packaged app's strict deep signature and bundled-sidecar
  smoke.
- [x] Keep the real Model/provider form while limiting visible Settings
  sections to Status, Model, Privacy and About.
- [ ] Repair or replace missing/stale accepted visual references before making
  any pixel-parity claim. The current visual-regression command exits 1 and
  remains documented rather than rebaselined.
- [ ] Complete packaged Dark, reduced-motion, long persisted-content and full
  keyboard-path acceptance in a later bounded verification slice.

### 2026-07-27 Agent Slice 05-A/B visual scope

- [x] Confirm these increments change persistence, internal outcome projection
  and host Playbook selection only; they add no page, navigation item, card,
  graph, dashboard or disclosure.
- [x] Retain the accepted Round 03 release-page evidence as the current visual
  baseline. No screenshot, visual-diff or parity claim is created for these
  backend-only slices.

### 2026-07-27 Slice 4 packaged restart visual boundary

- [x] Record Pro acceptance of the packaged service/durable-state restart
  result without misrepresenting it as a WebView capture.
- [ ] In a later bounded visual check, observe that the native WebView hydrates
  and renders one restored non-empty Session and exact route correctly. Pro
  explicitly marked this as independent and non-blocking for the accepted
  persistence gate.
- [ ] Keep Developer ID, notarization, packaged Dark/reduced-motion,
  fixed-reference diff and pixel parity unclaimed.

### 2026-07-27 Learning Feed selected-task repair

- [x] Remove the four-column task facts table and oversized step timeline.
- [x] Keep task status, course, due time, estimate and progress in one compact
  metadata line.
- [x] Prevent Today/Upcoming/Completed from truncating beside the course filter
  at 1203×768.
- [x] Replace internal Demo preview/inspection copy with learner-facing actions
  and one truthful no-save disclosure.
- [x] Compare the user-supplied rejected crop with the corrected runtime
  screenshot in
  `artifacts/ui-audit/2026-07-27/learning-feed-refine-comparison.png`.
- [ ] Recheck the same selected-task state in a fresh packaged Tauri build and
  against an exact authorized DeepTutor state before claiming parity.

### 2026-07-27 Core reader pages and adaptive-decision visibility

- [x] Converge Deep Learn on one collapsible path rail plus one bounded reading
  column; keep source detail on demand.
- [x] Replace formula-like prose with native MathML while keeping the recall
  input plain-language friendly.
- [x] Converge Review on one queue item, one reveal action and one FSRS rating
  decision; do not fabricate due cards for screenshots.
- [x] Converge History on compact question/session rows and remove oversized
  empty-state cards.
- [x] Surface the existing persisted adaptive reason in Deep Learn and the
  real Review scheduling provenance in Review.
- [x] Submit the same-viewport qualitative comparison and implementation
  evidence to ChatGPT Pro; Round 2 returned `ACCEPT`.
- [x] Build and launch the final production `.app`, wait for
  `Learning core ready`, and capture the truthful packaged-Light zero-data
  History and Review states.
- [x] Capture a real persisted adaptive-remediation proposal, accepted receipt,
  undone receipt and cold-restored state in the packaged WebView. Evidence is
  under
  `artifacts/orchestrator/round-05-agent-loop/native/`.
- [ ] Capture a real due Review item in the packaged WebView. Component and API
  tests cover it, but the current local profile has no truthful due item to
  photograph.
- [ ] Keep exact same-state pixel parity, packaged Dark/reduced-motion,
  Developer ID signing, notarization and learning efficacy unclaimed.

### 2026-07-27 Adaptive prerequisite loop packaged boundary

- [x] Observe the automatically started real-provider proposal before any plan
  write at 1203×768.
- [x] Observe native Accept as append-only plan version 2 and native Undo as
  append-only recovery version 3.
- [x] Fully quit the app and sidecar, cold launch, return through Home
  continuity, and observe the same undone receipt and plan version 3.
- [x] Confirm authoritative proposal/run/event counts remain `1 / 2 / 12`
  before and after cold launch.
- [ ] Do not treat these native captures as fixed-reference visual regression
  assets or a DeepTutor pixel-parity result.
